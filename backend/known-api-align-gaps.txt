# 前后端对齐 已知缺口台账 —— **只准变短**
#
# 由 backend/scripts/api-align.py --strict 执行（挂在 ops-web 的 npm run check:drift 下）。
# 新增一条 → 红。修好一条 → 也红，提示你把它从本文件删掉。
#
# 【为什么允许有已知缺口】
# 接入卡口时 B/C/D 合计 76 条，一次性清完不现实；但「全绿才过」会让卡口常红，
# 常红的卡口等于没有卡口。故照搬 known-menu-perm-mismatch.txt 的棘轮做法：
# 冻结现状为基线，只拦新增。
#
# 【三类的含义】
#   B <端点> <类型> <方向>:<字段>  出参形状与前端类型不符
#       「前端有后端无」= 界面读了后端不返回的字段，**运行期 undefined，用户可见**
#       「后端有前端无」= 后端返回了前端没声明的字段，浪费但不报错
#   C <动词> <路径>               后端有、运营端未调用（可能是待办，也可能是废端点）
#   D <类型> <端点>               后端出参在前端没有对应类型
#
# A 类（前端在调、后端没有 = 运行期 404）不进本台账：零容忍。

# ⚠️ 下面两条**不在本线修**，已转 i18n：
#   它们是「数据记录自带的多语言字段」，与界面文案外化是两件事。
#   仓里还没有「按当前 locale 从记录里取名」的统一口径 —— 没口径就接，
#   第二个页面必然出现第二套回落规则（ar 为空退英文还是退中文？）。
#   见 docs/technical/TDD-国际化i18n.md §6 P3（同处还列了另外三处同类字段）。
B GET /api/ops/alarms/codes AlarmCode 后端有前端无:messageAr
B GET /api/ops/alarms/codes AlarmCode 后端有前端无:messageEn
# ⚠️ 下面这条是**有意不接**，不是待办：
#   dedupKey 是「同源重复合并计数」的内部键（DDL: 去重键）。
#   它的可见结果是 count，前端已有并在告警号旁以「×47」显示。
#   把内部键也接进来只是把实现细节固化进契约。
B GET /api/ops/alarms/records AlarmRecord 后端有前端无:dedupKey
# ⚠️ 下面这条是**有意不接**，不是待办：
#   path 是后端为子树查询冗余的祖先路径（LIKE '/D1/%' 避免递归 CTE，见 IamDept 注释）。
#   前端是按 parent 客户端建树、filterDepts 也已自行保留祖先链，
#   接了只是把一份后端实现细节固化进契约。
B GET /api/platform/departments Department 后端有前端无:path
# ⚠️ 下面这条是**有意不接**，不是待办 —— 别把它「修」掉：
#   未审批的提现要按**现行**费率实时算（ops-web/lib/types/finance.ts 的 withdrawNetOf），
#   审核页给的必须是点「通过」那一刻真会扣的数；落库的 netAmount 是申请时的旧数。
#   接上它等于给审批人两个都叫「实际到账」的数，而放款时扣的是另一个。
B GET /api/trade/withdrawals Withdrawal 后端有前端无:netAmount
C GET /api/agent/accounts/{}
C GET /api/agent/commissions/{}
C GET /api/ops/inspection-plans/{}
C GET /api/ops/leads/{}
C GET /api/ops/sla-rules/{}
C GET /api/ops/venue-onboardings/{}
C GET /api/trade/settlements/{}

# ══════════════════════════════════════════════════════════════════════
# C 端（/mp/**）：后端建了、c-app 没调的 13 条
#
# 先澄清一个容易判错的前提：**这些不是「量尺拿错了比对对象」**。
# api-align 两边都扫（ops-web/lib/api/https 与 c-app/src/api），
# 而 c-app 确实一条都没调（全仓 grep，/mp/user/invoices 只出现在后端 DTO 里），
# 机器上也没有第二个 C 端工程。所以它们是真欠账。
#
# c-app 目前调了 20 个 /mp 接口 —— **接的全是「看」的那一半**。
# 下面按「差什么」分两组，处置方式完全不同。
#
# ── A 组：已上线功能的闭环缺一半（后端已实现 + c-app 页面已存在，差接线）──
# 用户现在就会撞上，不是「功能没做」，是「功能做了半截」。
#
# ── B 组：整块没做（c-app 连页面都没有）──
#                                    都要求提供账号删除入口），后端 logoff.apply 已就绪，不做上不了架
C GET /mp/user/invoices            # 开票（4 条一组）：to-B 需求，取决于目标市场口径
C POST /mp/user/invoices           #   同上
C GET /mp/user/invoice-titles      #   同上，抬头管理
C POST /mp/user/invoice-titles     #   同上
#
# 每接一条的完成定义：接 c-app/src/api 的 http 切片 → 去掉 mock 分支 →
# contract 测试覆盖 → **从本文件删掉对应行**（卡口对「修好了还留着」同样报红）。
# ══════════════════════════════════════════════════════════════════════

D AckResult POST /api/ops/alarms/records/{alarmNo}/ack
D ApplyResult POST /api/agent/applies
D ApplyView GET /api/agent/applies
D AssignmentLog POST /api/agent/assignments
D AuditLogEntry GET /api/platform/audit-logs
D CodeBatchRow GET /api/ops/device-code-batches
D CommandResult POST /api/ops/cabinets/{cabinetNo}/commands
D CouponTplVO GET /api/user/coupons
# ⚠️ 收藏页比「类型没对上」更严重：后端 FavoriteItem 只有 {siteNo,siteName,createdAt}，
#   而页面渲染的是门店卡片（cabinetNo/address/distanceM/availableBorrow/availableReturn）。
#   切到真后端时整列表只剩店名，key 用的 cabinetNo 还会全撞在 undefined 上，
#   点进去是 confirm?cabinetNo=undefined。要么后端把收藏出参补成门店卡片所需，
#   要么前端拿 siteNo 逐个查门店详情 —— 是个接口口径决策，单独排。
D FavoriteItem GET /mp/user/favorites
D InvoiceItem GET /mp/user/invoices
D InvoiceTitleItem GET /mp/user/invoice-titles
D Location GET /api/ops/locations
D LoginResp POST /api/auth/login
D MenuNode GET /api/auth/menus
D OkResult POST /api/ops/sites/{siteNo}/agents/{id}/remove
D OperatorMembership GET /api/auth/operators
D RentResult POST /mp/trade/orders/rent
D SharingStats GET /api/trade/site-sharing/stats
D UserCouponVO POST /api/user/coupons/{tplNo}/issue
D WorkOrderRef POST /api/ops/alarms/records/{alarmNo}/work-order
# ⚠️ 下面这条是**临时入口**，不是遗漏：
#   本该由共用调度器按 cron 回调（v4/07 的 JobHandler），但那要等 ai-shop 的任务服务
#   做完多系统改造 —— backend/pom.xml 的 enforcer 明令禁止依赖 ai.neargo.shop:*，
#   JobHandler 接口类根本引不进来。调度器就绪后把 PushService.sweepDue 挂成 JobHandler
#   即可，业务代码不动；届时这个端点与本条一起删。
C POST /api/user/push-messages/sweep-due
# ⚠️ 下面两条来自**另一条在做的线**（员工多角色，EmployeeRoles / iam_menu 那批）：
#   后端已返回 roleNo / roleNos，运营端类型还没声明。不是遗漏，是前后半没同时落地。
#   接上前端后棘轮会提示「已修复，请删掉」—— 由那条线的人删。

# ═══ 以下这批是 2026-09-25 修好对齐脚本的一个盲区之后**第一次显形**的，不是新出现的缺口 ═══
#
# 盲区：api-align.py 里 `checked` 这个去重集合是**全局**的，而同一个 VO 常常两端都用。
# 于是先被扫到的那个端点把类型名占掉，另一端就再也不比了 —— `/api/*` 先出现，
# `/mp/*` 的 c-app 那一半从来没被比过。三个类型正好落在这个洞里：
# NoticeVO / RentOrder / CsTicketVO。改成按 (受众, 类型名) 去重后它们才露出来。
# （CsTicketVO 那条已当场接上，所以下面实际剩 38 条。）
#
# ── RentOrder（22 条）：C 端订单页**每一个字段**都对不上 ──
#   前端读 amount/startAt/endAt/powerBankNo（大写 B）/siteNameBorrow/fees/timeline…，
#   后端返 feeAmount/rentStartAt/rentEndAt/powerbankNo/locationName/…。
#   切到真后端时订单列表与详情页**整页空白且不报错**。
#   修法不是改字段名了事：fees / timeline 后端根本没有（费用明细与状态时间线要新建投影），
#   siteNameBorrow/Return 也要后端补 join。属于一整块活，单独排。
#
# ── NoticeVO：已在同一批里改对（公告三语取列 + 已读态归还给站内信），本块不再有它 ──
#
# ── CsTicketVO：已在同一批的「报障进度」里接上，本块不再有它 ──
B GET /mp/trade/orders RentOrder 前端有后端无:amount
B GET /mp/trade/orders RentOrder 前端有后端无:cabinetNoBorrow
B GET /mp/trade/orders RentOrder 前端有后端无:cabinetNoReturn
B GET /mp/trade/orders RentOrder 前端有后端无:endAt
B GET /mp/trade/orders RentOrder 前端有后端无:fees
B GET /mp/trade/orders RentOrder 前端有后端无:freeFrozen
B GET /mp/trade/orders RentOrder 前端有后端无:powerBankNo
B GET /mp/trade/orders RentOrder 前端有后端无:siteNameBorrow
B GET /mp/trade/orders RentOrder 前端有后端无:siteNameReturn
B GET /mp/trade/orders RentOrder 前端有后端无:startAt
B GET /mp/trade/orders RentOrder 前端有后端无:timeline
B GET /mp/trade/orders RentOrder 后端有前端无:cabinetNo
B GET /mp/trade/orders RentOrder 后端有前端无:compensateAmount
B GET /mp/trade/orders RentOrder 后端有前端无:ejectCount
B GET /mp/trade/orders RentOrder 后端有前端无:feeAmount
B GET /mp/trade/orders RentOrder 后端有前端无:lastEjectAt
B GET /mp/trade/orders RentOrder 后端有前端无:locationName
B GET /mp/trade/orders RentOrder 后端有前端无:powerbankNo
B GET /mp/trade/orders RentOrder 后端有前端无:rentEndAt
B GET /mp/trade/orders RentOrder 后端有前端无:rentStartAt
B GET /mp/trade/orders RentOrder 后端有前端无:returnCabinetNo
B GET /mp/trade/orders RentOrder 后端有前端无:waivedAmount
