# 还没挂端点的表单 —— **只准变短，不准变长**。
#
# 表单上方写一行 `@form POST /api/...` 就算挂上了（建单与更新是两个端点就写两行）。
# 挂上之后，check-form-fields.py 才能核对「这张表单发出去的键后端接不接得住」。
#
# 为什么不自动推断：试过按文件里的 api.saveXxx 调用反查，一个页面里有七张表单时
# 根本对不准。而一条对错了的告警比没有告警更糟——它会让人开始忽略整个检查器。
#
# 新表单必须一开始就写注解；存量慢慢补，补一张从这里删一行。

# 2026-09-26：**清零**。48 张存量表单全部挂上了 @form（其中 dev/ui 的 DEMO_FIELDS
# 标的是 @form-none —— 组件展示页的假表单，onSubmit 只弹 toast，没有「发到哪」这件事）。
# 从此新表单必须一开始就带注解：这张表为空不是"没人管"，是"一张都不欠"。
