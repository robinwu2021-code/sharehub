# 菜单权限码 ↔ 后端端点权限码 不一致的台账 —— **只准变短**
#
# 由 MenuPermissionContractTest 执行。新增一条 → 测试红。
#
# 【为什么会不一致，且谁都发现不了】
# 菜单可见性用 `can(role, perm)` 过滤（ops-web/lib/nav.ts），
# 接口鉴权用 `@PreAuthorize("@perm.can(...)")`。
# 两边各查各的码，**没有任何东西要求它们一致**。表现只有两种：
#   · 看得见点不开（菜单码有、接口码没有 → 403）
#   · 能用但找不到（接口码有、菜单上无入口）
# 都不报错。功能权限清单 §末 的第 2、3 条问题就是这么来的，已在案但一直没修。
#
# 【条目格式】 菜单权限码 → 说明
#
# 依据：docs/requirements/功能权限清单.md（RBAC 真源）· scripts/feature-align.py

# ── 空了（2026-09-24）──
# 最后两条的结局记在这里，省得下一个人以为「一直是空的」：
#
#   · agent:settlement:read —— 代理门户「我的结算」点进去必 403，挂了一年。
#     结算三个**读**端点改成两码任一（finance:settlement:read 或 agent:settlement:read），
#     靠既有数据范围（stl_settlement / share_record → AGENT/agent_no）区分谁看到哪些行。
#     没有新开代理专用端点：那会让两端共用的页面按角色分叉去调不同接口。
#
#   · pricing:adjustment:read —— 菜单码在后端一个端点都没挂。
#     预约调价四个端点改挂真源表声明的 pricing:adjustment:*（原先是 pricing:rule:read/:config，
#     而 :config 根本不在真源表里）。副作用：BD 失去预约调价的读 —— 按真源它本来就只给 FIN。
#
# 这条台账**清零了，但别删文件**：它是「菜单码必须有端点强制」这条规矩的落点，
# 下次有人想加豁免时，空文件比不存在的文件更容易让人停一下。
