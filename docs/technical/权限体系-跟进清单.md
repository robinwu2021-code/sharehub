# 权限体系 · 跟进清单（待协调 / 待推进）

> 更新：2026-07-13 · 汇总权限体系已完成之外的所有待办，标 owner / 前置 / 状态，便于派活追踪。
> 图例状态：🟢可随时独立做 · 🟡需协调（他域/并行会话）· 🔴被外部阻塞（凭据/基建/发版）。

---

## A. 已完成（基线，不在跟进内）
运营端认证+RBAC(@perm)+数据权限(loc_site)+口径B动态权限+动态菜单接口 · C端5策略登录+unionid归并+属主鉴权 · 可切换 TokenStore(memory/ehcache/redis/mysql) · 模块化边界(ADR-015) · **U1 RBAC + U2 数据权限引擎已上推 neargo 并被 powerbank 采纳**。29 测试全绿。

---

## B. 我域内 · 可随时独立推进（不需协调）

| # | 动作 | 前置 | 状态 | 参考 |
|---|------|------|------|------|
| B1 | **U3 上推**：`SecurityUtils` + 口径B(`PermVersion`/`PrincipalRefresher`) 上推 neargo-common-security | 无（本地已有）| 🟢 | 权限能力上推清单 §6 |
| B2 | dev/ord/wo 数据权限**注册行**（`DataScopeRegistration` 各加 1 行）| 依赖 C1（列+数据）| 🟡阻在 C1 | 前端指引无关；见 C1 |
| B3 | IAM 后台**补全 CRUD**：角色增删改 / 菜单增改 / 员工↔角色 / 数据范围配置端点（现仅角色改权限）| 无 | 🟢 | 运营端权限-动态配置实现步骤 §5 |
| B4 | `iam_data_scope` 出 **REGION/SITE refs 数据** + 后台配置，激活区域/站点维度（现只 ALL/AGENT）| B3 | 🟢 | 同上 §8 |
| B5 | 审计切面 `OperateLog → iam_audit_log`（高危写留痕）| 无 | 🟢 | 运营端权限方案 §10 |
| B6 | （可选）Ehcache/MySQL 会话后端**补进 neargo** `SessionStore` 家族（A5）| neargo 发版 | 🟢低优先 | 上推清单 §2.A5 |

---

## C. 需协调 · 他域/并行会话在改（勿单方面动）

| # | 动作 | owner | 前置 | 状态 |
|---|------|-------|------|------|
| C1 | **dev/ord/wo 加 `agent_no`**：`DevCabinet`/`WoOrder`/`OrdRent` 实体 + 实际表列 + seeder/建单落值 | dev/wo/**trade** 域 | 表去规范化现状 | 🟡 并行会话在改这些域 |
| C2 | **前端动态菜单接入**：ops-web 消费 `/api/auth/menus`+`/permissions`（只加不改+静态兜底+开关灰度）| 前端 | 前端当前功能落地 | 🟡 前端在改别的功能 |
| C3 | **neargo U1/U2 PR 评审合并** | neargo 维护者 | PR 说明已给 | 🟡 待评审 |

> C1 完成后 → B2（我加注册行）即可让 AGENT 只见自己设备/订单/工单。
> C2 全流程见 `前端-动态菜单权限接入指引.md`（契约已冻结，前端可自助）。

---

## D. 被外部阻塞 · 依赖凭据/基建/合规

| # | 动作 | 阻塞点 | 状态 |
|---|------|--------|------|
| D1 | 登录接 **auth-core 真凭据**（pb_auth `cred_credential`，替换演示 {username,role}）| auth-core 集成 + pb_auth 建库 | 🔴 |
| D2 | C端 5 策略**外部真验签**：微信 code2session / Apple·Google JWKS（现 stub）| 各渠道 appid/secret/证书 | 🔴 |
| D3 | `TokenStore` 生产切 **Redis**（`token-store=redis`）| Redis 实例 | 🔴（代码就绪，配置即切）|
| D4 | **PII 落库** `pii_user` + 手机/证件 KMS（PDPL）| pb_pii 建库 + KMS | 🔴 |
| D5 | access-gateway 设备鉴权（TCP/MQTT/HTTP 验签）+ openapi AppKey 签名 | gateway 域实现 | 🔴 |

---

## E. 建议优先级（若要继续推进）

1. **B3 + B4**（IAM 后台全 CRUD + REGION/SITE 数据）——让"动态配置权限"真正完整可用，纯我域。
2. **B5**（审计切面）——高危操作留痕，合规刚需，纯我域。
3. **B1**（U3 上推）——把 SecurityUtils/口径B 也上推，收尾 neargo 复用。
4. 催 **C1**（dev/ord/wo 加 agent_no）——解锁 B2，让 AGENT 数据权限覆盖全资源。
5. **C2/C3** 按各自 owner 节奏；**D 类**待基建/凭据就位。
