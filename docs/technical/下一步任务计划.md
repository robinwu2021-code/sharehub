# 下一步任务计划

> 状态：2026-07-13（第二次更新）— 供接管者直接执行，无需翻阅其他文档即可定位任务
> 上下文：全局运行态见 [实现状态总表](./实现状态总表.md) §一；C端前端在 `c-app/`，运营端在 `ops-web/`，后端在 `backend/`

---

## 本次会话新增完成（2026-07-13 第二轮）

| 完成项 | 说明 |
|--------|------|
| 运营端数据层 | 新增 `UserRisk/UserBlacklist/AgentCommission/VenueOnboarding/SiteLifecycle` 五类实体全链路（types→mock/db→contract→mock.ts/http.ts） |
| 运营端页面补齐 | `/users` 风控+黑名单 tab (P2)；`/agents` 分润配置 tab (P1)；`/locations` Onboarding(P2)/生命周期(P3) tab |
| Dashboard 扩展 | 待办中心（3 快捷入口）、实时告警列表、排名榜单（Top 5 站点 GMV）|
| 构建验证 | tsc 零错 + vitest 48/48 + next build 全绿 |
| 文档回填 | 实现状态总表 + 运营端功能清单 均已更新 |
| **导航分类审查** | 基于 PDF V4 对照，识别 5 项问题（见下方「运营端导航优化建议」节） |

---

## 当前状态速览

| 层 | 现状 |
|----|------|
| **后端** | Spring Boot 4 单体；借还闭环 ✅ 落库；`/mp/auth/*` 认证 ✅；`/mp/nearby` / 钱包 / 收藏 等 C端业务端点 ⬜ |
| **c-app（C端前端）** | uni-app + Vue3 + Capacitor；全量 mock，借还/账户/地图 UI done；`VITE_USE_MOCK=0` + `VITE_API_BASE` 切真后端但端点未落地 |
| **ops-web（运营端）** | Next.js；`USE_MOCK=0` 可联调后端；14 模块前端 mock done（含本次 5 类新实体）；仅 loc/agt/dev/wo/ord 落库，其余内存 |

---

## 运营端导航优化建议（PDF 对照审查结论）

> 以下为本次会话新增分析，优先级：#1/#2 影响 P1 上线体验，建议在 P1 发布前处理。

| # | 问题 | 严重度 | 快速修法 |
|---|------|:------:|---------|
| 1 | `场地与拓展` 域含代理商管理，语义矛盾（代理是商业渠道，不是物理场地） | 中 | 改域标签为 **「商业合作」** 或 **「渠道与场地」**（`ops-web/lib/nav.ts` 第 ~30 行 `label` 字段） |
| 2 | P1 部署时「数据报表」菜单全灰——PDF 标「基础运营报表 P1」但 nav 所有 /reports tab 均 phase:2 | 中 | 加 1–2 条 phase 不设的基础报表条目，或在 nav.ts 注释记录「P1 报表由经营看板覆盖」 |
| 3 | 客服管理归在「用户与增长」，CS 是被动运营支撑而非增长工具 | 轻 | 域标签改为 **「用户与服务」** |
| 4 | 代理分润在 `/agents`，场地分润在 `/finance`，FINANCE 岗需跨域 | 轻 | 在 `/finance` 下加深链指向 `/agents?tab=commission` |
| 5 | 功能清单 §13「建设优先级 P1」与 §二·B「交付阶段 P2」同用 P1/P2 符号，易误读 | 文档 | 在 §13 标题注明「建设优先级（≠ 交付阶段）」 |

---

## P1（当前最高优先级）：C端后端联调打通

> 目标：让 `c-app` 脱离 mock，在真实 `/mp/**` 端点上跑通完整借还闭环。

### Task 1 — `/mp/nearby/cabinets` 端点（C-MAP）
**文件**：`backend/powerbank-app/src/main/java/ai/neargo/powerbank/mp/`

- 新建 `GET /mp/nearby/cabinets` Controller，接收 `lat/lng/keyword/returnable`，聚合 `loc_site` + `dev_cabinet` 影子库存，返回 `List<NearbyCabinetDto>`（字段见 `c-app/src/types/index.ts:NearbyCabinet`）
- 字段映射：`siteNo/siteName/address/lat/lng`（来自 `loc_site`）、`cabinetNo/availableBorrow/availableReturn/pricePerHour/currency/status`（来自 `dev_cabinet`，取 siteNo 下可借/可还台面计数）
- 鉴权：C 池 Bearer（`ConsumerTokenAuthFilter`），guest 也可访问（permitAll 或 optionalAuth）
- 测试：`NearbyApiTest`（`MockMvc`，至少：有结果、keyword 过滤、returnable 过滤）

### Task 2 — 前端路径对齐（C-AC）
**文件**：`c-app/src/api/http-client.ts` 或 `c-app/src/api/http.ts`

- 后端登录路径是 `/mp/auth/login`；前端契约 `login` 端点指向 `/mp/auth/login` ✅（已在 `http.ts` 中）
- 当前前端 `VITE_USE_MOCK=true`，切换为真实后端步骤：
  1. 在 `c-app/.env.local` 追加：`VITE_USE_MOCK=false` + `VITE_API_BASE=http://localhost:8080`
  2. 确认 `http-client.ts` baseURL 读 `VITE_API_BASE`
  3. OTP 测试：后端 `sendOtp` → 检查 Redis 有 key `otp:{phone}:{scene}`
- 验收：H5 登录页能成功换 C 池 Bearer，`/me` 显示真实 profile

### Task 3 — C端 profile 可写端点（C-ME）
**文件**：`backend/…/mp/web/UserMpController.java`

- `PATCH /mp/user/profile`：接收 `{nickname, email}`，更新 `usr_user`，返回最新 `UserProfileDto`
- `GET /mp/user/favorites` / `POST /mp/user/favorites/{siteNo}`：收藏表（`usr_favorite`，用户+siteNo 唯一，toggle 语义）
  - 建表 DDL：`CREATE TABLE usr_favorite (id BIGINT, c_user_no VARCHAR(32), site_no VARCHAR(32), created_at DATETIME, PRIMARY KEY(id), UNIQUE KEY uk_user_site(c_user_no, site_no))`

### Task 4 — 钱包端点（C-WA）
**文件**：`backend/…/mp/web/WalletMpController.java`

- `GET /mp/user/wallet` → 查 `usr_wallet`（balance/bonus/deposit/frozen/currency）
- `GET /mp/user/wallet/txns?page&size` → 查 `usr_wallet_txn`（type/title/amount/currency/at，分页）
- 建表 DDL：
  ```sql
  CREATE TABLE usr_wallet (id BIGINT, c_user_no VARCHAR(32) UNIQUE, balance DECIMAL(12,2), bonus DECIMAL(12,2), deposit DECIMAL(12,2), frozen DECIMAL(12,2), currency CHAR(3), PRIMARY KEY(id));
  CREATE TABLE usr_wallet_txn (id BIGINT, txn_no VARCHAR(32) UNIQUE, c_user_no VARCHAR(32), type VARCHAR(16), title VARCHAR(128), amount DECIMAL(12,2), currency CHAR(3), created_at DATETIME, PRIMARY KEY(id), KEY idx_user(c_user_no));
  ```

---

## P1（并行）：运营端内存域落库

> 目标：核心业务数据不因重启丢失。优先 trade/finance 两域。

### Task 5 — trade 域分层 + 落库（已有 ord_rent 表）

`ord_rent` 表已建，`RentOrderService` 状态机已通测试。补：

- `ord_rent` Mapper/Service 层清理（移除 `SeedData` 内存引用）
- `GET /orders` 列表改为 `SELECT` + `PageHelper` 分页，`keyword` 走 LIKE
- `POST /orders/{no}/intervene`（干预）写库状态机迁移（已有实现，确认走 Mapper）
- 补测试：`OrderServiceTest`（`@SpringBootTest` + H2 / Testcontainers）

### Task 6 — finance 域落库

- 建表：`fin_share_rule`（分润规则）、`fin_ledger`（账务记录）、`fin_settlement`（结算批次）、`fin_withdrawal`（提现申请）
- 提现审核 `POST /withdrawals/{no}/audit` 改为写库（状态机：PENDING → APPROVED/REJECTED）
- 分润计算触发点：`ord_rent` 结算事件 → `DomainEventPublisher` → `FinanceEventListener` 创建 `fin_ledger` 记录

---

## P2：横切地基补全

### Task 7 — IdGenerator 落地（T0.3）
- 各域现在手拼业务键（如 `"R" + nano`），替换为统一 `IdGenerator.nextNo(prefix, epoch)`
- 影响：trade/wo/finance/user/marketing 域所有 `nextNo` 调用
- 文件：`backend/powerbank-common/src/main/java/ai/neargo/powerbank/common/id/IdGenerator.java`

### Task 8 — iam 权限落库（T0.7）
- 现在 `RolePerms.MAP` 是静态 Map；改为从 `iam_role_permission` 表加载（启动预热 + 编辑后刷新）
- 建表 DDL：`iam_role_permission(id, role_code, permission_code, PRIMARY KEY(id))`
- 入口：`ops-web /system/roles`（角色权限配置页）已有 mock，接真端点后可操作

### Task 9 — 审计切面（T0.8）
- `@OperateLog` 注解 + AOP 切面 → 写 `audit_log` 表
- 运营端 `/audit-logs` 页已有 UI（读 mock），接真端点后可用

---

## P2：c-app 待补功能

> 前端 mock 已完成，等后端端点落地后联调，或优先完善 UI。

| 任务 | 文件 | 说明 |
|------|------|------|
| 真实 OTP 发送 | `c-app/src/pages/login/index.vue` | 前端已接 `sendOtp`，后端 OTP service 已通，仅需确认端点路径 + Redis config |
| ~~收藏门店：首页/门店详情 toggleFavorite~~ ✅ | `c-app/src/pages/home/index.vue` + `pb-store-sheet.vue` | 已接：门店弹层 + **列表卡片** 均有收藏心，`toggleFavorite(siteNo)` 同步列表/弹层状态，Added/Removed toast。`NearbyCabinet.favorite` 新增（可选），mock `nearbyCabinets`/`listFavorites` 回填。i18n(en/zh/ar) 补 `favorites.added`。H5 实测通过 |
| ~~scan to borrow 优化~~ ✅ | `c-app/src/pages/home/index.vue` | 登录门槛(`ensureLogin`：未登录 toast+跳登录)、防重复点击(`scanning` 态 + FAB 置灰)、H5 无可借柜机友好提示(`home.noBorrowable`)。i18n 补 `home.loginToBorrow`/`noBorrowable`。H5 实测：guest 拦截跳登录、登录后扫码→confirm(CAB-DXBM-01) 通过 |
| 消息推送注册 | `c-app/src/app-lifecycle.ts`（待建） | App 启动时调 `/mp/user/push-token` 注册 FCM/APNS token |
| Capacitor Google Maps API Key 白名单 | Google Cloud Console | APK 构建前：在 API Key 限制中加 `capacitor://localhost/*` 和 `https://localhost/*` |

---

## 环境快速启动（接管者必读）

```bash
# 后端（JDK21，MariaDB pb_core 需启动）
cd backend
JAVA_HOME=<jdk21路径> mvn -pl powerbank-app spring-boot:run
# http://localhost:8080

# c-app（H5 开发）
cd c-app
cp .env .env.local          # 首次：复制 env 模板
# 编辑 .env.local：
#   VITE_USE_MOCK=true      # mock 模式（默认）
#   VITE_GMAPS_KEY=<key>    # Google Maps key（已在本地 .env.local，不入 git）
npm run dev:h5              # http://localhost:5174

# c-app（App，需 Android Studio + 模拟器）
npm run build:app && npx cap sync android
# Android Studio 打开 android/ 目录 → Run

# 运营端
cd ops-web
USE_MOCK=true npm run dev   # mock 模式
USE_MOCK=false npm run dev  # 联调后端（需后端运行）
# http://localhost:3000
```

---

## 文件索引（接管时重点关注）

| 关注点 | 路径 |
|--------|------|
| C端 API 契约 | `c-app/src/api/contract.ts` |
| C端 mock 数据 | `c-app/src/mock/db.ts` |
| C端 http 实现 | `c-app/src/api/http.ts`（http 端点实现）|
| C端 类型定义 | `c-app/src/types/index.ts` |
| 后端 C端 BFF 设计 | `docs/technical/TDD-cend-bff.md` |
| 后端 认证设计 | `docs/technical/TDD-认证鉴权.md` |
| 实现进度权威 | `docs/technical/实现状态总表.md` |
| API 端点文档 | `docs/api/README.md` |
