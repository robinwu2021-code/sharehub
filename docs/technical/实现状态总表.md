# 实现状态总表（SSOT · 功能实现进度权威）

> 状态：**活文档（Living Doc）**，随实现推进更新
> 创建：2026-07-12 · 最近更新：2026-07-12
> 定位：本表是 powerbank **实现进度的唯一权威**。设计权威见各需求/领域/API 文档；本表只回答「**设计中的每项功能，现在实现到什么程度**」。

---

## 〇、使用协议（强约束 · 对齐 [项目开发规范](../../CLAUDE.md) / project-dev-standards）

1. **文档驱动**：任何功能开发前，先在本表定位对应行 → 读关联需求/TDD → 按 [P5] 写/更新 `docs/technical/TDD-*.md` → 编码 → 测试 → **回填本表状态**。
2. **完成的定义**：一行标 ✅ 必须同时满足：后端可用 + 落库（若该域已进入持久化阶段）+ 自动化测试通过 + 前端联调（若有前端）。缺一为 🟡。
3. **状态回填是收尾动作**：每次 PR/提交涉及功能变更，必须更新本表对应行的「状态」与「测试」列，并在文末「变更日志」追加一行。
4. **图例**：
   - ✅ 已实现（可用 + 有测试/联调）
   - 🟡 部分（骨架 / 内存态未落库 / 只读 / 未分层 / 前端 mock）
   - ⬜ 未实现（仅设计文档）
   - 🔴 阻塞 / 有已知缺陷
   - 🔒 休眠口子（有设计，MVP 不做）

---

## 一、全局运行态

| 项 | 现状 | 说明 |
|----|------|------|
| 后端 | ✅ Spring Boot 4.0 / JDK21 单体 `powerbank-app` | `backend/`；`:8080`；`JAVA_HOME=<jdk21> mvn -pl powerbank-app spring-boot:run` |
| 数据库 | 🟡 MariaDB `pb_core` 部分表 | loc/agt/dev/wo/usr 已落库；其余域仍内存 `SeedData`。`pb_pii`/`pb_auth` 未建 |
| 运营端 ops-web | ✅ Next.js，`USE_MOCK=0` 联调真后端 | `ops-web/`；`:3000` |
| C端 cend | 🟡 uni-app H5 闭环通（**mock**） | `cend/`；`:5173`；后端 `/mp` 业务端点大多未落地 → 仍走 mock |
| 认证鉴权 | ✅ 运营端 RBAC + C端属主 + 数据范围 | 见 §二 |
| 自动化测试 | ✅ 26 通过 | `backend/powerbank-app/src/test`：Smoke(3)+DataScopeAuth(5)+OperatorDailyFlow(7)+RbacMatrix(9)+DataScope(2) |

---

## 二、横切能力（地基）实现状态

| 能力 | 状态 | 实现位置 | 备注 |
|------|:---:|---------|------|
| 统一响应契约 `ApiResult{code,msg,data}` + `PageData` | ✅ | `common/ApiResponseWrapper` | 全域自动包裹 |
| 运营端认证（Bearer token） | ✅ | `auth/{SessionStore,StaffTokenAuthFilter,AuthController}` | `/api/auth/login` 换 token |
| RBAC 功能鉴权 `@PreAuthorize("@perm.can()")` | ✅ | `auth/{RolePerms,PermChecker,LoginUser.hasPerm}` | 通配镜像前端 `can()`；测试 RbacMatrix |
| C端认证（属主 + 5 登录策略） | ✅ | `user/consumer/*` + `auth/{ConsumerContext,ConsumerTokenAuthFilter}` | phone_otp 真 OTP；微信/苹果/谷歌 stub 验签；unionid 归并 |
| 数据范围（行级） | 🟡 | `auth/{DataScopeHandler,DataScopeTableRegistry,DataScope}` + `config/MybatisPlusConfig` | 仅 `loc_site` 注册（AGENT/REGION/SITE）；测试 DataScopeFlow。`ord_rent`/`wo_order` 有 agent_no 列但未注册 |
| 双安全链（`/mp` vs 运营端） | ✅ | `config/SecurityConfig` | @Order1 C端 / @Order2 运营端 |
| 分层（Controller→Service→Mapper→Entity） | 🟡 | loc/agt/dev/wo/**trade** 四件套✅ | user/platform/gateway 仍 Controller 直连内存 |
| 接入层分端 + common 复用 | 🟡 | `portal/{ops,mp}` 归位✅；envelope 复用 neargo Result/PageResult✅；BaseEntity(自增Long)+IdGenerator✅ | 见 TDD-接入层分端与common复用（M2/M4/M5余 待续）|
| 状态机（独立组件） | ✅ | `wo/WoStateMachine` + `trade/OrdStateMachine` ✅ | 借还 IN_USE→RETURNED→SETTLED→CLOSED |
| MyBatis-Plus 持久化 | 🟡 | loc/agt/dev/wo/usr | 见 §一 DB |
| IdGenerator（业务键） | ⬜ | — | 现各域手拼编号；T0.3 待做 |
| 审计切面 `@OperateLog → audit_log` | ⬜ | — | T0.8 待做；audit-logs 现读内存种子 |
| BaseEntity/审计字段自动填充 + CRUD 基类 | 🟡 | 实体各自带 created/updated/version/deleted | 无统一 `MetaObjectHandler`/`AbstractCrudService`；T0.9 |
| iam 权限落库 | ⬜ | 静态 `RolePerms.MAP` | 未接 `iam_role_permission` 表；T0.7 |
| 全量 DDL 入库 | 🟡 | loc/usr 建表；ddl/ 有全量 SQL | 其余域表未建；T0.6 |

---

## 三、运营端功能对照表（14 模块）

> 需求锚点：[运营端功能清单](../requirements/运营端功能清单.md) · [功能权限清单](../requirements/功能权限清单.md)（RBAC SSOT） · 领域/表锚点：[系统领域模型](./系统领域模型.md) · API 锚点：[api/README](../api/README.md)

| # | 模块 | 已实现子功能（API） | 持久化 | 分层 | ops-web 页面 | 测试 | 状态 |
|---|------|--------------------|:---:|:---:|-------------|------|:---:|
| 1 | 经营看板 dashboard | 经营总览 `GET /api/ops/dashboard`（GMV/订单/在线率/待办数/趋势） | 内存聚合 | — | 首页 `/` | Smoke, DailyFlow | 🟡 总览/实时告警/待办中心/排名榜单 ✅；快捷入口 ⬜ |
| 2 | 设备管理 device | 台账 `GET /cabinets`、详情+仓位 `GET /cabinets/{no}`、远控 `POST /cabinets/{no}/commands` | `dev_cabinet` ✅ | ✅ | `/devices` `/devices/detail` | DailyFlow(巡检/指令), Rbac | 🟡 台账/详情/仓位/远控骨架 done；充电宝/OTA/告警/库存调拨/批量 ⬜；指令为 gateway 占位 |
| 3 | 站点与点位 location | 站点 `GET/POST /sites`、点位 `GET/POST /locations`、场地方 `GET /venues`、合同 `GET /contracts` | `loc_site/location/venue/contract` ✅ | ✅ | `/locations` | DailyFlow(建站/点位), DataScope | 🟡 四类档案 read + 站点/点位 upsert done；门店 Onboarding(P2)/生命周期(P3) 前端 ✅ mock；场地/合同只读；CRM/坪效 ⬜ |
| 4 | 订单管理 order | 列表 `GET /orders`、详情 `GET /orders/{no}`、干预 `POST /orders/{no}/intervene` | **MariaDB ord_rent(120)** | RentOrderService | `/orders` | ConsumerRentFlow | ✅ 列表/详情/干预落库；退款 ⬜ |
| 5 | 计费定价 pricing | 计费模板 `GET /price-plans`（只读） | 内存 | — | `/pricing` | — | 🟡 模板只读；差异化/时段价规则 ⬜ |
| 6 | 财务分润 finance | 分润规则 `GET /share-rules`、账务 `GET /ledger`、结算 `GET /settlements`、提现 `GET /withdrawals` + 审核 `POST /withdrawals/{no}/audit` | 内存 | — | `/finance` | DailyFlow(提现审核), Rbac | 🟡 读通 + 提现审核（内存改状态）；复式账/对账/发票 ⬜；未落库 |
| 7 | 工单运维 workorder | 看板 `GET /work-orders`、派单 `POST /work-orders/{no}/dispatch`（状态机） | `wo_order` ✅ | ✅ | `/work-orders` | DailyFlow(派单+非法迁移), Rbac | 🟡 看板/派单/状态机 done；受理/处理验收/SLA/巡检计划 ⬜ |
| 8 | 用户运营 user | 用户列表 `GET /api/user/users`、拉黑 `POST /internal/user/credit/blacklist` | 列表内存 / `usr_user` 表(C端) | — | `/users` | Rbac(CS 越权) | 🟡 列表 + 拉黑（内存）done；风控用户/黑名单列表(P2) 前端 ✅ mock；信用分/会员/钱包 后端 ⬜ |
| 9 | 营销 marketing | 优惠券 `GET /api/user/coupons`（只读） | 内存 | — | `/marketing` | — | 🟡 券只读；活动/推送/裂变/广告位 ⬜ |
| 10 | 客服 cs | （复用 order 干预 / user 拉黑） | — | — | — | — | ⬜ 会话/报障受理独立能力未实现 |
| 11 | 代理商 agent | 档案 `GET /api/agent/agents` + upsert `POST /api/agent/agents[/{no}]` | `agt_agent` ✅ | ✅ | `/agents` | DailyFlow(BD建代理), Rbac(admin建代理) | 🟡 档案 CRUD + AGENT 数据范围 done；分润配置(P1) 前端 ✅ mock；划拨/结算/绩效/账号 ⬜ |
| 12 | 员工与权限 org | 员工 `GET /api/platform/employees`、角色 `GET /roles`、审计 `GET /audit-logs`（皆只读） | 内存 + 静态 RolePerms | — | `/employees` | — | 🟡 三者只读；iam 未落库；组织/数据权限维护/绩效 ⬜ |
| 13 | 数据报表 report | — | — | — | — | — | ⬜ 无端点 |
| 14 | 系统设置 system | 供应商 `GET /internal/gw/vendors` + 配置 `POST /vendors/{code}/config` | 内存 | — | `/system/vendors` | Smoke(vendors) | 🟡 供应商接入（内存）；通知模板/字典/地区/OpenAPI ⬜ |
| 15🔒 | 多租户 / 平台 | — | — | — | — | — | 🔒 休眠口子（tenant_id=MAIN 恒定） |

---

## 四、C端功能对照表（17 模块）

> 需求锚点：[C端功能清单](../requirements/C端功能清单.md)（ID `C-XX-NN`） · BFF 设计：[TDD-cend-bff](./TDD-cend-bff.md) · api/README §六
>
> **⚠️ 全局现状（2026-07-12 更新）**：C端**借还闭环后端已打通并测通**（`/mp/trade/orders/rent` 借出 → `/mp/trade/orders` 我的订单 → `/mp/trade/orders/{no}` 详情属主鉴权 → `/internal/trade/orders/{no}/return` 归还结算，落 `ord_rent`，`ConsumerRentFlowTest`）。**待补**：`/mp/nearby/cabinets`、`/mp/user/{wallet,coupons}`、支付(nearpay stub)、以及**前端路径对齐**（前端 `/mp/user/login` ↔ 后端 `/mp/auth/login`；cend 仍 mock 未接真后端）。

| # | 模块(ID) | 优先级 | 后端 `/mp` 端点 | 后端状态 | cend 页面 | 状态 |
|---|---------|:---:|----------------|:---:|----------|:---:|
| 1 | 账户与登录 C-AC | P0 | 后端 `/mp/auth/{login,otp,logout}` ✅（5策略+unionid） | 🟡 | `login` | 🟡 后端登录通；**前端调 `/mp/user/login` 与后端 `/mp/auth/login` 路径不符** → 前端 mock |
| 2 | 找柜与地图 C-MAP | P0 | `/mp/nearby/cabinets` | ⬜ | `index`(找柜/扫码) | ⬜ 后端 / 🟡 前端 mock |
| 3 | 扫码借出 C-RT | P0 | `/mp/trade/orders/rent` | ✅ | `order/confirm` | ✅ 后端(落库+属主) / 🟡 前端 mock |
| 4 | 免押与授权 C-DF | P0 | `/mp/trade/deposit/free` | ⬜ | — | ⬜ |
| 5 | 支付收银 C-PAY | P0 | `/mp/trade/pay` + `/notify/pay/nearpay` | ⬜ | — | ⬜（nearpay 未接） |
| 6 | 使用中 C-US | P0 | `/mp/trade/orders/{no}` | ✅ | `order/detail` | ✅ 后端(属主鉴权防IDOR) / 🟡 前端 mock |
| 7 | 归还与结算 C-RE | P0 | `/internal/trade/orders/{no}/return` | ✅ | `order/detail`(归还) | ✅ 后端(状态机+计费) / 🟡 前端 mock；支付 nearpay ⬜ |
| 8 | 订单与账单 C-BL | P0 | `GET /mp/trade/orders`（我的订单，属主过滤）✅ | ✅ | `orders` | ✅ 后端落库 / 🟡 前端 mock；账单/发票 ⬜ |
| 9 | 钱包与押金 C-WA | P1 | `/mp/user/wallet` + `/mp/user/wallet/txns`(待定) | ⬜ | `wallet`(独立页) | ⬜ 后端 / 🟡 前端 mock：余额/赠金/押金/免押冻结概览 + 流水(walletTxns) + 充值入口(pay recharge) done |
| 10 | 会员与权益 C-MB | P1 | `/mp/user/membership` | ⬜ | — | ⬜ |
| 11 | 优惠券 C-CP | P1 | `/mp/user/coupons` | ⬜ | — | ⬜ |
| 12 | 报障与客服 C-CS | P0 | `/mp/user/report` | ⬜ | `report` | ⬜ 后端 / 🟡 前端 mock |
| 13 | 发票 C-IV | P1 | `/mp/user/invoices` | ⬜ | — | ⬜ |
| 14 | 消息触达 C-MS | P0 | `/mp/user/push-token` `/messages` | ⬜ | — | ⬜ |
| 15 | 邀请与分享 C-SH | P2 | `/mp/user/invite` | ⬜ | — | ⬜ |
| 16 | 个人中心 C-ME | P0 | 后端 `GET /mp/user/profile` ✅（读）；`updateProfile`/`favorites` 端点待定 | 🟡 | `me/{index,profile,favorites}` | 🟡 profile 读通（前端仍 mock）；资料编辑(updateProfile 昵称/邮箱)、收藏门店(listFavorites/toggleFavorite) 前端 mock done |
| 17 | 蓝牙近场借还 C-BT | P2 | 设备/网关 | ⬜ | — | ⬜ |

---

## 五、南向 / 网关 / 三方开放

| 域 | 设计 | 状态 | 说明 |
|----|------|:---:|------|
| access-gateway 南向进程（TCP/MQTT/HTTP driver） | [TDD-access-gateway](./TDD-access-gateway.md) | ⬜ | 独立进程未建；运营端下发指令 `POST /cabinets/{no}/commands` 现为回执占位（`CMD+nano`），未真正经网关 |
| 供应商接入 gw vendor | api §四 | 🟡 | `/internal/gw/vendors` 列表/配置（内存） |
| 支付回调 `/notify/pay/nearpay` | ADR-005 | ⬜ | permitAll 已放行路径，处理器未实现 |
| OpenAPI 三方 `/openapi/**` | api §1.2 | ⬜ | 首期范围未定 |

---

## 六、下一步优先级（基于缺口，供排期）

按「补齐已开工域的分层+落库」→「打通 C端借还闭环后端」→「补横切地基」排序：

1. **🔴 C端借还闭环后端**（当前最大缺口）：`/mp/nearby/cabinets` + `/mp/trade/orders/rent` + `/internal/trade/orders/{no}/return` + `/mp/user/{profile,wallet}` 落地；**统一前端 `/mp/user/login` ↔ 后端 `/mp/auth/login` 路径**。委托 trade 域（见 TDD-cend-bff）。→ 使 cend 脱离 mock。
2. **trade 域四件套 + 落库**：ord_rent（订单状态机独立组件）+ price_plan + pay(Stub/nearpay) → 支撑运营端订单/计费 与 C端借还。
3. **finance 域**：acct 复式 + share + stl 落库 + 审核编排。
4. **user 域**：usr 用户/信用/钱包/券 落库（usr_user 已有，扩 credit/wallet/coupon）。
5. **横切地基**：IdGenerator（T0.3）、审计切面（T0.8）、iam 落库退役静态 RolePerms（T0.7）、全量 DDL 入库（T0.6）、数据范围注册面扩到 ord/wo（T0.5 续）。
6. **platform/gateway 分层 + 落库**；report/marketing/cs/org 深化；access-gateway 南向进程。

---

## 变更日志

| 日期 | 变更 | 关联 |
|------|------|------|
| 2026-07-12 | 建表；盘点全域实现现状（运营端 14 / C端 17 / 横切）。运营端 loc/agt/dev/wo 分层+落库+E2E 测试(26)；C端认证+profile+订单列表 done、其余 /mp 未落地 | [TDD-backend-layered-plan](./TDD-backend-layered-plan.md), [代码结构](./代码结构.md) |
| 2026-07-12 | ops-web 导航升级为三级（7 域 Rail → 模块 → 子功能，panel/miller 双呈现可切换+持久化；面包屑；RBAC 三级过滤；待建灰显零 404）。lib/nav.ts 为菜单 SSOT（对照 TDD 附录A），删旧 sidebar；引入 vitest（nav 纯函数 29 测全过）；tsc/next build/实机(ADMIN+VIEWER)验证通过。功能状态列无变化（纯导航呈现重构） | [TDD-运营端三级导航](./TDD-运营端三级导航.md) |
| 2026-07-12 | **ops-web 前端全量补齐（mock）**：多智能体 workflow 17 agent 建成——数据层 40 个新类型/mock/api 三件套；13 模块全部待建 tab 补成完整页（设备 6tab、财务 7tab、营销 7tab 等）+ 新建客服/数据报表页、系统设置转 6tab 页。nav.ts 去除全部 soon（附录A 待建项清零），nav 测试同步更新（29 过）。tsc/vitest/next build(19 路由静态导出)全绿 + 实机抽查（设备/报表/系统/客服）。**注：均为前端 mock，后端未动**（切真实后端仍走 lib/api/http.ts） | 本表 · [TDD-运营端三级导航](./TDD-运营端三级导航.md) |
| 2026-07-12 | ops-web UI 升级：Nunito 圆润字体（next/font 自托管）+ 中文系统回退；圆角加大、白底；8 套主题色块（顶栏调色板实时切换+localStorage 持久化+首帧防闪）。lib/stores/theme.ts + globals.css [data-theme] | ops-web/app/globals.css |
| 2026-07-12 | ops-web 扁平·色块分割改版：去分割线，色块（canvas/sidebar/secondary/muted/accent 由 --primary color-mix 派生）反衬分区 + 极轻阴影；分段 pill tabs、色块表头+zebra、填充式输入/徽章/按钮。统一所有页面走共享件（devices 机柜台账手搓表→DataTable，work-orders 计数 span→Badge） | ops-web/app/globals.css + components/ui/* |
| 2026-07-12 | ops-web 列表页体验统一：页头瘦身 + 悬停/点击伸缩 tab（TabHeader，标题=子功能名）；共享 `Toolbar`(搜索/筛选/新增) + `FormDrawer`(配置化字段) + `db.upsert/nextNo` mock CRUD。多智能体 workflow 给全部 ~40 列表 tab 铺「搜索 + 新增/编辑抽屉」(master/配置 tab 可增改、只读/日志/统计 tab 仅搜索)，新增 28 个实体 `save*`(db+契约三件套)。tsc/vitest(38)/build 全绿 + 实机(计费模板新增落库) | 本表 |
| 2026-07-13 | **c-app 真实 Google Maps 接入**（pb-map：Capacitor WebView 走 Maps JS API，有 `VITE_GMAPS_KEY` 加载真实地图/无则占位；用户/柜机 marker；首页 flex 布局重构填满高度 + FAB 覆盖）。key 存 `.env.local`（不入 git）。项目 `ng-vm-240813`，需启用 Maps JS/Places New/Geocoding 三 API | c-app/src/components/ui/pb-map.vue |
| 2026-07-13 | **c-app P1 账户页集群（前端 mock）**：新增 `me/profile`(昵称/邮箱编辑 updateProfile + 手机换绑占位)、`me/favorites`(收藏门店 listFavorites/toggleFavorite)、`wallet`(余额概览 + 流水 walletTxns + 充值 pay recharge)；契约扩 updateProfile/listFavorites/toggleFavorite/walletTxns（mock+http 双实现，http 端点标「待定」）；types 加 email/WalletTxn；Me tab 接线（资料卡可点、钱包卡跳转、收藏/钱包菜单入口）；zh/en/ar 三语补全。vue-tsc 全绿 + H5 实机验证（三页渲染/收藏取消/资料 dirty→保存持久化） | 本表 §四(9,16) · [[powerbank-c-app]] |
| 2026-07-12 | **i18n P1 前后端基础设施**：zh/en/ar 三语 + ar 全站 RTL。FE `lib/i18n`(catalog/useI18n/DIR)+locale store+首帧脚本+lang-switcher+`html lang/dir`；`ApiError`+http-client Accept-Language+`notify`toast+全局 mutation 错误提示；status.tsx/money/fmtTime/Pagination/Rail/面板/面包屑/login 本地化。BE `I18nConfig`(MessageSource+AcceptHeaderLocaleResolver)+`Messages`+改 GlobalExceptionHandler+`messages_{zh,en,ar}.properties`。默认 zh；ar 机翻 needs-review。tsc/build/vitest(38)/mvn/实机三语+RTL 全绿。**页面正文文案外化=P2 待做** | [TDD-国际化i18n](./TDD-国际化i18n.md) |
| 2026-07-29 | **运营端功能矩阵梳理（导航审查 #1–#5 收口）**：①域标签 `场地与拓展`→**渠道与场地**、`用户与增长`→**用户与服务**（nav.ts + nav-labels 三语 + TDD 附录A + 单测）；②`/finance` 加跨域深链叶「代理分润配置」→`/agents?tab=commission`（FINANCE 岗回链）；③报表 P1 灰显按「文档声明不动 nav」定调，nav.ts + 清单三处注明「P1 基础运营报表由经营看板承载」；④**功能清单 §三 重构为逐叶矩阵**（入口/建设优先级/**阶段**/前端实现度，与 nav 叶 1:1），逐页读码取证刷新实现列，**阶段 SSOT 从 §二·B 下沉至 §三**，TDD 附录A 状态列冻结为设计快照（消除三处状态重复）；⑤新增 **§三·B 横向缺口 G1–G7**（无删除/无导入导出/无批量/无地图/代理端缺位/工单闭环断裂/数据权限只有UI）；⑥补 8 个漏配三语的菜单标签 + 加「全菜单三语覆盖」单测防回归。tsc 零错 + vitest 50/50 + next build 全绿 + 实机验证（深链跳转/面包屑/EN 标签） | [运营端功能清单](../requirements/运营端功能清单.md) §三·§三·B · [下一步任务计划](./下一步任务计划.md) |
