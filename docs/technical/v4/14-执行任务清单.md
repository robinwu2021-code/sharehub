# 14 · 执行任务清单

> 所属：[v4 详细规划](./README.md) · 状态：**2026-09-23 按 [ADR-025](../ADR/ADR-025-直接复用ai-shop在跑的支付与任务服务-多系统改造.md) 重排**（原版按「各自部署实例 + 先建框架」排，已作废）
> 每完成一项在此打勾，并同步[实现状态总表](../实现状态总表.md)
> 汇总：[06](./06-认证与权限.md) A* · [07](./07-定时任务.md) J* · [08](./08-落地路线.md) S* · [09](./09-后端代码架构.md) C* · [15](./15-多系统多平台改造需求.md) MJ* / MP* / H*

---

> **2026-09-23 方向调整**（用户）：「目前核心是完成**架构方案和底层的功能代码以及代码框架，以及脚手架开发**，
> 具体的**业务代码，在业务模块梳理完成后再启动**」。
> 据此本清单的优先级重排：**底座 / 框架 / 脚手架优先**（B1 B2 B3 底座部分 B4 B7 + M0 + 框架线），
> 业务实现（B5 包归位后的 DTO 重构、逐实体域字段保护、S5–S8 的业务链路）**等业务模块梳理**。

## 〇、三个阶段

```
阶段一 · 本地跑通（现在）
  ShareHub：B1 安全止血 ─► B2 卡口与测试地基 ─► B3 实体请求体 ─► B4 目录重排 ─► B5 包归位 ─► B6 命名通用化
                                              └─► B7 跨进程地基 ─► H2 首批任务（进程内模式）
  ai-shop ：M0 任务多系统 ─────────────────► H1/H4 ShareHub 接入调度器 ─► S4
            M1 支付独立库 ─► M2 回调与收款进 pay-svc ─► M3 支付多系统 ─► M4 预授权 + AE 通道 ─► H3 ─► S6

阶段二 · 业务推进：S3 认证 · S5 协议对接 · S7 按摩椅 · S8 充电桩

阶段三 · 框架（**冻结中**，阶段一全部跑通且测试通过后解冻）
  K1 提交并打标签 ─► K2 搬迁 ─► K3 隔离统一 ─► K4 JDBC 适配器 ─► K5 基础设施 ─► K6 starter ─► K7 组件 ─► B8 切 2.0
```

**当前实测状态（2026-09-23）**：S0 的**后端**部分已修完（B1 ✅：固定码限 dev-mode、OTP 不回传、登录 fail-closed 且角色只由账号决定、内存令牌 TTL、`seed.enabled` 默认关、token-store 开关键名修正）；
**前端半边仍在**（`ops-web/lib/auth.ts:27` 写死 `role:"ADMIN"`、两端登出不调后端）→ W1，**需与后端同窗口上线**；
ai-shop 的 `ai-shop-pay`（:8083）与 `ai-shop-job` 已在生产跑，与 ShareHub 同一台腾讯云机器；
`neargo-framework` 本地目录已建成并验证可构建，现**原样冻结**。

---

## 一、需要你决定或动手的

| # | 事项 | 阻塞 | 推荐 |
|---|---|---|---|
| U1 | 把 [15 多系统多平台改造需求](./15-多系统多平台改造需求.md) 交给 ai-shop 会话排期 | M0–M4 → S4 · S6 | **先排 M0**（最小、解锁定时任务），M1 单独开一个窗口 |
| U2 | 首个 AE 支付通道选哪家（N-Genius / Tap / PayTabs） | M4 · S6 | 取决于签约 |
| U9 | **短信通道选型与接入**（阿里云 / MENA 通道）—— B1 关掉 OTP 后门后，**没有它 C 端在生产登录不了**（2026-09-23 用户定方案 A：照常上线，C 端演示暂停）。原属 N4「马上要用」，现升为 **C 端生产可用的前置** | C 端生产登录 · S7 | 与 U1 的 M0 一起提给 ai-shop，或本项目先按中性命名自写再回流 |
| U3 | 首家按摩椅厂商 | S7 | 取决于签约 |
| U4 | 钱包储值的 CBUAE 合规（SVF 牌照） | 钱包功能 | 上线前确认；不行则只做按次付费 |
| U5 | 库改名 `pb_*` → `sharehub_*` 的时间窗 | S4（D2） | 与 B6 命名通用化同一窗口前后 |
| U6 | **Nexus 收口**（不是新建 —— 2026-09-23 实测已有 `https://nexus.neargo.ai/nexus/`，NXRM **2.14.20-02**）：① 关掉 `releases` / `snapshots` 的**公网匿名读**（现在任何人都能下载 minipos / cbdc / qrpay 全部构件）；② 建**只读**与**部署**两个专用账号；③ **轮换 admin 口令**（本次在对话里明文给过）；④ 确认备份；⑤ 评估升级 3.x（2.x 已停止支持） | M0 的构件分发、N1–N2 | 先做 ①②③，其余另排 |
| U7 | 阶段三解冻的判据 | K1 | 建议：B1–B7 全绿 + M0 接通 + ShareHub 定时任务在共用调度器上按时跑 |
| U8 | 框架坐标残留风险确认：本线已定**从 `2.0.0` 起**（`ai.neargo:neargo-framework` 的 1.0.x 属 minipos / qrpay / cbdc 那条 Boot 3.3.2 + Sa-Token 的线）。需确认那些项目**都以固定版本依赖、不用版本区间**，否则它们升到 2.x 会拿到完全不同的栈 | N1 发布 | 发布前逐个确认 + 发布说明写明 |

> U6 提前了：原计划 Nexus 属于框架线，但 ShareHub 要依赖 ai-shop 发布的 `shop-job-api` / `shop-job-target` / `pay-client`，**没有私仓就只能靠本机 `mvn install`**，两个项目在同一台机器上开发时勉强可行，上 CI 或换机器就不行。
> 好消息是私仓**已经存在**（`nexus.neargo.ai`，组地址 `/nexus/content/groups/public/`，能代理到 `spring-boot-starter-parent:4.0.7`），只差权限收口与两个账号。详见 [10 §〇 · §六](./10-共享构件与Nexus.md)。

---

## 二、ShareHub 后端（本会话可直接做，不依赖任何外部）

| # | 任务 | 内容 | 依赖 | 验收 | 规模 |
|---|---|---|---|---|---|
| **B1** ✅ | **S0 安全止血**（A0 后端部分）—— **已完成 2026-09-23**（[TDD-auth-security-hotfix](../TDD-auth-security-hotfix.md)，138 测试全绿） | `000000` 只在 dev profile 生效、`/mp/auth/otp` 不再回传验证码 · 登录永远校验凭据、不采信前端传的角色 · `MemoryTokenStore` 加滑动过期与绝对上限 · 登出真正吊销令牌 · `seed.enabled` 默认 `false`、种子只在 demo profile | — | 任意账号 + 任意密码 → 401；非 dev 环境 `000000` 失败；令牌到期失效；空库启动不灌假数据 | 小 |
| **B2** 🟡 | **C0 卡口与测试地基**（2026-09-23 完成两项：**arch-guard 假绿修复** [TDD-arch-guard-fix](../TDD-arch-guard-fix.md) · **enforcer 依赖纪律** [TDD-enforcer-module-boundary](../TDD-enforcer-module-boundary.md)；**ArchUnit 已完成 2026-09-23**：`ArchitectureTest` 6 例 + `known-controller-direct-mapper.txt`——与 arch-guard.py 分工（那四条是正则能判的文本特征，ArchUnit 补字节码级真实依赖与**包循环**）。实测导入 1013 类：控制器直连 Mapper 16 处/1 类（入台账）、包循环 0、依赖 service.impl 0、字段注入 0，后三条零违规**当场锁死**（维持便宜、失去后恢复贵）。**剩 H2 测试隔离**，卡在 `pb_core_test` 尚未建库） | 修 `arch-guard.py` 假绿（`EXTRA_SRC` 未使用、G4 路径错）· ArchUnit `ArchitectureTest` + `known-*.txt` 写入现有违例 · enforcer `bannedDependencies` + `dependencyConvergence` · H2 + 由迁移生成表结构的 test profile（**测试不再写开发库 `pb_core`**）<br>⚠️ **不做父 POM 切换**（框架冻结，`sharehub-parent` 维持继承 `neargo-parent`） | — | 全量测试绿且不连开发库；故意写一个违例能被卡口拦住 | 中 |
| **B3** 🟡 | **C1 安全相关结构**（底座部分 2026-09-23 完成，[TDD-mass-assignment-hardening](../TDD-mass-assignment-hardening.md)：`AbstractCrudService` 集中加固 + 棘轮台账 + 卡口测试；**DTO 重构与逐实体域字段判定属业务代码，按 2026-09-23 新方向等业务模块梳理完再做**） | 78 处「实体直接作 `@RequestBody`」改 Req / Command，**先改带归属、状态、金额字段的实体**；`SeedData` 移出主代码 | B2 | `known-*` 台账该项下降到目标值 | 中–大 |
| **B4** | **R3 + C2 目录重排与三层拆分** | 按 [12 §4.2](./12-代码库与工程结构.md) 移入 `foundation/ contracts/ domains/ gateway/ apps/ support/`（artifactId 不变）· `sharehub-common` 拆 `common / store / auth` · `sharehub-svc-gateway` → `gateway/sharehub-gateway-kernel` · 新增 `sharehub-seed` · `sharehub-test-support` · 移植 ai-shop 中性件并登记 `known-pending-shared.txt` | B2 | 全量测试绿；[12 §5.2](./12-代码库与工程结构.md) 依赖规则生效 | 中 |
| **B5** | **C3 包归位** | 按 [09 §九](./09-后端代码架构.md) 逐模块 platform → core → ops → finance → app；控制器移入各域 `api/` | B4 | 分层类规则清零；胖控制器台账下降 | 大（可按模块并行） |
| **B6** | **S1 / C4 命名通用化** | [ADR-021](../ADR/ADR-021-通用设备命名与使用形态.md) 改名（19 张表的 `cabinet_no`、状态、接口、权限码）+ 命名卡口 | B5 | 命名棘轮基线建立；两个前端实机回归 | 大 |
| **B7** | **S2 跨进程地基** | Outbox 跨进程投递 + 消费去重 · 服务凭证过滤器 · `traceparent` 透传 | B4 | 两进程冒烟：事件至少一次送达且不重复处理 | 中 |

**并发注意**：B4 会移动整个 `backend/`，其他会话同时改后端会大面积冲突 —— 动手前确认没有别的会话在改 `backend/`（CLAUDE.md 并发纪律），做完立即提交。

---

## 三、ai-shop 改造（需求见 [15](./15-多系统多平台改造需求.md)，ai-shop 会话实施，ShareHub 验收）

| 批 | 内容 | 解锁 | ShareHub 怎么验收 |
|---|---|---|---|
| **M0** | 任务服务多系统：MJ1–MJ2 加 `system` 列与唯一键 · MJ3 targets 按系统分组、每目标一把令牌 · MJ4 声明同步按系统 · MJ5 运营端按系统过滤 · MJ7 收 `shop-job-target` · MJ8 发布三个构件 | S4 定时任务 | 同一调度器里两系统任务名相同不冲突；ShareHub 运营台看不到 ai-shop 的任务 |
| **M1** | 支付独立库（MP1，**单独窗口 + 回退脚本**） | 后续全部支付改造 | `pay-svc` 连自己的库；主应用不再跑 pay 迁移 |
| **M2** | 回调与收款入口进 pay-svc（MP2–MP3） | ShareHub 能付款 | 独立形态下收款可用；回调不经 ai-shop 业务进程 |
| **M3** | 支付多系统：MP4 表加 `system` · MP5 通道凭据进库按 `(system, market, channel)` · MP6 业务字段泛化 · MP7 单号带系统前缀 · MP8 令牌即系统身份 · MP9 分系统对账 | 两系统共用一个支付服务 | 拿 SHAREHUB 令牌查 SHOP 流水 → 404；两套通道凭据同时装配互不可见 |
| **M4** | MP10 预授权族 · MP11 首个 AE 通道（需 U2）· MP12 `pay-client` 独立模块 | S6 免押借还 | 借出预授权 / 归还请款在通道沙箱闭环 |

**顺序**：M0 与 M1 可并行（一个在 job、一个在 pay）。M0 最小、解锁最快，**建议先排**。

---

## 四、ShareHub 对接共用服务

| # | 任务 | 依赖 | 验收 |
|---|---|---|---|
| **H2** | 首批 `JobHandler`（`outbox-dispatch` · `session-purge` · `order-pending-sweep` · `order-overdue-scan`），先用**进程内模式**跑 | B7 | 单测 + 本机进程内调度实跑 |
| **H1** | 三个目标进程装 `shop-job-target`：`SHAREHUB`（单体）· `GATEWAY`（协议对接）；`PAY` 由服务自带 | M0 · H2 | 端点测试：令牌错 401、抢锁失败 409、返回体无信封 |
| **H4** | 在共用调度器登记 `SHAREHUB` 系统与目标地址、令牌；配置放 `deploy/instances/`，密钥走环境变量 | M0 · H1 | ShareHub 的任务在共用调度器上按时回调 |
| **H3** | `PaymentPort` 远程实现（经 `pay-client`）+ 支付编排 + 支付不可用时的降级 | M4 | 借还与充值链路在沙箱闭环 |
| **H5** | 删掉 `sharehub_pay` / `sharehub_job` 两个库的规划与部署配置（数据在共用服务里按 `system` 隔离） | M0 · M3 | [03 数据库](./03-数据库.md) 只剩四个库 |

---

## 五、前端会话（本会话不做，列出以便交接）

| # | 任务 | 依赖 |
|---|---|---|
| W1 | S0 前端部分：删掉 `ops-web/lib/auth.ts:27` 写死的 `role: "ADMIN"`；两端登出调后端；401 跳登录 | **与 B1 同时上线** |
| W2 | c-app 信封与后端对齐（c-app 仍按 `msg` / `records` 解析，后端是 `message` / `list`） | — |
| W3 | A5 ops-web 登录态与权限接线 · A6 c-app 登录接线 | S3 |
| W4 | 运营端「定时任务」页（按系统过滤，只显示 SHAREHUB） | M0 · H4 |

---

## 六、阶段二：业务推进（阶段一之后，可并行）

| 阶段 | 内容 | 依赖 |
|---|---|---|
| **S3** 认证对齐 | A1–A7：认证模块化、会话外置与分池、真实账号与凭据、功能点权限模型、两端接线、卡口 | B7；`neargo-auth-store` 部分**改为本地同构实现**（框架冻结期间不等下沉） |
| **S4** 库改名 + 任务 | `pb_*` → `sharehub_*`（D2）；ShareHub 任务全部搬到共用调度器；运营端任务页 | B6 · M0 · H4 · U5 |
| **S5** 协议对接服务 | `transport-http` + 首家厂商驱动 + 指令生命周期；`sharehub_gw`；目标 `GATEWAY` 接入调度器 | B7 · H1 |
| **S6** 支付接入 | ShareHub 侧 `RemotePaymentPort` + 支付编排；借出预授权 / 归还请款 / 钱包充值 | M4 · H3 |
| **S7** 按摩椅试点 | `MassageChairModule` · `SESSION` 族驱动 · 套餐计价 · 先付流程 | S5 · S6 · U3 |
| **S8** 充电桩 | `transport-ocpp` · `EV_CHARGER` 族 · kWh 计价 · 预约占位 | S7 |

---

## 七、阶段三：框架（**冻结中**）

> ADR-025 决定 5：`neargo-framework` 暂缓。本地 `/Users/robin/work/ai/neargo-framework` **原样保留**（20 个提交的历史、11 个模块 `mvn -o verify` 全绿，版本 `2.0.0-SNAPSHOT`），但：
> **不提交、不建远端、不发布、不 install、不做 2.x 重排**；ShareHub 的父 POM 维持 `neargo-parent`。
> 方案（[13](./13-neargo-framework独立仓库.md) · [框架 design/01](../../../../neargo-framework/docs/design/01-分层粒度与多ORM.md)）保留不作废。

解冻判据（U7）：B1–B7 全绿 + M0 接通 + ShareHub 定时任务在共用调度器上按时跑。解冻后顺序：

| # | 任务 | 依赖 |
|---|---|---|
| K1 | 提交工作区 · 打 `v2.0.0`（原样抽出的现状，本地已是 `2.0.0-SNAPSHOT` 且构建全绿）· 开 `2.0.x` 分支 · 主干升 `2.1.0-SNAPSHOT` · 建远端并推送 | 解冻 · U8 |
| K2 | F2a 搬迁：目录按级重排、改名、上下文下沉 L0（只搬不改行为） | K1 + 框架 design/01 §九 七项拍板 |
| K3 | F2b 隔离统一：`IsolationRegistry` + MP 适配器 + TCK | K2 |
| K4 | F2c JDBC 适配器：`IsolatingJdbcOperations`（PoC 转正式） | K3 |
| K5 | F2d 基础设施：凭据改 JdbcClient；抽 `neargo-store` · `neargo-auth-store` · `neargo-security` | K3 · ai-shop 配合 |
| K6 | F2e starter · `neargo-archrules` · `neargo-test` · 1.x→2.x 映射表 | K5 |
| K7 | F2f 组件 `neargo-notify` · `neargo-media`（补 MENA 短信、S3 存储） | K2（可与 K3–K6 并行）· ai-shop 配合 |
| B8 | ShareHub 切框架 2.0：改 import、删副本、每域补 `XxxIsolation`、报表与网关批量写改 JdbcClient | K6 · B5 |

**冻结期间的代偿**：`neargo-notify` / `neargo-media` 未就绪，ShareHub 需要短信与图片上传时，先在 `sharehub-svc-platform` / `-ops` 内按中性命名自写，登记进 `known-pending-shared.txt`，K7 之后替换。

---

## 八、建议的第一批（今天就能开工）

| 顺序 | 任务 | 谁 |
|---|---|---|
| 1 | **B1 后端安全止血 + W1 前端安全止血**（同一次上线） | 本会话 + 前端会话 |
| 2 | **U1**：把 [15](./15-多系统多平台改造需求.md) 交给 ai-shop，先排 M0 | 你 |
| 3 | **B2 卡口与测试地基** | 本会话 |
| 4 | **U6**：定 Nexus（M0 的构件要靠它分发） | 你 |
| 5 | B7 + H2：跨进程地基与首批任务（进程内模式，不等 M0） | 本会话 |
