# 功能矩阵 → 前端 → 后端 逐层贯通对照表

> 生成日期：**2026-08-04** · 生成方式：逐层扫描工作树真实代码（非文档转抄）
>
> **统计口径**
> - **前端**＝mock 层完成度（F0–F3，取 [前端静态实现-逐菜单待办清单](./前端静态实现-逐菜单待办清单.md) §二 工作树版本）。F3 只代表「前端 + mock 功能完整」，不代表可上线。
> - **后端端点**＝工作树实际存在的 `@GetMapping/@PostMapping/...`（backend/ 五个 svc 模块 + sharehub-app portal 层全量 grep），并用 `ops-web/scripts/check-backend-parity.py` 只读运行一次交叉验证（本次输出：**前端契约 265 · 后端端点 353 · 已对上 251 · 缺 7 · 动词不符 7 · 严格覆盖率 94%**）。
> - **落库**＝**DDL + Mapper 双证据**：Flyway 迁移（`backend/sharehub-app/src/main/resources/db/migration/V1–V30`，135+ 张业务表已全部建齐）确认表在，且该端点的 Service 真经 Mapper 读写该表。**只有表没有链路、或链路存在但读内存种子，都不算落库**，逐行注明。
>
> **本表为快照**。SSOT 仍是 [运营端功能清单 §三](../requirements/运营端功能清单.md)（98 行菜单，行序与 `ops-web/lib/nav.ts` 1:1）与 [实现状态总表](./实现状态总表.md)。实现状态总表 §三 的多数行停留在 2026-07-12（如「finance 内存」「report 无端点」），与当前代码冲突处**一律以本表扫描结果为准**并已注明。
>
> ⚠️ **`ops-web/lib/api/https/*.ts` 的「后端缺口」标注已部分过时**（标注写于 2026-07-30，后端此后又补了一轮）：报表域 10 端点、告警重发、工单 accept/handle/complete/reject/rework、巡检 run、地区树、模板 preview/test-send、OpenAPI reset-secret、发送记录 resend、会员权益/次卡端点、活动与广告启停、CRM 跟进、合同附件、差异化/时段价——**这些标注说缺、实际已实现**。本表按实际代码取数；仍然真缺的只有下文「缺口台账」里的 14 条。

**图例**：✅ 存在且走真实链路 · ◐ 部分（读写分家 / 空壳 / 参数不认）· ⬜ 不存在 · `深链` 复用其它行 · 「内存」= 端点在但读写 `SeedData`（`backend/sharehub-app/.../seed/SeedData.java`，纯内存 `@Component`，重启即失）

各表列：| 子功能 | 入口 | 前端 | 契约方法 → API 路径 | 后端端点 | Controller | Service | Mapper/Entity | DB 表 · 落库 |（模块名在表标题）

---

## 1. 经营看板（1 行）

**贯通度小结：前端 1/1 F3 · 后端端点 1/1 · 分层 ⬜（Controller 直连内存）· 落库 0**

| 子功能 | 入口 | 前端 | 契约方法 → API 路径 | 后端端点 | Controller | Service | Mapper/Entity | DB 表 · 落库 |
|---|---|:--:|---|:--:|---|---|---|---|
| 经营总览 | `/` | F3 | `getDashboard → GET /api/ops/dashboard`（页内快捷动作复用 sendCommand / createWorkOrder / listOrders） | ✅ | `OpsController#dashboard` | ⬜ **Controller 直连内存**（`db.dashboard()`，OpsController.java:51） | ⬜ | ⬜ 无专表，内存聚合。GMV/在线率等未从 ord_order/dev_cabinet 实算 |

---

## 2. 设备管理（8 行）

**贯通度小结：前端 8/8 F3 · 后端端点 22/23（缺 saveCabinet）· 分层 ✅ 四件套 · 落库 ✅ dev_\*（但设备日志服务空壳、指令下发为占位不落库）**

| 子功能 | 入口 | 前端 | 契约方法 → API 路径 | 后端端点 | Controller | Service | Mapper/Entity | DB 表 · 落库 |
|---|---|:--:|---|:--:|---|---|---|---|
| 设备台账 | `/devices` | F3 | `listCabinets/getCabinet → GET /api/ops/cabinets[/{no}]`；`saveCabinet → POST /api/ops/cabinets`；`importCabinets → POST /api/ops/cabinets/import`；archive/unarchive | ◐ | `OpsController` | `CabinetService`(svc-core) | `CabinetMapper`,`SlotMapper` / `DevCabinet`,`DevSlot` | `dev_cabinet`,`dev_slot` ✅（`site_no` 列已由 V9 补齐，S8 时的「DDL 缺列」结论已过时）。**缺口：`saveCabinet` 建档/编辑 upsert 后端没有**（只有 GET/import/archive），前端新增机柜抽屉 `USE_MOCK=0` 必 404 |
| 充电宝管理 | `?tab=powerbanks` | F3 | `listPowerbanks/savePowerbank → GET·POST /api/ops/powerbanks[/{no}]`；archive/unarchive | ✅ | `DeviceController` | `PowerbankService` | `PowerbankMapper` / `DevPowerbank` | `dev_powerbank` ✅ |
| 实时监控 | `?tab=monitor` | F3 | `listCabinetMonitor → GET /api/ops/cabinet-monitor`；`autoRaiseWorkOrders → POST /api/ops/alarms/auto-work-orders` | ✅ | `DeviceController` / `AlarmController` | `MonitorService` / `AlarmService` | `ShadowMapper`,`HeartbeatMapper` | `dev_shadow`,`dev_heartbeat` ✅ 表在链路在；**`dev_heartbeat` 无上报写入方**（南向网关进程未建），在线率为当前快照 |
| 远程控制·指令记录 | `?tab=commands` | F3 | `sendCommand → POST /api/ops/cabinets/{no}/commands`；`listCommandRecords → GET /api/ops/command-records` | ◐ | `OpsController` / `InventoryController` | `CabinetService` / `CommandLogService`(svc-gateway) | `GwCommandLogMapper` / `GwCommandLog` | `gw_command_log` ✅ 表+读链路在；**下发是占位**：`CabinetServiceImpl.sendCommand` 直接返回 `"CMD"+nanoTime`（L54-57），不经网关也**不写指令记录** → 下发成功但记录页查不到 |
| 设备日志 | `?tab=logs` | F3 | `listDeviceLogs → GET /api/ops/device-logs` | ◐ | `DeviceController` | `DeviceLogService` **空壳**（Impl L35 恒返回空页；SQL 设计只在注释里） | ⬜（设计为 gw_command_log ∪ gw_message_log） | 表在（`gw_command_log`,`gw_message_log`）· 读链路 ⬜ **待核**：UNION 读模型未实现 |
| 库存调拨 | `?tab=inventory` | F3 | `listInventoryTransfers/saveInventoryTransfer → GET·POST /api/ops/inventory-transfers[/{no}]` | ✅ | `InventoryController` | `InventoryTransferService` | `InvTransferMapper`,`InvTransferItemMapper`,`InvStockMapper`,`InvWarehouseMapper` | `inv_transfer`,`inv_transfer_item`,`inv_stock`,`inv_warehouse` ✅ |
| 固件 OTA | `?tab=ota` | F3 | `listOtaRollouts/saveOtaRollout`、`listOtaReleases/saveOtaRelease`、`listOtaTasks → /api/ops/ota-*` | ✅ | `DeviceController` | `OtaService` | `OtaReleaseMapper`,`OtaRolloutMapper`,`OtaTaskMapper` | `dev_ota_release`,`dev_ota_rollout`,`dev_ota_task` ✅（实体 `version`/`versionCode` 与 DDL 对齐已由 V13 收口） |
| 设备编码 | `?tab=codes` | F3 | `listDeviceCodeBatches/saveDeviceCodeBatch → GET·POST /api/ops/device-code-batches[/{no}]` | ✅ | `DeviceController` | `CodeBatchService` | `CodeBatchMapper` / `DevCodeBatch` | `dev_code_batch` ✅ |

---

## 3. 告警管理（4 行）

**贯通度小结：前端 4/4 F3 · 后端端点 14/14 · 分层 ✅ · 落库 ✅ dev_alarm_\*（https 层「无 resend」标注已过时）**

| 子功能 | 入口 | 前端 | 契约方法 → API 路径 | 后端端点 | Controller | Service | Mapper/Entity | DB 表 · 落库 |
|---|---|:--:|---|:--:|---|---|---|---|
| 告警记录 | `/alarms` | F3 | `listAlarmRecords → GET /api/ops/alarms/records`；`ackAlarm → POST .../records/{no}/ack`；`raiseAlarmWorkOrder → POST .../records/{no}/work-order`；`autoRaiseWorkOrders → POST .../auto-work-orders` | ✅ | `AlarmController` | `AlarmService`(svc-ops) | `DevAlarmMapper` / `DevAlarm` | `dev_alarm` ✅ |
| 告警通知 | `?tab=notices` | F3 | `listAlarmNotices → GET /api/ops/alarms/notices`；`resendAlarmNotice → POST .../notices/{no}/resend` | ✅ | `AlarmController` | `AlarmService` | `DevAlarmNoticeMapper` | `dev_alarm_notice` ✅（重发端点已在，V24；`alarm.ts:20` 的缺口标注过时） |
| 告警代码 | `?tab=codes` | F3 | `listAlarmCodes/saveAlarmCode → GET·POST /api/ops/alarms/codes[/{code}]`；archive/unarchive | ✅ | `AlarmController` | `AlarmCodeService` | `DevAlarmCodeMapper` | `dev_alarm_code` ✅（archived_at V22） |
| 通知规则 | `?tab=rules` | F3 | `listAlarmRules/saveAlarmRule → GET·POST /api/ops/alarms/rules[/{no}]`；archive/unarchive | ✅ | `AlarmController` | `AlarmRuleService` | `DevAlarmRuleMapper` | `dev_alarm_rule` ✅ |

---

## 4. 工单管理（4 行）

**贯通度小结：前端 3/4 F3 + 1 F2（看板不做拖拽，决策终态）· 后端端点 14/14 · 分层 ✅ · 落库 ✅ wo_\*（workorder.ts 的 T1-C/S7 缺口标注全部过时）**

| 子功能 | 入口 | 前端 | 契约方法 → API 路径 | 后端端点 | Controller | Service | Mapper/Entity | DB 表 · 落库 |
|---|---|:--:|---|:--:|---|---|---|---|
| 工单列表 | `/work-orders?view=list` | F3 | `listWorkOrders → GET /api/ops/work-orders`；`createWorkOrder → POST /api/ops/work-orders`；dispatch/accept/handle/complete/close/reject/rework → `POST .../{woNo}/<action>` | ✅ | `OpsController`(list/dispatch) + `WoExtController`(其余) | `WorkOrderService` + `WoOpsService`（复用 `WoStateMachine`） | `WoMapper`,`WoDispatchMapper`,`WoHandleMapper` / `WoOrder` | `wo_order`,`wo_dispatch`,`wo_handle` ✅（assignee_id NOT NULL 500 已修，见 未实现任务清单 三轮记录 #3） |
| 工单看板 | `?view=board` | **F2** | 同上（同一批端点，view 仅前端呈现） | ✅ | 同上 | 同上 | 同上 | 同上 |
| SLA 管理 | `?view=sla` | F3 | `listSlaRules/saveSlaRule → GET·POST /api/ops/sla-rules[/{no}]` | ✅ | `WoExtController` | `SlaRuleService` | `WoSlaRuleMapper`,`WoSlaMapper` | `wo_sla_rule`,`wo_sla` ✅ |
| 巡检计划 | `?view=inspection` | F3 | `listInspectionPlans/saveInspectionPlan → GET·POST /api/ops/inspection-plans[/{no}]`；`runInspectionPlan → POST .../{planNo}/run` | ✅ | `WoExtController` | `InspectionPlanService` | `WoInspectionPlanMapper` | `wo_inspection_plan` ✅（run 端点已在；`workorder.ts:28` 标注过时） |

---

## 5. 站点与点位（8 行）

**贯通度小结：前端 8/8 F3 · 后端端点 22/25（缺 saveVenue / saveContract / saveVenueOnboarding）· 分层 ✅ · 落库 ✅ loc_\*（站点坐标实体缺字段落不了库）**

| 子功能 | 入口 | 前端 | 契约方法 → API 路径 | 后端端点 | Controller | Service | Mapper/Entity | DB 表 · 落库 |
|---|---|:--:|---|:--:|---|---|---|---|
| 站点管理 | `?tab=sites` | F3 | `listSites/saveSite → GET·POST /api/ops/sites[/{no}]`；archive/unarchive | ✅ | `OpsController` | `LocService`(svc-platform) | `LocMappers.SiteMapper` / `LocSite` | `loc_site` ✅。**坐标缺口仍在**：DDL 有 `lng`/`lat`（V1 L51-52），`LocSite` 实体与 `LocDtos.Site` 至今无该字段 → 前端抽屉填的坐标接真后端**静默丢失** |
| 点位管理 | `?tab=points` | F3 | `listLocations/savePoint → GET·POST /api/ops/locations[/{no}]`；archive/unarchive | ✅ | `OpsController` | `LocService` | `LocationMapper` / `LocLocation` | `loc_location` ✅ |
| 站点坪效 | `?tab=analysis` | F3 | `listSiteAnalysis → GET /api/ops/site-analysis` | ✅ | `LocExtController` | `SiteAnalysisService` → 委托 `ReportService`（口径同源） | 报表域事实 Mapper | 读模型（ord_order 等）✅ |
| 场地方 | `?tab=venues` | F3 | `listVenues → GET /api/ops/venues`；`saveVenue → POST /api/ops/venues`；archive/unarchive | ◐ | `OpsController` | `LocService` | `VenueMapper` / `LocVenue` | `loc_venue` ✅。**缺口：saveVenue upsert 无**（archive 有、新增/编辑没有，动词不符） |
| 进场合同 | `?tab=contracts` | F3 | `listContracts → GET /api/ops/contracts`；`saveContract → POST /api/ops/contracts`；`addContractAttachment / removeContractAttachment → POST /api/ops/contracts/{no}/attachments[...]` | ◐ | `OpsController`(list) + `LocExtController`(附件) | `LocService` / ext | `ContractMapper`,`LocContractAttachMapper` | `loc_contract`,`loc_contract_attach`（V28）✅。**缺口：saveContract upsert 无**；附件两个端点已在（`location.ts:22`「四个端点均缺」标注过时一半） |
| 门店 Onboarding | `?tab=onboarding` | F3 | `listVenueOnboardings → GET /api/ops/venue-onboardings`；`saveVenueOnboarding → POST /api/ops/venue-onboardings` | ◐ | `LocExtController` | `VenueOnboardingService` | `LocVenueOnboardingMapper` | `loc_venue_onboarding` ✅。**缺口：前端走 upsert、后端只有 `POST .../{no}/review` 审核端点**，形状不合（动词不符） |
| 门店生命周期 | `?tab=lifecycle` | F3 | `listSiteLifecycles → GET /api/ops/site-lifecycles`；`changeSiteStage → POST .../{siteNo}/stage` | ✅ | `LocExtController` | `SiteLifecycleService` | `LocSiteLifecycleMapper`,`LocSiteLifecycleLogMapper` | `loc_site_lifecycle`,`loc_site_lifecycle_log` ✅ |
| BD 拓展 CRM | `?tab=crm` | F3 | `listLeads/saveLead → GET·POST /api/ops/leads[/{no}]`；`listLeadFollowUps/addLeadFollowUp → GET·POST /api/ops/leads/{no}/follow-ups` | ✅ | `LocExtController` | `LeadService`,`LeadFollowService` | `LocLeadMapper`,`LocLeadFollowMapper` | `loc_lead`,`loc_lead_follow`（V28）✅（`location.ts` 跟进缺口标注过时） |

---

## 6. 代理商管理（6 行）

**贯通度小结：前端 6/6 F3 · 后端端点全在（路径 14/14）但 **2 个读端点是空壳** · 分层 ✅（绩效无 Mapper）· 落库 ✅ agt_\***

| 子功能 | 入口 | 前端 | 契约方法 → API 路径 | 后端端点 | Controller | Service | Mapper/Entity | DB 表 · 落库 |
|---|---|:--:|---|:--:|---|---|---|---|
| 代理商档案 | `/agents` | F3 | `listAgents/saveAgent → GET·POST /api/agent/agents[/{no}]`；archive/unarchive | ✅ | `AgentController` | `AgentService`(svc-platform) | `AgentMapper` / `AgtAgent` | `agt_agent` ✅（+`agt_agent_region`） |
| 代理账号管理 | `?tab=accounts` | F3 | `listAgentAccounts/saveAgentAccount → GET·POST /api/agent/accounts[/{no}]` | ✅ | `AgentExtController` | `AgentAccountService` | `AgtAccountMapper` | `agt_account` ✅ |
| 设备/点位划拨 | `?tab=assign` | F3 | `assignAgentAssets → POST /api/agent/assignments`（逐资产）；`reclaimAgentAssets → POST .../reclaim`；`listAgentAssignments → GET /api/agent/assignments`；流水 `?view=log`；`listAssignableAssets → GET /api/agent/assignable-assets` | ◐ | `AgentExtController` | `AgentAssignmentService`（写 ✅ 含 `AgentOwnershipSync` 级联回写归属）；**默认列表视图路由到 `AgentPerformanceService.assignments()`——空壳恒返回空**（AgentExtController.java:110） | `AgtAssignmentMapper` | `agt_assignment` ✅ append 留痕；`loc_site/loc_location/dev_cabinet.agent_no` 同事务回写。**划拨汇总列表接真后端恒空** |
| 分润配置 | `?tab=commission` | F3 | `listAgentCommissions/saveAgentCommission → GET·POST /api/agent/commissions[/{ruleNo}]` | ✅ | `AgentExtController` | `AgentCommissionService` | `AgtCommissionMapper` | `agt_commission` ✅ |
| 代理收益结算 | 深链 `/finance?tab=settlements` | F3 | 深链，复用财务·结算单行 | — | — | — | — | — |
| 代理绩效 | `?tab=performance` | F3 | `listAgentPerformance → GET /api/agent/performance` | ◐ | `AgentExtController` | `AgentPerformanceService` **空壳**（Impl 两方法均 `List.of()`，聚合 SQL 只在注释里） | ⬜ 无 Mapper | 读模型 ⬜ **待核**：需按注释落自定义 Mapper（agt_agent ⋈ ord_order GROUP BY） |

---

## 7. 订单管理（7 行）

**贯通度小结：前端 7/7 F3 · 后端端点 20/21（缺 listOrderInterventions）· 分层 ✅ · 落库 ✅ ord_\*（免费订单 2 端点空壳）**

| 子功能 | 入口 | 前端 | 契约方法 → API 路径 | 后端端点 | Controller | Service | Mapper/Entity | DB 表 · 落库 |
|---|---|:--:|---|:--:|---|---|---|---|
| 订单列表 | `/orders` | F3 | `listOrders/getOrder → GET /api/trade/orders[/{no}]`；`interveneOrder → POST .../{no}/intervene`；`listOrderInterventions → GET /api/trade/order-interventions` | ◐ | `TradeController` | `RentOrderService`(svc-core, `OrdStateMachine`) | `OrdMapper`,`InterventionMapper` / `OrdOrder` | `ord_order`(+`ord_rent_ext`),`ord_intervention`(V30) ✅。**缺口：干预留痕读端点没有**——表与 Mapper 都在，写入在、查询缺，前端干预时间线接真后端读不到 |
| 预约订单 | `?tab=reservations` | F3 | `listReservations → GET /api/trade/reservations`；`cancelReservation → POST .../{no}/cancel` | ✅ | `OrderOpsController` | `ReservationService` | `OrdReservationMapper` | `ord_reservation` ✅ |
| 异常订单 | `?tab=exceptions` | F3 | `listOrderExceptions → GET /api/trade/order-exceptions`；`handleOrderException → POST .../{no}/handle` | ✅ | `OrderOpsController` | `OrderExceptionService` | `OrdExceptionMapper` | `ord_exception` ✅（时间格式 500 已修） |
| 投诉订单 | `?tab=complaints` | F3 | `listOrderComplaints/createOrderComplaint → GET·POST /api/trade/complaints`；`handleOrderComplaint`；`raiseComplaintWorkOrder → POST .../{no}/work-order` | ✅ | `OrderOpsController` | `ComplaintService` | `OrdComplaintMapper` | `ord_complaint` ✅ |
| 退款记录 | `?tab=refunds` | F3 | `listRefundRecords/createRefund → GET·POST /api/trade/refunds`；`auditRefund → POST .../{refundNo}/audit` | ✅ | `OrderOpsController` | `RefundService`（幂等键 400 校验） | `OrdRefundMapper` | `ord_refund` ✅ |
| 押金与欠费 | `?tab=deposit` | F3（nav `ready`） | `listDepositRecords → GET /api/trade/deposits`；`releaseDeposit → POST .../{no}/release`；`buyoutDeposit → POST .../{no}/buyout`；`dunArrears → POST .../{no}/dun` | ✅ | `OrderOpsController` + `FinanceController`(buyout/dun) | `DepositService` | `OrdDepositMapper` | `ord_deposit` ✅（V25） |
| 免费订单 | `?tab=free` | F3 | `listFreeOrders → GET /api/trade/free-orders`；`getFreeOrderStats → GET .../stats` | ◐ | `OrderOpsController` | `FreeOrderService` **空壳**（page/stats 恒返回空，TODO 注明待 `OrdOrder` 实体补 v2 列） | ⬜ 未接 OrdMapper | 表列在（`ord_order.free_reason` 等，V8）· 实体未补 → **接真后端整 tab 恒空** |

---

## 8. 计费定价（3 行）

**贯通度小结：前端 3/3 F3 · 后端端点 8/8 · 分层 ✅ · 落库 ◐（写落库；计费模板**列表读内存种子**）**

| 子功能 | 入口 | 前端 | 契约方法 → API 路径 | 后端端点 | Controller | Service | Mapper/Entity | DB 表 · 落库 |
|---|---|:--:|---|:--:|---|---|---|---|
| 计费模板 | `/pricing` | F3 | `listPricePlans → GET /api/trade/price-plans`；`savePricePlan → POST /api/trade/price-plans[/{no}]`；archive/unarchive | ◐ | 读:`TradeController` 写:`PricingController` | 写:`PricePlanService` ✅；**读: Controller 直连内存**（TradeController.java:90 `db.pricePlans()`）| `PricePlanMapper`(+Item/Ladder/Scope) | `price_plan`,`price_plan_item`,`price_ladder`,`price_plan_scope`（V17）✅ 写落库；**读写分家：新建模板落库后列表（读种子）不回显** |
| 差异化定价 | `?tab=diff` | F3 | `listPricingDiffs/savePricingDiff → GET·POST /api/trade/pricing-diffs[/{ruleNo}]` | ✅ | `PricingController` | `PricingDiffService` | `PriceRuleMapper` / `PriceRule`（`siteNo` 字段已补，S6 遗留已闭合） | `price_rule` ✅（V19 补 base 列） |
| 活动/时段价 | `?tab=schedule` | F3 | `listPricingSchedules/savePricingSchedule → GET·POST /api/trade/pricing-schedules[/{ruleNo}]` | ✅ | `PricingController` | `PricingScheduleService` | `PriceScheduleMapper` | `price_schedule` ✅（`pricing.ts:21` 缺口标注过时） |

---

## 9. 财务管理（11 行，含 2 深链）

**贯通度小结：前端 10 F3 + 1 F2（账务分录，手工记账刻意未开）· 后端端点 20/22（缺 createVoucher / generateSettlements）· 分层 ✅ svc-finance 六服务 · 落库 ◐（写落库；share-rules/ledger/settlements **三个列表读内存**；share_record/stl_settlement **无生成链路、表恒空**）**

| 子功能 | 入口 | 前端 | 契约方法 → API 路径 | 后端端点 | Controller | Service | Mapper/Entity | DB 表 · 落库 |
|---|---|:--:|---|:--:|---|---|---|---|
| 分润规则 | `?tab=rules` | F3 | `listShareRules → GET /api/trade/share-rules`（带 `dimension` 双向视图参数）；`saveShareRule → POST /api/trade/share-rules[/{ruleNo}]` | ◐ | 读:`TradeController` 写:`FinanceController` | 写:`ShareService`(svc-finance) ✅；**读: 内存**（TradeController.java:59）**且不认 `dimension` 参数**（finance.ts:8 参数级缺口仍成立） | `ShareRuleMapper` | `share_rule` ✅ 写落库；读写分家 |
| 分润明细 | `?tab=records` | F3 | `listShareRecords → GET /api/trade/share-records` | ✅ | `FinanceController` | `ShareService` | `ShareRecordMapper` | `share_record` ✅ 表+链路在；**无逐单分润生成链路，表恒空**（报表域「已结算恒 0」同因） |
| 分润统计 | `?tab=summary` | F3 | `listShareSummaries → GET /api/trade/share-summaries` | ✅ | `FinanceController` | `ShareService` | 同上聚合 | 同上（数据空则统计空） |
| 结算单 | `?tab=settlements` | F3 | `listSettlements → GET /api/trade/settlements`；`generateSettlements → POST /api/trade/settlements/generate`；`confirmSettlement → POST .../{no}/confirm`；`listSettlementRecords → GET .../{no}/records` | ◐ | 读:`TradeController` 其余:`FinanceController` | `SettlementService` ✅；**列表读内存**（TradeController.java:76）；**generate 只有 `/internal/trade/settlements/generate`，`/api` 前缀版没有** → 运营端生成按钮 404 | `StlSettlementMapper`,`StlSettlementDetailMapper` | `stl_settlement`,`stl_settlement_detail` ✅（deleted 列 V20 已补）；读写分家 + 生成入口错位 |
| 账务分录 | `?tab=ledger` | **F2** | `listLedger → GET /api/trade/ledger`；`getVoucher → GET /api/trade/ledger/vouchers/{no}`；`createVoucher → POST /api/trade/ledger/vouchers` | ◐ | 读:`TradeController`(list)+`FinanceController`(voucher) | `LedgerService` ✅（凭证下钻/平衡判定）；**列表读内存**（TradeController.java:67）；**createVoucher 端点无**（前端也刻意未开 UI，同拍板） | `AcctLedgerMapper`,`AcctAccountMapper` | `acct_ledger`,`acct_account` ✅（deleted 列 V20 已补） |
| 对账 | `?tab=reconcile` | F3（nav `ready`） | `listReconciles/getReconStats/listReconDiffs → GET /api/trade/reconciles[...]`；`handleRecon → POST .../{batchNo}/resolve` | ✅ | `FinanceController` | `ReconcileService` | `ReconTaskMapper`,`ReconDiffMapper` | `recon_task`,`recon_diff` ✅ |
| 发票 | `?tab=invoices` | F3（nav `ready`） | `listInvoices/saveInvoice → GET·POST /api/trade/invoices[/{no}]`；`issueInvoice/voidInvoice → POST .../{no}/issue|void` | ✅ | `FinanceController` | `InvoiceService` | `FinInvoiceMapper`,`FinInvoiceItemMapper` | `fin_invoice`,`fin_invoice_item` ✅（V26） |
| 代理分润配置 | 深链 `/agents?tab=commission` | F3 | 深链，复用代理商·分润配置行 | — | — | — | — | — |
| 提现审核 | `?tab=withdrawals` | F3 | `listWithdrawals → GET /api/trade/withdrawals`；`auditWithdrawal → POST .../{withdrawNo}/audit`（另读 `GET /api/platform/biz-rules` 取费率） | ✅ | `FinanceController` | `WithdrawalService` | `StlWithdrawalMapper` | `stl_withdrawal` ✅ |
| 用户钱包 | 深链 `/users?tab=wallets` | F3 | 深链，复用用户管理·钱包行 | — | — | — | — | — |
| 充值订单 | `?tab=recharges` | F3 | `listRechargeOrders → GET /api/user/recharge-orders` | ✅ | `UserOpsController` | `RechargeOrderService` | `UsrRechargeOrderMapper` | `usr_recharge_order` ✅ |

---

## 10. 用户管理（7 行）

**贯通度小结：前端 7/7 F3 · 后端端点 22/24（缺 getUserProfile / saveWallet）· 分层 ◐（用户主表读写仍内存）· 落库 ◐ usr_\*（次卡列表空壳）**

| 子功能 | 入口 | 前端 | 契约方法 → API 路径 | 后端端点 | Controller | Service | Mapper/Entity | DB 表 · 落库 |
|---|---|:--:|---|:--:|---|---|---|---|
| 用户列表 | `/users` | F3 | `listUsers → GET /api/user/users`；`getUserProfile → GET /api/user/users/{no}/profile` | ◐ | `UserController` | ⬜ **Controller 直连内存**（仅注入 SeedData）；**profile 聚合端点无**（user.ts:11 标注仍成立） | ⬜（`usr_user` 表由 C 端链路写） | `usr_user` 表在（C 端注册/借还写它）；**运营端列表读内存种子，与库数据两个世界**；详情抽屉接真后端 404 |
| 风控用户 | `?tab=risk` | F3（nav `ready`） | `listUserRisks → GET /api/user/risk-users`；`adjustCreditScore → POST /api/user/users/{no}/credit-score`；`listCreditScoreChanges → GET /api/user/credit-score-changes` | ✅ | `UserOpsController` | `UserRiskService`,`CreditScoreService` | `UsrCreditMapper`,`UsrCreditChangeMapper` | `usr_credit`,`usr_credit_change` ✅（V27） |
| 黑名单 | `?tab=blacklist` | F3 | `listUserBlacklist → GET /api/user/blacklist`；`setBlacklist → POST /internal/user/credit/blacklist` | ◐ | 读:`UserOpsController` 写:`UserController` | 读:`UserBlacklistService`→`usr_blacklist` ✅；**写: 内存**（UserController.java:45-51 改 SeedData） | `UsrBlacklistMapper` | `usr_blacklist` ✅ 读落库；**写不落库（读写分家，方向与常见相反）** |
| 免费用户白名单 | `?tab=whitelist` | F3 | `listFreeWhitelist/saveFreeWhitelist → GET·POST /api/user/free-whitelist[/{userNo}]`；`revokeFreeWhitelist → POST .../{userNo}/revoke` | ✅ | `UserOpsController` | `FreeWhitelistService` | `UsrFreeWhitelistMapper`（UserCoreMappers 内） | `usr_free_whitelist` ✅ |
| 会员/次卡 | `?tab=members` | F3 | `listMembers/saveMember → GET·POST /api/user/members[/{userNo}]`；`listMemberBenefits/saveMemberBenefit → GET·POST /api/user/member-benefits[/{level}]`；`listMemberCards/grantMemberCard → GET·POST /api/user/member-cards` | ◐ | `UserOpsController` | `MembershipService`,`MemberService`；**`MemberService.pageCards` 空壳**（Impl L124-126 恒空） | `MbrBenefitMapper` 等 | `usr_membership`,`mbr_benefit`（V27）✅；**次卡列表接真后端恒空**（user.ts:16「四端点均缺」标注过时——端点在，但一个是空壳） |
| 钱包 | `?tab=wallets` | F3 | `listWallets → GET /api/user/wallets`；`saveWallet → POST /api/user/wallets`；`listWalletTxns → GET .../{userNo}/txns` | ◐ | `UserOpsController` | `WalletService`（读 ✅）；**saveWallet 写端点无**（动词不符） | `UsrWalletMapper`,`UsrWalletTxnMapper` | `usr_wallet`,`usr_wallet_txn` ✅ 读落库；手工调余额接真后端 404 |
| 充值套餐 | `?tab=recharge` | F3 | `listRechargePackages/saveRechargePackage → GET·POST /api/user/recharge-packages[/{no}]`；archive/unarchive | ✅ | `UserOpsController` | `RechargePackageService` | `UsrRechargePkgMapper` 等 | `usr_recharge_pkg`(+`_market`) ✅（archived_at V22） |

---

## 11. 营销管理（8 行）

**贯通度小结：前端 8/8 F3 · 后端端点 24/25（缺 saveReferralRule）· 分层 ✅ · 落库 ◐（**优惠券列表读内存**，其余落库；marketing.ts 三条缺口标注中两条过时）**

| 子功能 | 入口 | 前端 | 契约方法 → API 路径 | 后端端点 | Controller | Service | Mapper/Entity | DB 表 · 落库 |
|---|---|:--:|---|:--:|---|---|---|---|
| 公告管理 | `?tab=notices` | F3 | `listNotices/saveNotice → GET·POST /api/ops/marketing/notices[/{no}]`；archive/unarchive | ✅ | `MarketingController` | `NoticeService` | `MktNoticeMapper` | `mkt_notice` ✅ |
| 优惠券 | `/marketing` | F3（nav `ready`） | `listCoupons → GET /api/user/coupons`；`saveCoupon → POST /api/user/coupons[/{no}]`；`issueCoupon → POST .../{tplNo}/issue`；`listCouponIssueRecords → GET /api/user/coupon-issue-records`；archive/unarchive | ◐ | 读:`UserController` 写:`MarketingController` | 写:`CouponTplService`,`UserCouponService` ✅；**列表读内存**（UserController 仅注入 SeedData） | `CouponTplMapper`,`UsrCouponMapper`,`UsrCouponIssueMapper` | `coupon_tpl`,`usr_coupon`,`usr_coupon_issue`（V27）✅ 写落库；**读写分家：新建券模板列表不回显** |
| 活动 | `?tab=campaigns` | F3 | `listCampaigns/saveCampaign → GET·POST /api/user/campaigns[/{no}]`；`transitionCampaign → POST .../{no}/{start\|pause\|end}` | ✅ | `MarketingController` | `CampaignService` | `MktCampaignMapper` | `mkt_campaign` ✅（启停端点已在，`marketing.ts:30` 标注过时） |
| 推送触达 | `?tab=push` | F3（nav `ready`） | `listPushMessages/savePushMessage → GET·POST /api/user/push-messages[/{no}]`；`sendPushMessage → POST .../{pushNo}/send` | ✅ | `MarketingController` | `PushService` | `MktPushMapper` | `mkt_push` ✅ |
| 邀请裂变 | `?tab=referral` | F3 | `listReferrals → GET /api/user/referrals`；`listReferralRules → GET /api/user/referral-rules`；`saveReferralRule → POST /api/user/referral-rules` | ◐ | `MarketingController` | `ReferralService` | `MktReferralMapper` | `mkt_referral` ✅ 读落库；**缺口：奖励规则写端点无**（读有写无，动词不符）——规则存哪张表**待核**（迁移清单未见 mkt_referral_rule 表） |
| 广告位管理 | `?tab=ad-slots` | F3 | `listAdSlots/saveAdSlot → GET·POST /api/ops/ad-slots[/{no}]` | ✅ | `MarketingController` | `AdSlotService` | `AdSlotMapper` | `ad_slot` ✅ |
| 广告活动 | `?tab=ad-campaigns` | F3 | `listAdCampaigns/saveAdCampaign → GET·POST /api/user/ad-campaigns[/{no}]`；`transitionAdCampaign → POST .../{adNo}/{action}` | ✅ | `MarketingController` | `AdCampaignService` | `AdCampaignMapper`(+Creative/Advertiser) | `ad_campaign`,`ad_creative`,`ad_advertiser` ✅（`marketing.ts:23` 标注过时） |
| 投放与曝光 | `?tab=ad-delivery` | F3 | `listAdDeliveries → GET /api/user/ad-deliveries` | ✅ | `MarketingController` | `AdDeliveryService` | `AdImpressionMapper` | `ad_impression`(+`ad_placement`) ✅ |

---

## 12. 客服管理（4 行，含 2 深链）

**贯通度小结：前端 4/4 F3 · 后端端点 7/7 · 分层 ✅ · 落库 ✅ cs_\***

| 子功能 | 入口 | 前端 | 契约方法 → API 路径 | 后端端点 | Controller | Service | Mapper/Entity | DB 表 · 落库 |
|---|---|:--:|---|:--:|---|---|---|---|
| 报障受理 | `/cs` | F3 | `listCsTickets/saveCsTicket → GET·POST /api/ops/cs/tickets[/{no}]`；`woCsTicket → POST .../{no}/work-order`；`refundCsTicket → POST .../{no}/refund`（均以 ticketNo 幂等） | ✅ | `CsController` | `CsTicketService`(svc-ops) | `CsTicketMapper` | `cs_ticket` ✅ |
| 客服会话 | `?tab=sessions` | F3 | `listCsSessions → GET /api/ops/cs/sessions`；`listCsMessages/replyCsSession → GET·POST .../{sessionNo}/messages` | ✅ | `CsController` | `CsSessionService` | `CsSessionMapper`,`CsMessageMapper` | `cs_session`,`cs_message` ✅ |
| 退款/补偿 | 深链 `/orders` | F3 | 深链，复用订单管理·退款记录行 | — | — | — | — | — |
| 黑名单处理 | 深链 `/users` | F3 | 深链，复用用户管理·黑名单行 | — | — | — | — | — |

---

## 13. 数据报表（6 行）

**贯通度小结：前端 6/6 F3 · 后端端点 9/9（`report.ts:4`「本域全部端点后端都不存在」的标注**整域过时**——10 端点已于三轮落地）· 分层 ✅ 读模型 · 落库 ✅ 事实源为真表（部分指标有诚实的口径缺口）**

| 子功能 | 入口 | 前端 | 契约方法 → API 路径 | 后端端点 | Controller | Service | Mapper/Entity | DB 表 · 落库 |
|---|---|:--:|---|:--:|---|---|---|---|
| 设备运营分析 | `?tab=device` | F3 | `listReportDevice → GET /api/ops/reports/device`；`getReportTrend → GET .../trend` | ✅ | `ReportController` | `ReportService`（单一事实源 `(bizDate,bizHour,siteName)` 四向折叠） | `ReportMappers.{OrderFact,CabinetFact,WoFact}Mapper` | 读 `ord_order`,`dev_cabinet`,`wo_order` ✅。**onlineRate/faultRate 是当前快照非周期均值**（dev_heartbeat 空、无每日快照表），切周期不变 |
| 点位坪效 | `?tab=location` | F3 | `listReportLocation → GET /api/ops/reports/location` | ✅ | `ReportController` | `ReportService` | `OrderFactMapper`,`ContractRateMapper`,`ShareFactMapper` | 读 `ord_order`,`loc_contract`,`share_record` ✅。**payback 恒 0**（无资产成本表）、opex 未计入 |
| 财务报表 | `?tab=finance` | F3 | `listReportFinance → GET /api/ops/reports/finance` | ✅ | `ReportController` | `ReportService` | 同上 | 同上。**settle 恒 0**（share_record/stl_settlement 无生成链路） |
| 实时大屏 | `?tab=screen` | F3 | `listReportScreen → GET .../screen`；`getScreenBoard → GET .../screen-board` | ✅ | `ReportController` | `ReportService` | 同上 | ✅ |
| 自定义报表 | `?tab=custom` | F3 | `listReportCustom → GET .../custom`；`listReportMetrics → GET .../metrics` | ✅ | `ReportController` | `ReportService` | 同上 | ✅ |
| 消费者分析 | `?tab=consumer` | F3 | `getConsumerInsight → GET .../consumer-insight`；`listConsumerSegments → GET .../consumer-segments` | ✅ | `ReportController` | `ReportService` | `OrderFactMapper` | ✅。画像仅 PERIOD 一维、漏斗 3 层非 5 层（无事件表，不倒推假转化率） |

---

## 14. 员工与权限（5 行）

**贯通度小结：前端 4 F3 + 1 F2（绩效数据非工单派生）· 后端端点 12/15（缺 saveRoleRow / archiveRole / unarchiveRole）· 分层 ◐（**员工/角色/审计三个列表仍读内存**）· 落库 ◐ iam_\*（**iam_audit_log 恒空——审计是空壳**）**

| 子功能 | 入口 | 前端 | 契约方法 → API 路径 | 后端端点 | Controller | Service | Mapper/Entity | DB 表 · 落库 |
|---|---|:--:|---|:--:|---|---|---|---|
| 员工 | `?tab=employees` | F3 | `listEmployees → GET /api/platform/employees`；`saveEmployee → POST /api/platform/employees[/{no}]` | ◐ | 读:`PlatformController` 写:`OrgController` | 写:`EmployeeService` ✅（IamEmployee/EmployeeRole/Dept/Role 四 Mapper）；**读: 内存**（PlatformController.java:35 `db.employees()`） | `IamEmployeeMapper`,`IamEmployeeRoleMapper` | `iam_employee`,`iam_employee_role` ✅ 写落库；**读写分家：新建员工列表不回显** |
| 组织架构 | `?tab=org` | F3 | `listDepartments/saveDepartment → GET·POST /api/platform/departments[/{deptNo}]` | ✅ | `OrgController` | `DepartmentService` | `IamDeptMapper` | `iam_dept` ✅ |
| 角色权限 | `?tab=roles` | F3 | `listRoles → GET /api/platform/roles`；`saveRoleRow → POST /api/platform/roles`；archive/unarchive；`listPermissions → GET /api/platform/iam/permissions`；`listRolePermissions/saveRolePermissions → GET·PUT /api/platform/iam/roles/{roleNo}/permissions`；`saveRoleDataScope → PUT /api/platform/data-scopes/{type}/{no}` | ◐ | `PlatformController`(角色列表) + `IamAdminController`(权限树，svc-platform) + `OrgController`(数据范围) | `PermissionService`,`MenuService`,`DataScopeService` ✅；**角色行 upsert/archive/unarchive 三端点无**；角色列表读内存 | `IamMappers.RoleMapper`,`iam_role_perm` 相关 | `iam_role`,`iam_role_perm`,`iam_data_scope` ✅ 权限覆盖写+读已配对；**角色档案本身写不进库** |
| 操作审计 | `?tab=audit` | F3 | `listAudits → GET /api/platform/audit-logs`；`getAuditDetail → GET .../{id}` | ◐ | `PlatformController` | `AuditLogService`（详情先查库、查不到回退种子，PlatformController.java:78-82）；**全后端 0 处调用 `AuditLogService.append`** | `IamAuditLogMapper` | `iam_audit_log` ✅ 表在；**恒空——审计留痕是空壳**，列表数据全部来自内存种子（无审计切面，实现状态总表 T0.8 未做） |
| 绩效报表 | `?tab=performance` | **F2** | `listStaffPerformance → GET /api/platform/staff-performance` | ✅ | `OrgController` | `StaffPerformanceService` | `IamStaffPerfMapper` | `iam_staff_perf` ✅（静态表；未从 wo_handle 聚合，与前端 F2 缺口同因） |

---

## 15. 系统设置（16 行）

**贯通度小结：前端 16/16 F3 · 后端端点 43/44（缺 testVendorConnectivity）· 分层 ✅（唯**供应商接入读写走内存**）· 落库 ◐（15/16 落库；gw_vendor 表在但 portal 未接）**

| 子功能 | 入口 | 前端 | 契约方法 → API 路径 | 后端端点 | Controller | Service | Mapper/Entity | DB 表 · 落库 |
|---|---|:--:|---|:--:|---|---|---|---|
| 供应商接入 | `?tab=vendors` | F3 | `listVendors → GET /internal/gw/vendors`；`saveVendor → POST .../{code}/config`；`testVendorConnectivity → POST .../{code}/test` | ◐ | `VendorController` | ⬜ **Controller 直连内存**（仅注入 SeedData）；**test 端点无**（system.ts:13 标注仍成立） | ⬜ portal 未接（svc-gateway 另有 `GwVendorMapper`,`GwVendorConfigMapper` 未被 portal 使用） | `gw_vendor`,`gw_vendor_config` 表在 ✅；**读写均不落库** |
| 支付渠道 | `?tab=payment` | F3 | `listPaymentChannels/savePaymentChannel → GET·POST /api/platform/payment-channels[/{code}]` | ✅ | `PaymentChannelController` | `PaymentChannelService`(svc-core) | `PayChannelMapper`,`PayChannelScopeMapper` | `pay_channel`,`pay_channel_scope` ✅ |
| 通知模板 | `?tab=notify` | F3 | `listNotifyTemplates/saveNotifyTemplate → GET·POST /api/platform/notify-templates[/{no}]`；`previewNotifyTemplate/testSendNotifyTemplate → POST .../{no}/preview|test-send` | ✅ | `NotifyController` | `NotifyTemplateService`,`NotifySendService` | `NotifyTemplateMapper` | `notify_template` ✅（preview/test-send 已在，`system.ts:33` 标注过时） |
| 发送记录 | `?tab=notify-log` | F3 | `listNotifyLogs/getNotifyLogStats → GET /api/platform/notify-logs[...]`；`resendNotifyLog → POST .../{no}/resend` | ✅ | `NotifyController` | `NotifyLogService` | `NotifyLogMapper` | `notify_log` ✅（resend 已在，V24；`system.ts:47` 标注过时） |
| 触达拉黑 | `?tab=notify-blacklist` | F3 | `listNotifyBlacklist/saveNotifyBlacklist → GET·POST /api/platform/notify-blacklist`；`releaseNotifyBlacklist → POST .../{blockNo}/release` | ✅ | `NotifyController` | `NotifyBlacklistService` | `NotifyBlacklistMapper` | `notify_blacklist` ✅ |
| 业务规则 | `?tab=rules` | F3 | `getBizRules/saveBizRules → GET·POST /api/platform/biz-rules` | ✅ | `SysSettingController` | `BizRuleService` | `SysBizRuleMapper` | `sys_biz_rule` ✅（提现手续费口径的 SSOT） |
| 登录设置 | `?tab=login` | F3 | `listLoginSettings/saveLoginSetting → GET·POST /api/platform/login-settings[/{country}]` | ✅ | `SysSettingController` | `LoginSettingService` | `SysLoginSettingMapper` | `sys_login_setting` ✅ |
| 应用版本 | `?tab=app-version` | F3 | `listAppVersions/saveAppVersion → GET·POST /api/platform/app-versions[/{id}]`；`rollbackAppVersion → POST .../{id}/rollback` | ✅ | `SysSettingController` | `AppVersionService` | `SysAppVersionMapper` | `sys_app_version` ✅ |
| 参数字典 | `?tab=dict` | F3 | `listDictEntries/saveDictEntry → GET·POST /api/platform/dict-entries[/{no}]` | ✅ | `SysConfigController` | `DictEntryService` | `DictItemMapper` | `dict_item` ✅ |
| 地区库 | `?tab=region` | F3 | `listRegions/saveRegion → GET·POST /api/platform/regions[/{id}]`；`listRegionTree → GET .../tree` | ✅ | `SysConfigController` | `RegionService` | `MdRegionMapper` | `md_region` ✅（tree 已在，`system.ts:31` 标注过时） |
| 银行管理 | `?tab=banks` | F3 | `listBanks/saveBank → GET·POST /api/platform/banks[/{code}]`；archive/unarchive | ✅ | `SysDictController` | `BankService` | `MdBankMapper` | `md_bank` ✅ |
| 问题管理 | `?tab=problems` | F3 | `listProblems/saveProblem → GET·POST /api/platform/problems[/{no}]`；archive/unarchive | ✅ | `SysConfigController` | `ProblemService` | `MdProblemMapper` | `md_problem` ✅ |
| 系统参数 | `?tab=params` | F3 | `listSysParams/saveSysParam → GET·POST /api/platform/sys-params[/{key}]` | ✅ | `SysConfigController` | `SysParamService` | `SysParamMapper` | `sys_param` ✅ |
| 税率与发票 | `?tab=tax` | F3 | `listTaxSettings/saveTaxSetting → GET·POST /api/platform/tax-settings[/{country}]` | ✅ | `SysSettingController` | `TaxSettingService` | `SysTaxSettingMapper` | `sys_tax_setting` ✅ |
| 多国家市场 | `?tab=markets` | F3 | `listMarketCountries/saveMarketCountry → GET·POST /api/platform/markets[/{code}]` | ✅ | `SysSettingController` | `MarketService` | `MdMarketCountryMapper` | `md_market_country` ✅ |
| OpenAPI 应用 | `?tab=openapi` | F3 | `listOpenApiApps/saveOpenApiApp → GET·POST /api/platform/openapi-apps[/{no}]`；`resetOpenApiAppSecret → POST .../{appNo}/reset-secret` | ✅ | `SysSettingController` | `OpenApiAppService` | `OpenapiAppMapper` | `openapi_app` ✅（reset-secret + `app_secret` 列已在，V23；`system.ts:37` 标注过时） |

---

## 缺口台账（工作树实测，2026-08-04）

### A. 后端连路径都没有（7，与 parity 脚本输出一致）

| 域 | 契约方法 | 路径 | 影响 |
|---|---|---|---|
| order | `listOrderInterventions` | `GET /api/trade/order-interventions` | 干预留痕时间线读不到（表 `ord_intervention` V30 + `InterventionMapper` 都在，唯独查询端点没写） |
| finance | `createVoucher` | `POST /api/trade/ledger/vouchers` | 手工记账（前端也刻意未开 UI，双端一致留白） |
| finance | `generateSettlements` | `POST /api/trade/settlements/generate` | 生成结算单 404 —— 后端只有 `/internal/` 前缀版，运营端调不到 |
| org | `archiveRole` / `unarchiveRole` | `POST /api/platform/roles/{no}/(un)archive` | 角色归档不可用 |
| system | `testVendorConnectivity` | `POST /internal/gw/vendors/{code}/test` | 供应商连通性测试 404 |
| user | `getUserProfile` | `GET /api/user/users/{no}/profile` | 用户详情抽屉整屏 404 |

### B. 路径在、动词不符 = 写端点缺（7）

`saveCabinet`（机柜建档/编辑）· `saveVenue`（场地方）· `saveContract`（合同）· `saveVenueOnboarding`（进件，后端只有 review）· `saveReferralRule`（邀请奖励规则）· `saveRoleRow`（角色行）· `saveWallet`（钱包手工调整）—— 前端「新增/编辑」按钮在 `USE_MOCK=0` 下必 404。

### C. 端点在但恒返回空的空壳服务（4，parity 覆盖率看不见这一层）

| 端点 | 空壳位置 |
|---|---|
| `GET /api/ops/device-logs` | `DeviceLogServiceImpl.page` 恒空（UNION 读模型只在注释里） |
| `GET /api/trade/free-orders` + `/stats` | `FreeOrderServiceImpl` 两方法恒空（待 OrdOrder 实体补 v2 列） |
| `GET /api/agent/performance`；`GET /api/agent/assignments`（默认视图） | `AgentPerformanceServiceImpl` 两方法恒空；划拨列表默认视图路由到它（`AgentExtController.java:110`） |
| `GET /api/user/member-cards` | `MemberServiceImpl.pageCards` 恒空 |

### D. 读写分家（写走库、列表读内存种子 SeedData，重启即回种子）

| 资源 | 读（内存） | 写（落库） |
|---|---|---|
| 计费模板 | `TradeController:90` | `PricingController → PricePlanService` |
| 分润规则 | `TradeController:59`（且不认 `dimension`） | `FinanceController → ShareService` |
| 账务分录列表 | `TradeController:67` | —（voucher 读侧已落库） |
| 结算单列表 | `TradeController:76` | `FinanceController → SettlementService` |
| 优惠券列表 | `UserController`（GET /api/user/coupons） | `MarketingController → CouponTplService` |
| 员工列表 / 角色列表 / 审计列表 | `PlatformController:35/41/58` | `OrgController → EmployeeService`（员工）；角色行写端点缺 |
| 用户列表 | `UserController`（GET /api/user/users） | —（usr_user 由 C 端链路写） |
| 用户拉黑（方向相反：**读落库、写内存**） | 写 `UserController:45-51` | 读 `UserOpsController → UserBlacklistService` |
| 供应商接入 | `VendorController`（读写全内存） | —（gw_vendor Mapper 在 svc-gateway，portal 未接） |
| 经营看板 | `OpsController:51` | — |

### E. 其它已确认的贯通断点

- **审计空壳**：全后端 0 处调用 `AuditLogService.append`，`iam_audit_log` 恒空（审计切面 T0.8 未做）。
- **指令下发占位**：`CabinetServiceImpl.sendCommand` 返回 `"CMD"+nanoTime`（L54-57），不经网关、不写 `gw_command_log` → 「下发成功但指令记录查不到」。
- **站点坐标**：`loc_site` DDL 有 `lng/lat`（V1），`LocSite` 实体/DTO 无字段 → 坐标接真后端静默丢失。
- **分润数据链**：`share_record` / `stl_settlement` 无逐单生成链路，表恒空 → 分润明细/统计、报表「已结算」均为 0。
- **心跳**：`dev_heartbeat` 无写入方（南向 access-gateway 进程未建）→ 监控在线率为快照。

---

## 全局汇总（每模块一行）

> 前端完成度 = F3 数/行数（深链行随被复用行计）。后端端点覆盖率 = 该模块前端契约方法中「动词+路径都在」的比例（parity 严格口径）。分层完整度：✅ 全链 Controller→Service→Mapper→Entity；◐ 部分行直连内存或空壳；⬜ 无分层。落库状态：✅ 读写皆落库；◐ 部分（读写分家/空壳/表空）；⬜ 不落库。

| # | 模块 | 前端完成度 | 后端端点覆盖率 | 分层完整度 | 落库状态 |
|---|---|:--:|:--:|:--:|---|
| 1 | 经营看板 | 1/1 F3 | 1/1 (100%) | ⬜ 直连内存 | ⬜ 内存聚合 |
| 2 | 设备管理 | 8/8 F3 | 22/23 (96%) | ✅（日志服务空壳） | ✅ dev_\*（指令下发不落库、日志读模型未接） |
| 3 | 告警管理 | 4/4 F3 | 14/14 (100%) | ✅ | ✅ dev_alarm_\* |
| 4 | 工单管理 | 3 F3 + 1 F2 | 14/14 (100%) | ✅ | ✅ wo_\* |
| 5 | 站点与点位 | 8/8 F3 | 22/25 (88%) | ✅ | ✅ loc_\*（坐标字段实体缺） |
| 6 | 代理商管理 | 6/6 F3 | 14/14 (100%)* | ✅（绩效空壳） | ✅ agt_\*（*划拨列表/绩效两读端点恒空） |
| 7 | 订单管理 | 7/7 F3 | 20/21 (95%) | ✅（免费单空壳） | ✅ ord_\*（干预表在无读端点） |
| 8 | 计费定价 | 3/3 F3 | 8/8 (100%) | ✅ | ◐ 写落库、模板列表读内存 |
| 9 | 财务管理 | 10 F3 + 1 F2 | 20/22 (91%) | ✅ svc-finance | ◐ 写落库；3 列表读内存；share_record/stl_settlement 恒空 |
| 10 | 用户管理 | 7/7 F3 | 22/24 (92%) | ◐ 用户主表直连内存 | ◐ usr_\*（用户列表读内存、拉黑写内存、次卡空壳） |
| 11 | 营销管理 | 8/8 F3 | 24/25 (96%) | ✅ | ◐ 落库、券列表读内存 |
| 12 | 客服管理 | 4/4 F3 | 7/7 (100%) | ✅ | ✅ cs_\* |
| 13 | 数据报表 | 6/6 F3 | 9/9 (100%) | ✅ 读模型 | ✅ 真表事实源（快照/恒 0 指标已注明） |
| 14 | 员工与权限 | 4 F3 + 1 F2 | 12/15 (80%) | ◐ 三列表直连内存 | ◐ iam_\*（审计表恒空、角色写不进库） |
| 15 | 系统设置 | 16/16 F3 | 43/44 (98%) | ✅（供应商直连内存） | ◐ 15/16 落库；gw_vendor 未接 |
| — | **合计** | **95 F3 + 3 F2**（97%） | **251/265（94%）** | 4 模块 ◐/⬜ | 全量表已建（Flyway V1–V30）；真贯通断点见缺口台账 A–E |
