# ShareHub 系统架构图（architecture-diagram.md）

> 创建：2026-07-29 · 定位：**架构的可视化视图**，与 [architecture.md](./architecture.md)（架构决策与选型）互补
> 口径：**图 = 代码实际结构 + v2 定稿设计**，不画未决方案。凡设计已定但未实现的，用 `⬜` 标注，不混淆现状与蓝图。
> 上游锚点：[api/README.md](../api/README.md)（248 端点）· [db-design.md](./db-design.md)（131 表）· [运营端功能清单](../requirements/运营端功能清单.md)（98 菜单叶）· [实现状态总表](./实现状态总表.md)（实现度权威）

---

## 〇、先说清楚一件事：**当前不是微服务**

这点必须在图之前讲明，否则整张图会被误读：

| | 现状（2026-07-29） | 设计目标 |
|---|---|---|
| 部署形态 | **模块化单体 1 个进程**（`powerbank-app`）| 模块化单体 + `access-gateway` **2 个进程** |
| 域边界 | 靠 **Java 包隔离**，域间直接方法调用 | 域间经 `DomainEventPublisher` + `RestClient` |
| 拆分单元 | — | **子域前缀 = 未来拆库/拆服务的单元**（`dev_`/`ord_`/`usr_`…）|
| 何时拆 | **不预先拆**（ADR-001 modulith 起步） | 按信号裂解：先拆支付账务，再拆工单 |

> **「域 = 逻辑边界，部署 = 物理进程」** —— 域分而不裂。代码按域分模块，将来拆分是**搬迁不重写**：抽出独立启动类 + 独立 pom 即成服务，业务代码不动（域内已隔离、事件已走抽象）。
> 所以下文的「业务模块图」画的是**逻辑域**，不是当前的进程边界；进程边界见 §六。

---

## 一、全景图（系统上下文）

```mermaid
flowchart TB
  subgraph CLIENT["① 前端 / 端"]
    direction LR
    OPSWEB["运营端 ops-web<br/>Next.js · 98 菜单叶<br/>ADMIN/OPS/CS/FINANCE/BD/VIEWER"]
    AGENTV["代理端 AGENT 自助 ⬜<br/>复用运营端页面 + 数据范围裁剪"]
    CAPP["C端 c-app<br/>uni-app Vue3 · App优先/小程序次要<br/>zh/en/ar + RTL"]
  end

  subgraph EDGE["② 接入层（受信头注入 · 客户端同名头一律剥离）"]
    direction LR
    PORTALOPS["portal/ops<br/>/api/auth · /api/ops · /api/trade<br/>/api/user · /api/agent · /api/platform"]
    PORTALMP["portal/mp<br/>/mp/**（C池会话）"]
    INTERNAL["/internal/**<br/>域间编排"]
    NOTIFY["/notify/**<br/>nearpay 回调"]
    OPENAPI["/openapi/** ⬜<br/>AppKey+限流"]
  end

  subgraph CORE["③ 服务端业务域（模块化单体 powerbank-app）"]
    direction LR
    TRADE["trade 交易"]
    OPS["ops 设备运维"]
    USER["user 用户营销"]
    PLATFORM["platform 平台支撑"]
  end

  subgraph BASE["④ 基础支撑（横切，见 §五）"]
    direction LR
    AUTHZ["认证 · RBAC · 数据范围"]
    OBS["日志 · 审计 · 可观测"]
    COMMON["契约 · 异常 · i18n · 幂等 · 状态机"]
  end

  subgraph GW["⑤ access-gateway（独立进程 ⬜）"]
    NETTY["Netty TCP 私有协议"]
    MQTT["EMQX5 / MQTT5"]
    HTTPCB["HTTP 供应商回调 /gw/**"]
    DRIVER["DeviceDriver SPI<br/>一供应商一 driver"]
  end

  subgraph DATA["⑥ 数据与中间件"]
    direction LR
    PBCORE[("pb_core<br/>131 表")]
    PBPII[("pb_pii<br/>独立KMS·PDPL")]
    PBAUTH[("pb_auth<br/>独立KMS")]
    REDIS[("Redis<br/>会话·影子·幂等·锁")]
    MQ["Kafka / DomainEventPublisher"]
    JOB["XXL-Job 调度"]
    OSS["OSS/MinIO 附件"]
  end

  subgraph EXT["⑦ 外部系统"]
    NEARPAY["neargo nearpay ⬜<br/>收单·预授权·退款·对账"]
    VENDOR["硬件供应商柜机 A/B/C"]
    PUSH["APNs / FCM / SMS / WhatsApp"]
  end

  OPSWEB --> PORTALOPS
  AGENTV --> PORTALOPS
  CAPP --> PORTALMP
  PORTALOPS --> CORE
  PORTALMP --> CORE
  INTERNAL --> CORE
  NOTIFY --> TRADE
  OPENAPI --> CORE
  CORE -.依赖.-> BASE
  CORE --> DATA
  TRADE -. PaymentPort .-> NEARPAY
  NEARPAY -. 回调 .-> NOTIFY
  CORE <-. DeviceEvent / Command .-> GW
  GW <--> VENDOR
  GW --> REDIS
  PLATFORM --> PUSH
```

---

## 二、① 前端层

```mermaid
flowchart LR
  subgraph OW["运营端 ops-web（Next.js + TS + Tailwind）"]
    direction TB
    OWNAV["lib/nav.ts —— 菜单 SSOT<br/>7 域 › 15 模块 › 98 叶<br/>phase 1/2/3 分期屏蔽"]
    OWPAGE["app/** 页面<br/>一模块一页 + tab/view 深链"]
    OWUI["components/ui/**<br/>DataTable · FormDrawer · Toolbar<br/>useConfirm · export-csv · Progress"]
    OWAPI["lib/api/**<br/>contracts(契约) / mocks / https<br/>161 方法 · 契约一致性单测锚死"]
    OWSTATE["lib/stores + lib/i18n<br/>theme · locale(zh/en/ar + RTL)"]
    OWPERM["lib/permissions.ts<br/>can() 前端权限裁剪(与后端同码)"]
  end

  subgraph CA["C端 c-app（uni-app + Vue3 + Pinia + UnoCSS）"]
    direction TB
    CAPAGE["pages/** 21 路由<br/>首页找柜 · 借还 · 订单 · 钱包 · 我的"]
    CAUI["components/ui/pb-*.vue<br/>28 组件 · easycom 自动注册 · 可换肤"]
    CAPORT["ports/**<br/>auth · pay · map · scan · push<br/>条件编译隔离端差异"]
    CAAPI["api/{contract,mock,http}.ts<br/>唯一切换点 VITE_USE_MOCK"]
    CAI18N["i18n/{zh,en,ar}<br/>三语 + RTL"]
  end

  OWAPI -->|"USE_MOCK=0"| BE["后端 /api/**"]
  CAAPI -->|"VITE_USE_MOCK=0"| BEMP["后端 /mp/**"]
```

**两端的共同设计**：`contract` 接口 + `mock` / `http` 双实现，**切换点唯一**（一个环境变量）。前端可在后端未就绪时全量自洽开发 —— 这也是当前 ops-web 98 叶全部可点、但数据仍是 mock 的原因。

| 端 | 栈 | 角色/身份 | 认证池 | 状态 |
|---|---|---|---|---|
| ops-web | Next.js 静态导出 | ADMIN/OPS/CS/FINANCE/BD/VIEWER | STAFF | ✅ 98 叶全有页面（数据 mock）|
| 代理端 | 复用 ops-web | AGENT | AGENT | ⬜ **整体未建**（[G5](../requirements/运营端功能清单.md)）|
| c-app | uni-app（App 优先） | 消费者 | CONSUMER | 🟡 借还闭环通，多数页 mock |

---

## 三、② 接入层（端点前缀 = 接入面，不是领域）

```mermaid
flowchart TB
  subgraph OPSFACE["运营端 6 前缀（2026-07-29 治理定案，不再新增）"]
    A1["/api/auth —— 登录/登出/我的权限/动态菜单"]
    A2["/api/ops —— 看板·设备·告警·工单·场地·客服·报表"]
    A3["/api/trade —— 订单·计费·支付·财务·分润·结算"]
    A4["/api/user —— C端用户·风控·会员钱包·营销·广告"]
    A5["/api/agent —— 代理档案·账号·划拨·分润·绩效"]
    A6["/api/platform —— 组织权限·审计·系统设置·字典·主数据"]
  end
  subgraph OTHER["非运营前缀"]
    B1["/mp —— C端 BFF（形状为端上直接可渲染）"]
    B2["/internal —— 域间编排（内网+受信头透传）"]
    B3["/gw —— 供应商南向回调（driver 验签）"]
    B4["/notify —— nearpay 异步回调（渠道验签+幂等）"]
    B5["/openapi ⬜ —— 第三方（AppKey 签名+限流）"]
  end
```

**接入层的三条硬规则**：

1. **受信头边缘注入、内部只读** —— `X-Region-Id` / `X-User-Id` / `X-Merchant-Id`(承载 `tenant_id`) / `X-Roles` / `X-Store-Scope`；**客户端伪造的同名头在入口一律剥离**。
2. **`/mp` 与 `/api` 返回形状可以不同** —— `/mp` 是 BFF，必须返回端上直接可渲染的结构（订单详情带 `fees[]` + `timeline[]`）；`/api` 返回扁平结构给运营端表格。同一份数据两种投影，不强求统一。
3. **写操作统一形态** —— `POST /{collection}` 建、`POST /{collection}/{no}` 改、动作用动词后缀（`/dispatch` `/audit` `/handle` `/cancel` `/revoke` `/release` `/rollback`）。**全站零 DELETE**（软删除，见 [api/README §6A.3](../api/README.md)）。

---

## 四、③ 服务端业务域与模块

### 4.1 五域 × 15 运营模块 × 子域前缀

```mermaid
flowchart TB
  subgraph TRADE["trade 交易域"]
    direction TB
    T1["订单管理 ord_<br/>租借·预约·异常·投诉·退款·押金·免费单"]
    T2["计费定价 price_<br/>模板·差异化·时段价"]
    T3["支付编排 pay_<br/>PaymentPort→nearpay · 渠道配置"]
    T4["账务分润 acct_ share_<br/>复式记账·分润规则/明细/统计"]
    T5["结算对账 stl_ recon_ fin_<br/>结算单·提现·三方对账·发票"]
  end

  subgraph OPS["ops 设备运维域"]
    direction TB
    O1["设备管理 dev_<br/>机柜·仓位·充电宝·影子·OTA·编码"]
    O2["告警管理 dev_alarm_<br/>记录·通知·代码字典·通知规则"]
    O3["工单管理 wo_<br/>状态机·派单·处理·SLA·巡检"]
    O4["站点与点位 loc_<br/>场地方·站点·点位·合同·CRM·Onboarding"]
    O5["代理商 agt_<br/>档案·账号·划拨·分润配置·绩效"]
    O6["库存调拨 inv_"]
  end

  subgraph USER["user 用户营销域"]
    direction TB
    U1["用户管理 usr_<br/>账户·多渠道身份·风控·黑白名单"]
    U2["资产 usr_ mbr_ coupon_<br/>钱包·流水·充值·会员·券"]
    U3["营销 mkt_<br/>公告·活动·推送·裂变"]
    U4["广告 ad_ —— 设备投屏(P2)"]
    U5["客服 cs_<br/>报障受理·会话·消息"]
  end

  subgraph PLAT["platform 平台支撑域"]
    direction TB
    P1["组织权限 iam_<br/>员工·部门·角色·权限·数据范围·菜单·绩效"]
    P2["审计 iam_audit_log —— WORM 只增"]
    P3["消息 notify_<br/>模板·发送记录·全渠道拉黑"]
    P4["系统配置 sys_ md_ dict_<br/>业务规则·登录·版本·税率·银行·问题·市场"]
    P5["租户 tenant 🔒 休眠口子"]
    P6["开放平台 openapi_ ⬜"]
  end

  subgraph GWD["gateway 南向域 gw_"]
    G1["供应商 · 配置 · SN绑定"]
    G2["指令日志（幂等源）· 报文留痕"]
  end

  TRADE <--> OPS
  TRADE <--> USER
  OPS <--> GWD
  USER <--> TRADE
  PLAT -.被全域依赖.-> TRADE
  PLAT -.-> OPS
  PLAT -.-> USER
```

### 4.2 域间协作的三条主链路

| 链路 | 参与域 | 触发 | 关键幂等键 |
|---|---|---|---|
| **借还主链** | user → trade → gateway → ops | C端扫码 | `order_no` · `command_id` |
| **告警→工单链** | gateway → ops(告警) → ops(工单) | 设备上行 FAULT | `wo_order.source_ref`(alarm_no) |
| **诉求→处置链** | user(C端报障) → cs → wo / trade(退款) | C端提交 | `source_ref`(complaint_no) · `idempotency_key` |

> 三条链路的共同点：**跨域动作全部幂等**，重复触发返回首次结果而非产生第二张单。这是分布式下最容易出事的地方，也是 v2 专门立 [db-design §1.6 幂等键约定](./db-design.md) 的原因。

### 4.3 后端代码结构（实际）

```
backend/powerbank-app/src/main/java/ai/neargo/powerbank/
├── portal/                    ★接入层（薄控制器：路由 + @PreAuthorize + 调 service）
│   ├── ops/                   OpsController · TradeController · UserController
│   │                          PlatformController · AgentController · VendorController
│   └── mp/                    ConsumerAuthController · RentController
├── trade/                     OrdStateMachine · RentOrderService ✅落库
├── dev/                       CabinetService ✅落库
├── wo/                        WoStateMachine · WorkOrderService ✅落库
├── loc/                       LocService（站点/点位/场地方/合同）✅落库
├── agent/                     AgentService ✅落库
├── user/consumer/             5 登录策略 + unionid 归并 ✅落库
├── platform/iam/              动态 RBAC（角色/权限/菜单/数据范围）✅落库
├── auth/                      ★横切：双 Token 过滤链 · TokenStore 四实现 · 数据范围
├── config/                    SecurityConfig · MybatisPlusConfig · I18nConfig
├── common/                    ApiResponseWrapper · GlobalExceptionHandler · Messages
└── seed/SeedData              🟡 未落库域的内存种子（渐进替换中）
```

> **分层现状**：`loc / agt / dev / wo / trade` 五域已是完整四件套（Controller→Service→Mapper→Entity）并落库；`user / platform / gateway` 仍是 Controller 直连内存 `SeedData`。详见 [实现状态总表 §二](./实现状态总表.md)。

---

## 五、④ 基础支撑（横切能力）

> 这些不属于任何业务域，但**每个域都依赖**。它们是「一次做对、全域受益」的地基，也是最容易被业务功能挤掉的部分。

```mermaid
flowchart TB
  subgraph SEC["安全与权限"]
    S1["认证 —— 双安全链<br/>@Order1 /mp C池 · @Order2 运营端 STAFF池"]
    S2["TokenStore 四实现<br/>Memory / LocalCache / Redis / MySQL"]
    S3["RBAC 功能权限<br/>@PreAuthorize(@perm.can) · 89 权限码<br/>前后端同一套通配语义"]
    S4["数据范围（行级）<br/>ALL/REGION/SITE/LOCATION/VENUE/AGENT/SELF<br/>MyBatis-Plus 拦截器自动拼条件"]
    S5["租户隔离 🔒<br/>tenant_id 自动注入(恒 MAIN)"]
    S6["脱敏 —— 服务端出参即脱敏<br/>手机/邮箱/密钥掩码 · 明文只在 pb_pii"]
  end

  subgraph LOG["日志与可观测"]
    L1["操作审计 iam_audit_log<br/>@OperateLog 切面 · WORM 只增 · 7年"]
    L2["报文留痕 gw_message_log<br/>上下行原始报文(脱敏) · 可按SN回放"]
    L3["指令日志 gw_command_log<br/>幂等源 + 状态机(PENDING→ACKED/TIMEOUT)"]
    L4["触达记录 notify_log<br/>全渠道 + 成本核算"]
    L5["同意留痕 usr_consent —— PDPL 可举证"]
    L6["应用日志 / APM / 链路<br/>Prometheus + Grafana + SkyWalking ⬜"]
  end

  subgraph PLUMB["通用管道"]
    C1["统一响应 Result{code,msg,data} + PageResult"]
    C2["全局异常 GlobalExceptionHandler + ErrorCode"]
    C3["i18n —— MessageSource + Accept-Language<br/>zh/en/ar · 前端 RTL 镜像"]
    C4["幂等 —— Idempotency-Key + 业务键UK<br/>重复返首次结果"]
    C5["状态机 —— 独立组件<br/>Ord / Wo ✅ · Powerbank / Cabinet ⬜"]
    C6["ID生成 IdGenerator<br/>前缀注册表(60+) · 扫描max+1"]
    C7["事件 DomainEventPublisher<br/>进程内→Kafka 零改代码"]
    C8["调度 XXL-Job<br/>对账·结算·超时关单·告警扫描·巡检"]
  end
```

### 5.1 权限体系（三层，缺一不可）

```mermaid
flowchart LR
  REQ["请求"] --> AUTHN["① 认证<br/>Bearer → LoginUser<br/>realm: STAFF/AGENT/CONSUMER"]
  AUTHN --> AUTHZ["② 功能权限 RBAC<br/>perm code: device:cabinet:read<br/>无码 → 403"]
  AUTHZ --> SCOPE["③ 数据范围<br/>SQL 自动追加<br/>agent_no IN (...) / site_no IN (...)"]
  SCOPE --> BIZ["业务逻辑"]
  BIZ --> MASK["④ 出参脱敏"]
  MASK --> RESP["响应"]
```

| 层 | 回答的问题 | 落点 | 状态 |
|---|---|---|---|
| ① 认证 | 你是谁 | `auth/*TokenAuthFilter` + `cred_credential` | ✅ |
| ② 功能权限 | 你能不能调这个接口 | `iam_role_perm` + `@PreAuthorize` | ✅（🟡 仍读静态 `RolePerms`，未接表）|
| ③ 数据范围 | 你能看哪几行 | `iam_data_scope` + MyBatis 拦截器 | 🟡 **仅 `loc_site` 注册**，`ord_rent`/`wo_order` 有列未注册 |
| ④ 脱敏 | 你能看到哪些字段的明文 | 服务端出参 + `pb_pii` | 🟡 部分 |

> ⚠️ **③ 是当前最大的安全缺口**：`ord_rent` 与 `wo_order` 都已有 `agent_no` 冗余列，但没注册到数据范围表 —— 意味着**代理商角色目前能看到全量订单与工单**。代理端（G5）开放前必须补齐。

### 5.2 日志的四种用途（别混为一谈）

| 类型 | 表 | 答什么问题 | 保留 |
|---|---|---|---|
| **操作审计** | `iam_audit_log` | 谁在什么时候改了什么（**合规追责**）| WORM 7 年 |
| **设备报文** | `gw_message_log` / `gw_command_log` | 设备到底收没收到、回了什么（**排障回放**）| 月分区，热 3 月 |
| **业务流水** | `ord_event_log` / `acct_ledger` / `usr_wallet_txn` | 状态怎么流转的、钱怎么走的（**对账**）| 7 年不删 |
| **应用日志/APM** | 不落库 | 服务健康、慢查询、异常栈（**运维**）| 日志平台 |

> 「设备日志」菜单叶展示的是**前两者的联合视图**：`gw_command_log`(下行) UNION `gw_message_log`(上行) 按时间轴排序 —— 因果关系一眼可读，这是相对竞品单向日志的实质差异。

---

## 六、⑤⑥ 部署形态与「微服务」演进路径

### 6.1 现状：2 进程

```mermaid
flowchart TB
  subgraph NOW["当前部署（MVP）"]
    direction TB
    P1["powerbank-app（Spring Boot 4 / JDK21）<br/>portal + trade + ops + user + platform<br/>域间 = 进程内直调 · 事件 = 进程内分发"]
    P2["access-gateway ⬜ 未建<br/>Netty + MQTT + HTTP回调<br/>长连接与业务解耦"]
  end
  P1 <--> DB[("MariaDB pb_core")]
  P1 <--> RD[("Redis")]
  P2 -.规划.-> DB
```

> `access-gateway` 独立进程是**已定的设计但尚未落地**：当前运营端的远程指令 `POST /cabinets/{no}/commands` 是回执占位（返回 `CMD+nano`），并未真正经网关下发到设备。

### 6.2 演进：按信号裂解，不预先拆

```mermaid
flowchart LR
  M1["M1 模块化单体<br/>1+1 进程"] --> M2["M2 先拆资金<br/>trade 独立"]
  M2 --> M3["M3 再拆运维<br/>ops/wo 独立"]
  M3 --> M4["M4 按需拆<br/>user / platform"]
```

| 阶段 | 拆什么 | 触发信号 | 前置条件 |
|---|---|---|---|
| M1 | 不拆 | — | ✅ 当前 |
| M2 | **trade（订单/支付/账务）** | 资金链路需独立扩缩容 / 独立发布节奏 / 合规审计边界 | 域间调用先改为 `RestClient` + 事件；`pb_core` 按 `ord_`/`pay_`/`acct_` 前缀拆 schema |
| M3 | **ops + wo** | 设备量级上来，告警/工单写入压过交易 | `dev_heartbeat`/`gw_message_log` 已月分区，可整体搬走 |
| M4 | user / platform | 按需 | platform 是全域依赖，最后拆或永不拆 |

**拆分之所以是「搬迁不重写」，靠三个前置设计**（现在就要守住，否则将来拆不动）：
1. **表名 = `<子域前缀>_<实体>` 且库内唯一** → 拆库 = 纯 schema 迁移，零改名。
2. **跨域只有逻辑引用、不建物理 FK** → 拆开后不会有跨库外键要处理。
3. **域间通信已走 `DomainEventPublisher` 抽象** → 进程内分发换 Kafka，业务代码零改动。

> 反过来说：**任何一次「图省事直接 join 另一个域的表」都是在给未来的拆分埋雷**。这是 code review 要盯的第一条。

---

## 七、⑥ 数据层

```mermaid
flowchart TB
  subgraph CORE3["3 库（ADR-010）"]
    direction LR
    C1[("pb_core · 131表<br/>全部业务域合库<br/>子域前缀区分")]
    C2[("pb_pii · 1表<br/>独立KMS + 区域驻留<br/>PDPL 硬要求")]
    C3[("pb_auth · 1表<br/>独立KMS<br/>仅 auth-core 访问")]
  end
  C1 -. 只持 subject_no 逻辑引用 .-> C2
  C1 -. 只持 user_id 逻辑引用 .-> C3
```

| 库 | 为什么单独 | 谁能访问 |
|---|---|---|
| `pb_core` | 业务数据，合库降低运维成本；子域前缀保留拆分能力 | 业务域 + access-gateway（仅 `gw_*`）|
| `pb_pii` | **PDPL 硬要求**：个人数据独立密钥 + 区域驻留，不能并入 core | 业务域经**脱敏接口**，不直连 |
| `pb_auth` | 凭据泄露爆炸半径必须最小 | **仅 neargo-auth-core** |

**分区与保留**：遥测（`dev_heartbeat`/`gw_message_log`）月分区热 3 月；资金流水（`acct_ledger`/`usr_wallet_txn`/`notify_log`）月分区留 7 年；审计（`iam_audit_log`/`usr_consent`）WORM 留 7 年。

**热路径索引**：数据范围过滤 + 列表排序一次走完 —— `(tenant_id, agent_no, status, created_at)`；C端属主查询 —— `(tenant_id, c_user_no, created_at)`。

---

## 八、关键链路时序

### 8.1 借出（跨 4 域 + 网关 + 外部支付）

```mermaid
sequenceDiagram
  autonumber
  participant C as c-app
  participant MP as portal/mp
  participant T as trade
  participant O as ops
  participant G as access-gateway
  participant P as nearpay
  participant D as 供应商柜机

  C->>MP: POST /mp/trade/orders/rent
  MP->>T: 创单
  T->>O: GET /internal/ops/cabinets/{no}/availability
  Note over T,O: 权威二次校验，不信端上传的可借数
  O-->>T: 可借 + 仓位
  T->>P: PaymentPort.preAuth（免押冻结）
  P-->>T: authNo / frozen
  T->>T: 订单 CREATED → DISPENSING
  T->>G: POST /internal/gw/commands (EJECT_SLOT, commandId)
  G->>D: driver.encode / invoke
  D-->>G: RENT_CONFIRMED(宝SN, 仓位)
  G-->>T: DeviceEvent（DomainEventPublisher）
  T->>T: 订单 → IN_USE，开始计费
  T-->>C: 借出成功
  Note over T,G: 超时未确认 → 重试 → 终态失败<br/>→ 订单转 EXCEPTION + 撤销预授权 + 告警开单
```

### 8.2 告警 → 工单（自动化闭环）

```mermaid
sequenceDiagram
  autonumber
  participant D as 柜机
  participant G as access-gateway
  participant A as ops/告警
  participant W as ops/工单
  participant N as platform/消息

  D-->>G: FAULT 上行
  G->>A: POST /internal/ops/alarms
  A->>A: 查 dev_alarm_code 字典<br/>厂商码 → 平台统一码（多厂商归一化）
  alt autoWorkOrder = true
    A->>W: 自动开单（source_ref=alarm_no，UK 保幂等）
    W-->>A: 回填 dev_alarm.wo_no
  end
  A->>N: 命中 dev_alarm_rule → 发通知
  Note over N: 避开 quiet 窗口<br/>超 escalateMinutes 未处理 → 升级
  N->>N: 落 dev_alarm_notice + notify_log
```

### 8.3 C端诉求 → 三条处置出口

```mermaid
flowchart LR
  R["POST /mp/user/report"] --> T["cs_ticket<br/>唯一受理单"]
  T -->|"md_problem.suggestedAction"| D{分流}
  D -->|SELF_SERVICE| E1["自助解决 → 关单"]
  D -->|TO_WORKORDER| E2["wo_order<br/>source=USER"]
  D -->|TO_REFUND| E3["ord_refund<br/>待财务审批"]
  D -->|TO_CS| E4["cs_session 转人工"]
  T -.订单争议额外派生.-> E5["ord_complaint<br/>挂 order_no"]
```

> 分流规则来自**字典而非代码** —— 运营在「问题管理」里调整处置策略，无需发版。

---

## 九、现状对照（图上哪些是真的）

| 层 | 组件 | 状态 |
|---|---|---|
| 前端 | ops-web 98 叶 | ✅ 全有页面（**数据 mock**）|
| 前端 | c-app 借还闭环 | 🟡 后端通，前端多数页仍 mock |
| 前端 | 代理端 | ⬜ 整体未建 |
| 接入 | `/api` 6 前缀 + `/mp` | 🟡 部分端点，248 设计端点中已实现约 30 |
| 业务域 | loc / agt / dev / wo / trade | ✅ 分层 + 落库 + E2E |
| 业务域 | user / platform / gateway | 🟡 Controller 直连内存 `SeedData` |
| 业务域 | 告警 / 报表 / 客服 / 营销 | ⬜ 无端点 |
| 网关 | access-gateway 进程 | ⬜ 未建（指令为占位回执）|
| 支撑 | 认证 · 双安全链 · RBAC | ✅ |
| 支撑 | 数据范围 | 🟡 **仅 `loc_site` 注册**（安全缺口）|
| 支撑 | 审计切面 · IdGenerator · iam 落库 | ⬜ |
| 支撑 | i18n（zh/en/ar + RTL）| ✅ |
| 数据 | `pb_core` | 🟡 部分表建；DDL 已备全量 131 表 |
| 数据 | `pb_pii` / `pb_auth` | ⬜ 未建（DDL 已备）|
| 外部 | nearpay | ⬜ 未接（`PaymentPort` Stub）|

> 权威实现度以 [实现状态总表](./实现状态总表.md) 为准，本表是它的架构视角摘要。

---

## 十、这张图暴露的三个结构性问题

1. **数据范围只注册了一张表** —— `ord_rent` / `wo_order` 有 `agent_no` 列却没接入拦截器，代理端一旦开放即越权。这是**安全问题不是功能问题**，优先级应高于任何新功能。
2. **access-gateway 未建，导致「设备可控」这条 MVP 主线其实是断的** —— 远程指令是占位回执，借出链路的 `EJECT_SLOT` 没有真正的对端。C端借还闭环目前跑通的是「状态机」而非「真实弹仓」。
3. **前端 98 叶全绿 vs 后端约 30 端点** —— 差距约 8 倍。前端已把设计空间铺满，后端补齐的顺序需要明确取舍，建议按 [实现状态总表 §六](./实现状态总表.md) 的优先级推进，而不是按菜单顺序。

---

## 十一、相关文档

| 文档 | 关系 |
|---|---|
| [architecture.md](./architecture.md) | 架构**决策与选型**（技术栈、ADR 索引、演进路线）——本文是它的可视化视图 |
| [api/README.md](../api/README.md) | 248 端点目录（接入层的完整展开）|
| [db-design.md](./db-design.md) | 131 表（数据层的完整展开）|
| [代码结构.md](./代码结构.md) | 目录级代码组织 |
| [权限体系设计.md](./权限体系设计.md) | §5.1 三层权限的详细设计 |
| [TDD-access-gateway.md](./TDD-access-gateway.md) | ⑤ 南向网关的详细设计 |
| [实现状态总表.md](./实现状态总表.md) | **实现度权威**（§九 是其架构视角摘要）|
