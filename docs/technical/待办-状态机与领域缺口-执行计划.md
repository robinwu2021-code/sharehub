# 执行计划 · 状态机闭合 + 领域缺口闭合

> 创建 2026-09-24 · 上游：[领域模型-运营端功能对齐](./领域模型-运营端功能对齐.md)（本轮产出）·
> [实体-领域对象对账表 §三](./实体-领域对象对账表.md)（词表欠账）
>
> **排序原则：先补卡口，再补功能。** 没有卡口护着的修复，下个月会以同样形状长回来 ——
> 三本棘轮台账已经验证过这一点。所以 A 组（卡口）排在 B 组（功能）前面，
> 尽管 B 组里有些条目"看起来更像正事"。

---

## 〇、先说已经不用做的（避免重复劳动）

盘点时发现三件事已被做掉，**计划里不再列**：

| 曾经的待办 | 现状 |
|---|---|
| L1.5 词表收敛（对账表 §3.4 写的还是"建议…待确认"） | **已全部落地**：34 个 enum + `known-bare-status-literals.txt`(47) + 4 条架构测试（含跨端 `StatusVocabularyAcrossEndsTest`）。→ 文档过期，见 C1 |
| 生产化三件套 | `seed.enabled=false` ✅ · `dev-mode.enabled=false` ✅ · `token-store` 缺省已从 memory 改 `mysql+ehcache` ✅。**只剩 pb_pii/pb_auth 分库**，见 D4 |
| [待办-定时推送后端](./待办-定时推送后端.md) | 主体已完成（V75 + 四态 + 幂等键 + 台账 7 条已清零）。**只剩 `sweepDue` 挂调度器**，见 D2 |

---

## 一、阻塞项（不占排期，但要知道在等谁）

| | 在等什么 | 影响 |
|---|---|---|
| **D1** | `docs/api/contract.json` 过期，`IamAdminController` / `OrgController` **两个都在别的会话手里在途** | `check:drift` 现在是红的，但**不是我们的红**。别人提交前不能重新生成，否则会把他们未完成的改动扫进来（本仓库已发生过两次） |
| **D2** | ai-shop 的共享调度器 MJ1–MJ8（[工单](../../..//ai-shop/docs/technical/待办-任务服务多系统改造.md)） | `PushService.sweepDue` 与 `OutboxDispatchScheduler` 都等它。**定时推送现在只能手动触发** |
| **D3** | IAM 多角色线（`IamEmployee` / `IamDept` 转 DTO） | 归那条线的人做；我们只在 `known-api-align-gaps.txt` 留了 `Employee.roleNo/roleNos` 两条带出处的登记 |
| **D4** | pb_pii / pb_auth 分库 | 架构级，需单独立项。**当前唯一一条没有任何卡口盯着的债** |

---

## 二、A 组 · 状态机闭合（先做，因为它是卡口）

### A1 · 6 个状态机内部收敛到 enum —— **它现在正从棘轮盲区里漏过去**

**发现**：8 个状态机里只有 `Wo` / `Alarm` 用了 enum，其余 6 个
（`Powerbank` `InvTransfer` `Ord` `Campaign` `Settlement` `Withdrawal`）**状态仍是裸字符串**：

```java
// SettlementStateMachine —— SettlementStatus 这个 enum 早就存在，这里没用
"CONFIRM", Map.of("GEN", "CONFIRMED"),
```

**为什么棘轮没拦住**：`BareStatusLiteralRatchetTest` 锚在**用法**上
——「字段名以 `Status`/`Type`/`Level`/`Action`/`Mode` 结尾」。而
`Map.of("GEN", "CONFIRMED")` 里没有字段名，于是整类漏检。
**状态机是最不该漏的那一处**：它是这个对象合法状态的定义处。

**技术方案**：机械替换，6 个文件，`XxxStatus` 枚举全部已存在（`SettlementStatus`
`WithdrawalStatus` `PowerbankStatus` `PriceAdjustmentStatus` … 共 34 个）。
`InvTransfer` 需确认是否有对应 enum，没有则新建（照 `AlarmStatus` 的形状）。
出入参保持 `String`（`next(String from, String event)`），只把**字面量的来源**收敛
—— 与 §3.4 已落地的那一轮同一口径，不牵动 MyBatis 类型处理器。

**顺带**：给 `BareStatusLiteralRatchetTest` 加第二个锚点 —— `*StateMachine.java` 内的
所有大写字符串字面量一律计入。补完 A1 后该锚点基线为 0，**零容忍不设台账**。

**验收**：6 个文件内 `grep -E '"[A-Z_]{2,}"'` 只剩事件名（事件是动词，本就该是字符串
—— 见 `WoStateMachine` 注释「状态是名词、事件是动词，类型不同让两者无法被混为一谈」）。

**工作量**：半天。

---

### A2 · 边的跨端卡口（第 6 个检查器）—— **词表卡住了，边没有**

**现状**：`StatusVocabularyAcrossEndsTest` 比的是**节点集**（这个对象有哪些状态），
两端已经一字不差。**没有任何检查比较边**（从哪能到哪）。

**代价是不对称的**：

| 分叉方向 | 症状 | 严重度 |
|---|---|---|
| 前端边**比后端宽** | 按钮亮着，点了报错 | 吵，但安全 |
| 前端边**比后端窄** | **按钮灰着，而接口放行** | 静默失能 |

后者已经发生过：前端把 `accept` 写成 `→PROCESSING`，后端落 `ACCEPTED`，
于是没有任何动作的 `from` 含 `ACCEPTED` —— **那张工单界面上一个按钮都没有**，
处理/完工/驳回全部消失，且不报错。是人肉发现的。

**技术方案** —— `StateMachineEdgeAcrossEndsTest`，照 `AcrossEnds` 同款（自动发现，不靠人登记）：

1. **后端侧**：扫 `*StateMachine.java` 的 `Map.of("EVENT", Map.of(FROM, TO, …))`
   → 边集 `{(from, to)}`。**A1 是前置条件**：状态是 enum 常量才能稳定解析。
2. **前端侧**：扫 `ops-web/lib/types/*.ts` 的
   `export const X_TRANSITIONS: Record<A, { from: readonly S[]; to: S }>`
   → 边集 `{(from, to)}`。
3. **配对靠状态类型名，不靠状态机名**（`WoStateMachine` vs `WO_TRANSITIONS` 机械对不上）：
   前端表的类型注解里写着 `from: readonly WorkOrderStatus[]`，后端状态机 A1 之后
   引用的是 `WorkOrderStatus` —— 而这两个同名词表 `AcrossEnds` 已经保证一字不差，
   **拿它当主键是安全的**。
4. **不比事件名**：后端 `"ACCEPT"`/`"DONE"`，前端 `accept`/`complete` —— 事件名是各端
   的词汇，不是契约。只比 `(from, to)`。

**两个方向分别断言**（严宽不同，这是设计要点）：

- **方向一（零容忍）**：前端每条边，后端必须**可达**。
  注意是**可达**不是**存在** —— 工单 `close` 前端是 `DONE→CLOSED`，后端是
  `DONE→AUDITED→CLOSED` 两跳，这是**合法的复合边**（AUDITED 是过程态，不单独出按钮）。
  要求存在就会误报，所以用后端边集的**传递闭包**判定。
- **方向二（带台账）**：后端每条 `from` 为**用户可见态**的边，前端应有对应动作；
  差异登记进 `known-missing-ui-transitions.txt`，只准变短。
  首批入账的就是 A3 那 5 个 —— 它们现在是"页面手写 if"，卡口一立就显形。

**验收**：两向对照 —— 先把 `WO_TRANSITIONS` 的 `accept` 改回 `→PROCESSING`，
方向一必须红；改回来必须绿。（不做反向对照的卡口，等于没验证断言打在点上。）

**工作量**：1–1.5 天（测试类约 200 行 + 台账 + 接进 `check:drift`）。

---

### A3 · 补 5 张前端迁移表

`Powerbank` / `Ord` / `InvTransfer` / `Withdrawal` / `Alarm` 五个后端状态机
**前端没有对应表**，页面在手写：

- [`finance/page.tsx:782`](../../ops-web/app/finance/page.tsx#L782)
  `(w.status === "AUDIT" || w.status === "APPLY")` —— 就是 `WithdrawalStateMachine`
  的 `APPROVE` from 集抄了一份；
- [`alarms/page.tsx:244`](../../ops-web/app/alarms/page.tsx#L244)
  `a.status === "OPEN"` —— `AlarmStateMachine` 的 `ACK` 边抄了一份。

抄一份不是风格问题：后端加一条边时，这些手抄处**没有任何东西会提醒**。

**技术方案**：照 `WO_TRANSITIONS` 的形状写进各自的 `lib/types/*.ts`，页面改读
`canXxxAction()`。A2 立起来之后这 5 条会自动从台账里显形、修一条删一条。

**工作量**：每个 2–3 小时，可分批。**建议在 A2 之后做** —— 有卡口盯着，改错会红。

---

### A4 · 一处注释引错了 SSOT（顺手）

[`finance/page.tsx:722`](../../ops-web/app/finance/page.tsx#L722) 注释写
「只有 **DRAFT** 能确认（状态机 `STL_TRANSITIONS`）」，而表里和代码里都是 **`GEN`**。
引了 SSOT、引错了值、而且**没真读那张表**（`canSettlementTransition` 全站零页面引用）。
改注释 + 改读表，10 分钟。

---

## 三、B 组 · 领域缺口闭合

来自[对齐文档](./领域模型-运营端功能对齐.md) §三。四条有共同形状：
**C 端或后端已经在产生数据，运营端没有对应的消费面** —— 不报错、不红、
用户也不会投诉（他们不知道本该有）。

### B1 · 订单事件读侧 —— **写进去了，没人读得到**（最高性价比）

`ord_event_log` 有表、`OrderEventLogService.timeline(orderNo)` **已实现**、
`Refund`/`Deposit`/`OrderException`/`Complaint` **四个 service 都在写**，
而 `timeline()` 的**调用方是 0 个**，没有 Controller 暴露它。
订单详情页那个 `<Timeline>` 渲染的是干预历史（`ord_intervention`），不是状态流转。

**技术方案**：
1. 后端 `OrderController` 加 `GET /api/trade/orders/{orderNo}/events` → `List<OrderEvent>`
   （DTO `OrderDtos.OrderEvent` **已存在**，不用新建）；
2. 前端 `lib/types/order.ts` 加 `OrderEvent` 类型 + contracts/https/mocks 三件套；
3. 订单详情抽屉加「状态流转」时间线，**与「干预历史」并列**（两者语义不同：
   一个是状态怎么走的，一个是人做了什么，合并会丢信息）；
4. `contract.test.ts` 端点计数 +1。

**验收**：造一张走过 EXCEPTION 的订单，详情页能看到完整迁移链。
**工作量**：半天。**注意 D1** —— contract.json 要等别的会话提交后才能重新生成。

### B2 · 注销申请的运营端队列（合规）

C 端 `POST /mp/user/logoff` 已开放，`usr_logoff` 有 PENDING 与冷静期，
**运营端零入口** ⇒ 冷静期内没有任何人能看到待注销队列，也没有撤销入口。
`UserLogoffService` 注释写明「冷静期已过此时数据可能已开始清除」——
误操作的用户只能等数据被清掉。

**技术方案**：`/users` 下加 tab「注销申请」（权限码需先在
[功能权限清单](../requirements/功能权限清单.md)中确认存在，勿新造）；
列表 + 撤销动作，走 `UserLogoffService` 既有方法。

**工作量**：1 天。

### B3 · 停用的员工仍能继续用旧令牌（本轮查得最实的一个洞）

**结论先说**：员工停用后，其**已签发令牌继续有效**，而且现有的
`PermVersion` 机制**救不了** —— 这不是"少调一个撤销方法"，是重建逻辑本身不回查在职状态。

证据链：

1. `TokenStore` 只有 `revoke(String token)`，**没有按人撤销**；
   `sys_token` 有 `subject_no` 列但**没有索引**（PK 是 token，另一个只有 `idx_token_expire`）；
   `RedisTokenStore` 的键只有 `pb:auth:token:{token}`，**无二级索引**。
   ⇒ 现在根本无法"撤销某人的全部会话"。
2. 看起来有个现成机制 `PermVersion.bump()`（改数据范围时就在用，"在线生效"），
   `StaffTokenAuthFilter` 每请求比对会话戳，变了就
   `refresher.rebuild(d.user(), d.roleNos())` 重建会话。
3. **但 `PermissionService.rebuild()`（:136）是按 `old.realm()/old.userNo()` 与
   *会话里带来的* `roleCodes` 重算权限的，全程不读 `iam_employee`。**
   ⇒ 停用一个员工再 bump，他的会话只会"照原样重算一遍"然后放行。

**技术方案**（两步，第一步单独可上，成本小且用的是现成机械）：

- **第一步 —— 让重建回查在职状态。**
  `rebuild()` 增加一次主体有效性校验（`iam_employee.status == ACTIVE`），
  失效则让过滤器按未认证处理；`EmployeeServiceImpl` 的停用/删除路径调 `permVersion.bump()`。
  这样停用后**下一次请求**即失效，不需要新建任何索引。
  ⚠️ 注意 `rebuild()` 在 `sharehub-common`（infra 层）被调用，回查要走
  已有的 `PrincipalRefresher` 抽象，**不能让 infra 反向依赖 IAM**（ADR-015 分层）。
- **第二步（可后置）—— 真正的按人撤销**：`sys_token.subject_no` 加索引 +
  `TokenStore.revokeBySubject(realm, subjectNo)`；Redis 侧加
  `pb:auth:subject:{realm}:{no}` → token 集合的二级索引（**不能用 SCAN**）。
  会话列表 / 手动踢人界面建在它上面。

**顺带记一个已存在的限制**（不在本任务范围，但与 D4/多副本相关）：
`PermVersion` 是**进程内的 `AtomicLong`**。多副本部署时一个副本 bump
不会传到另一个副本 ⇒ "改权限即时生效"在多副本下**只对自己这台成立**。
切 `token-store: redis` 做水平扩展时，这一条必须一起解决。

**验收**（两向）：停用员工后，用他原来的 token 调任意接口必须 401；
停用前必须 200。配场景测试。

⚠️ `EmployeeServiceImpl` **当前在别的会话手里在途**（见 D3）—— 先沟通或等他们提交。

**工作量**：第一步 1 天（含分层与测试）；第二步 1.5 天。

### B4 · C 端开票申请的受理面

`usr_invoice` / `usr_invoice_title`，C 端可提交（本轮刚把 `InvoiceApplyReq` 转成 DTO
的那个端点）。运营端「发票」叶子管的是 `fin_invoice` ——**给场地方/代理商开的结算发票**，
与 C 端开票是两个对象。⇒ 消费者提交的开票申请落库后没有受理界面。

与 B2 是**同一个模式**（C 端提交、运营端受理），建议一起做，共用列表/受理组件。
**工作量**：与 B2 合并做的话 +0.5 天。

> §三 余下三条 —— 同意记录 Consent（后端零引用，**整条没做**，该走需求而非本计划）、
> 设备类型 DeviceType（ADR-021 预留）、自然人 Principal（ADR-030 待建表）——
> **随各自 ADR 放行，不单独排期**。

---

## 四、C 组 · 文档同步

- **C1** [实体-领域对象对账表](./实体-领域对象对账表.md) §3.4 还写着「建议的收敛方式（待确认）」，
  实际已全部落地。改成已落地 + 指向现存的 4 条测试与台账。**10 分钟，但不改的话
  下一个读到的人会以为还没做，可能重做一遍。**
- **C2** [实现状态总表](./实现状态总表.md) 补本轮：领域对齐文档、A/B 组任务、三条已完成项。

---

## 五、建议顺序

```
A4 (10min) → C1 (10min) → A1 (半天) → A2 (1.5天) ──┬─→ A3 (分批，各 2-3h)
                                                    └─→ B1 (半天，等 D1)
B2+B4 (1.5天，合做) ──→ B3 第一步 (1天，等 D3 让路)
```

**理由**：A4/C1 是十分钟的清理，先清掉；A1 是 A2 的前置；A2 立起来之后，
A3 的 5 条会自动从台账里显形，改一条删一条，**不需要记得**。
B 组不依赖 A 组，可并行，但 B1 卡 D1、B3 卡 D3。

**一条都不做的话**：A 组的洞会继续以「按钮灰着但接口放行」的形状出现，
且**只能靠人肉发现**；B 组的四条会继续在无人知晓中积累数据。
