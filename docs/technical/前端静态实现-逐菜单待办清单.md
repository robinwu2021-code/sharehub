# 前端静态实现 · 逐菜单待办清单（待确认后实施）

生成：2026-07-30 · **状态：✅ 已确认（用户 2026-07-30），按 §四 批次实施中**
目标（用户定调）：**先不管后端 —— 纯前端 + mock 把所有静态功能做完；API 与后端一致、mock 规范符合后端规范；将来一键切后端。**

---

## 一、对齐标准（先定标准，再谈完成度）

「API 与后端一致」「mock 符合后端规范」需要可核验的标准。已核对后端源码，结论：

### 1.1 已经对齐的（无需改动）

| 项 | 后端 | 前端 | 结论 |
|---|---|---|---|
| 响应包裹 | `Result{code:0,message,data}` (`ApiResponseWrapper`) | `http-client` 按 `body.code!==0` 抛错、返回 `body.data` | ✅ 一致 |
| 分页返回 | `PageResult{list,total}` (`Pages.of`) | `PageResult{list,total,page?,size?}` | ✅ 兼容（page/size 可选）|
| 分页入参 | `page` 从 1 起、缺省 `page=1/size=10` | 同 | ✅ 一致 |
| 字段命名 | camelCase | camelCase | ✅ 一致 |
| 业务主键 | `xxxNo`（`cabinetNo`/`siteNo`/`agentNo`）| 同 | ✅ 一致 |
| 时间字段 | `xxxAt` 字符串 | 同 | ✅ 一致 |
| 枚举 | 大写下划线字符串（`IN_CABINET`/`ONLINE`）| 同 | ✅ 一致 |
| 端点前缀 | `ops/platform/trade/user/agent/auth` + `internal` | 已于 2026-07-29 收敛为同一套 | ✅ 一致 |

> **结论：规范层面前后端已经一致，不需要重构。** 这点比我预期的好。

### 1.2 需要处理的偏差（3 项）

| # | 偏差 | 处置建议 |
|---|---|---|
| A1 | **`Cabinet` 前端缺 `siteNo` / `agentNo`** —— 后端 `DevCabinet` 有这两列，前端类型没有，导致「机柜归属哪个站点/代理」在前端拿不到 | ✅ **已闭合**：`agentNo` 随 S1 补齐（划拨功能的写入点）；`siteNo` 随 S5 补齐——**不独立维护**，恒等于 `locationNo` 所在点位的站点（`siteNoOfLocation()` 反查，与后端实体注释「冗余·随点位级联」同一口径），设备台账列表/导出/详情与关键词搜索都已带上，引用完整性测试覆盖 `cabinets.siteNo → sites.siteNo` + 「与 locationNo 所属站点一致」不变量。⚠️ **遗留（记入后端待办）**：后端**实体**有 `siteNo`，但 **DDL `dev_cabinet` 没有 `site_no` 列**（`pb_core-device-gateway.sql` 只有 `agent_no`/`location_no`，v2-alter 也未补）→ 需补一条 `ALTER TABLE dev_cabinet ADD COLUMN site_no`；在此之前接真后端该字段可能为空，前端一律显示「未归属」，不猜 |
| A2 | **`archivedAt` 后端实体没有** —— B6 软删除是前端先做的，15 个实体都加了 `archivedAt`，后端实体与 DDL 都还没有 | 前端不动（保持规范）；**记入后端待办**：15 张表加 `archived_at` 列 |
| A3 | 前端 `PageResult` 回传 `page/size`，后端不回 | 不改。前端已按可选处理，分页状态由前端自己维护 |

---

## 二、逐菜单完成状态（98 项）

### 判定口径

| 级别 | 含义 |
|:--:|---|
| **F3** | 静态功能完整：列表 + 应有的写操作（mock 真落库）+ 空态/权限/导出 |
| **F2** | 主功能通，缺次要项（详情抽屉 / 筛选 / 导出 / 统计条之一）|
| **F1** | 只有列表，**业务上应该有的写操作没有** |
| **F0** | 页面在但动作是**伪实现**（点了没反应）|

---

### 概览 · 经营看板（1 项）

| 子功能 | 级别 | 缺什么 / 待实施 |
|---|:--:|---|
| 经营总览 | **F3** | — 已补 3 个页内快捷动作（远程弹出 / 开工单 / 查订单，`components/quick-actions.tsx`）：抽屉复用 `ui/drawer` + `ui/form-drawer`，接口与设备/工单/订单页同一批；机柜目标一律从台账下拉选（无自由文本，防指令下发到不存在的柜子），按 `device:command:send` / `workorder:wo:create` / `order:order:read` 逐个显隐。<br>✅ 2026-08-04 补：订单页已读 `?keyword=` 深链（初始化 + 参数变化响应），「去订单页做干预」落地即预填该单号（[TDD-运营端前端静态收尾](TDD-运营端前端静态收尾.md)）|

### 设备运营 · 设备管理（8 项）

| 子功能 | 级别 | 缺什么 / 待实施 |
|---|:--:|---|
| 设备台账 | **F3** | ✅ S8 已补 **机柜建档/编辑抽屉**：主按钮从「导入台账」换成「新增机柜」（导入退到筛选区，加一台机柜不必再先编 CSV），行内加「编辑」。抽屉**只收建档字段**（SN/供应商/型号/仓位数/点位/出厂固件/设备状态）——`siteNo` 与 `locationName` 由所选点位反查（抽屉里实时回显「归属站点」，塞 `siteNo` 一律不采信，否则 `lib/mock/db/integrity.test.ts` 的「siteNo ≡ 点位所属站点」不变量就破了）；`agentNo` 只能经代理域划拨；`onlineStatus`/`lastHeartbeatAt`/`availableCount` 是设备上报的事实，表单填不动。校验全部落 mock 层 `saveCabinet`（SN ≥4 位且全平台唯一 / 柜机号 `CAB+数字` / 供应商须已接入 / 点位须已建档 / 仓位数 1~48 且**不得缩到在仓充电宝数以下**），与导入共用同一套口径，换入口塞不进脏数据；`importCabinets` 一并改为反查 `locationName`。⚠️ **后端缺口**：机柜**写端点后端完全没有**（`OpsController` 只有 `GET /cabinets`、`GET /cabinets/{no}`；`/cabinets/import`、`/archive`、`/unarchive` 同样缺），已在 `lib/api/https/device.ts` 标注，`USE_MOCK=0` 时建档/编辑必然 404。<br>🔸 S5 已补 **归属站点**（`Cabinet.siteNo`，见 §一 A1）：列表新增「归属站点」列、导出加同名列、详情页「归属站点 · 点位号」、关键词可搜站点号；值由点位反查，导入时不从 CSV 采信 |
| 充电宝管理 | **F3** | — |
| 实时监控 | **F3** | ✅ 已接「故障→自动开工单」联动 —— 这是告警规则引擎缺的最后一环：`AlarmCode.autoWorkOrder` 开关与幂等的转工单端点**都早已存在，但没有任何东西把两者连起来**，开关形同摆设。现按码字典逐条开单并派运维，**天然幂等**（复用 alarmNo 幂等键），重复点只增 skipped —— 一个反复上报的故障刷出一堆重复工单是运维成本事故。只处理未关闭且尚无工单的告警（不翻历史）；是否开单只看码字典不看等级（BATTERY_LOW 是 WARN 但不必开单）。地图瓦片受环境限制属既有降级，非缺口 |
| 远程控制·指令记录 | **F3** | ✅ S8 已补 **单柜指令下发入口**：tab 右上「下发指令」抽屉（选机柜 → 选指令 → 仓位号）+ 行内「重新下发」（带原记录的机柜/指令/仓位预填，超时或失败最常见的处置就是照原样再发一次），权限 `device:command:send`，无码时显示 `ReadOnlyNotice` 而非隐藏。语义与 `/devices/detail` 完全一致（同一个 `api.sendCommand`、同一份 `COMMAND_TYPES` 词表、同样「不带仓位 = 任意仓」），不另造第二套。<br>🔸 顺带修掉两处名实不符：① 指令词表原先三处各说各话（详情页发 `EJECT_ANY`/`EJECT_SLOT`、批量发 `FW_SYNC`，而 `CommandRecord.type` 只有四值 → 记录里放不下这些指令），已收敛为 types 层 SSOT `COMMAND_TYPES = EJECT/LOCK/REBOOT/LOCATE/FW_SYNC`（弹仓靠有无 `slotIndex` 区分任意仓/指定仓），`SLOT_REQUIRED_COMMANDS` 供抽屉与落库校验共用；② mock 的 `sendCommand` 原先只回一个假 `commandId`，「下发成功了但指令记录查不到」，现在**下发即落一条 `SENT` 记录**（不直接写 ACKED——那等于伪造设备回执），批量下发同样留痕。守卫在 `recordCommand`：机柜不存在/已归档、未知指令名、锁仓缺仓位、仓位号越界一律拒绝 |
| 设备日志 | **F3** | — |
| 库存调拨 | **F3** | — |
| 固件 OTA | **F3** | — |
| 设备编码 | **F3** | — |

### 设备运营 · 告警管理（4 项）

| 子功能 | 级别 | 缺什么 / 待实施 |
|---|:--:|---|
| 告警记录 | **F3** | — |
| 告警通知 | **F3** | ✅ 已补：**重发通知**（拍板 #6，口径与「系统设置 › 发送记录 › 重发」逐条对齐——同一条触达链路，两个入口判得不一样运营就会绕）。`idempotencyKey` 必填、同键第二次 mock 层直接拒；**只许重发失败通知**；目标命中未到期的**触达拉黑**（名单存脱敏值、通知存原值，两种形态都比）一律拒；**告警已关闭**也拒（事已了结还催值班人是纯噪音）；重发是**新增一条**、原记录一字不改（列表 + 导出加「重发自」列）。键在全部校验通过后才登记，校验失败不烧键。seed 里埋了一条「目标已退订」的失败通知（AN50004），否则拉黑分支在页面上永远走不到。新权限码 **`workorder:alarm:notice_resend`**（不复用 `system:notify_log:resend`：那是系统设置域的资源，且 CS 有它——客服不该重发设备告警给值班工程师）。⚠️ 后端缺口：`AlarmController` 的 `/notices` 是 append 表、只读，无 `/notices/{no}/resend`；落地时**后端必须自己按幂等键去重**，前端这把键挡不住刷新重放与网关重试 |
| 告警代码 | **F3** | — |
| 通知规则 | **F3** | — |

### 设备运营 · 工单管理（4 项）

| 子功能 | 级别 | 缺什么 / 待实施 |
|---|:--:|---|
| 工单列表 | **F3** | 全流转已通（含我补的 rework 返工）|
| 工单看板 | **F2** | **不可拖拽**（用动作按钮替代）。待确认：是否要原生 HTML5 拖拽 |
| SLA 管理 | **F3** | — |
| 巡检计划 | **F3** | ✅ S7 已补：**「立即执行一次」**（`workorder:inspection:update` + `workorder:wo:create` 双码，不新造权限码）。按路线拆站点，**每站真的落一张 INSPECT 工单**（`source=PLAN`、`sourceNo=planNo`，工单列表搜计划号即可认回），并经状态机 `dispatch` 派给计划负责人——不直接写 `assigneeName`。**幂等**：周期键 `inspectionPeriodKey(frequency)`（每日按天/每周按自然周序/双周/每月按月）作为唯一键，同计划同周期第二次执行整批拒绝；判定用的 `inspectionRunnable()` **页面按钮与 mock 校验共用一份**，杜绝「按钮亮着点了报错」。守卫：计划停用、路线站点在台账里没有在册机柜、路线为空一律整批拒绝，不做半成功；`nextAt` **不推进**（手动补跑不代表排期走过）；`saveInspectionPlan` 剥离 `lastRun*`，表单塞不出「已执行」。⚠️ **后端缺口**：`WoExtController` 无 run 端点、也无定时任务，已在 `lib/api/https/workorder.ts` 标注，`USE_MOCK=0` 时该动作 404 |

### 渠道与场地 · 站点与点位（8 项）

| 子功能 | 级别 | 缺什么 / 待实施 |
|---|:--:|---|
| 站点管理 | **F3** | ✅ S4 已补：站点抽屉加 `lat/lng` 数字输入 + 列表/导出加「坐标」列。**不做地图选点**——底图依赖 `NEXT_PUBLIC_GMAPS_KEY`，缺 key 时地图整体降级为占位列表，编辑入口若挂在地图上，没配 key 的环境就彻底改不了坐标。校验落 mock 层 `assertSiteCoords`：窗口取 `SITE_COORD_BOUNDS`（UAE 外接矩形，types 层 SSOT，抽屉提示与落库校验同源），专门抓「经纬度写反」——全球范围合法但会把点扔进印度洋，表现为「地图上少了个站点」而非报错。⚠️ 后端缺口：`Dto.Site` / `LocSite` 都没有 lat/lng 字段（DDL `loc_site` 有 `lng`/`lat` 列），后端补字段才能真正落库 |
| 点位管理 | **F3** | — |
| 站点坪效 | **F3** | ✅ 已补周期切换（复用报表域 `REPORT_PERIODS`，导出早已有）。**关键是数字真的跟周期变**：整表改由报表域的 `dayFact(站点,日)` 事实层派生，`turnover` 由 订单/柜数/天数 真算（原为 `1.2+(i%7)*0.6` 的凭空数）。与点位报表**同源**，按站点求和后总营收必然相等（`site-analysis.test.ts` 断言的是跨视图等式而非硬编码数字）。后端已用同一思路收口（`SiteAnalysisServiceImpl` 委托 `ReportService`）|
| 场地方 | **F3** | — |
| 进场合同 | **F3** | ✅ S4 已补：附件抽屉 + **假上传**（拍板点 #3）。只取 `File.name` / `File.size` 登记一条 `ContractAttachment`，字节流不读不传；**故意不给下载/预览入口**——给一个点不开的链接比明确没有更难查。守卫在 mock 层：格式白名单（`ATTACH_EXTS`）/ 单份 ≤10MB / 空文件 / 同名重复（无内容哈希可比，同名会让「哪份最新」无从判断）；`saveContract` 兜住「编辑合同不清空附件」。列表加「扫描件」列（缺件一眼可见）。⚠️ 后端缺口：合同写入侧后端**连 `POST /api/ops/contracts` 都没有**（只有 GET），附件端点 `POST /contracts/{no}/attachments`、`.../{attachNo}/remove` 更是全缺 |
| 门店 Onboarding | **F3** | — |
| 门店生命周期 | **F3** | ✅ 已补：「推进阶段」抽屉 + 留痕。**刻意不做状态机** —— 后端 `SiteLifecycleServiceImpl` 明注不设转移图（`db-design §9A` 只为 `ord_rent`/`wo_order`/`dev_*` 冻结状态机，且「CHURNED 的店重新签回来」是正常业务）；前端比后端严会造出「接口能做、按钮不给点」的幻觉约束，故只校验取值域与非空转，并有反向断言 `CHURNED → SIGNED` 必须成功。`gmvLtm` 只读展示不给手填（那是阶段决策快照，手填会污染）|
| BD 拓展 CRM | **F3** | ✅ S4 已补：线索详情抽屉 + 跟进时间线（复用 `components/ui/timeline.tsx`）+ 「记一条跟进」。**跟进与阶段推进是同一个动作**：阶段变化必须由某一条跟进来解释，否则又回到「阶段悄悄变了没人知道为什么」。db 层保证 `lead.updatedAt` = 最新跟进时间（列表「最后跟进」列与时间线首条同源），阶段没动时 `fromStage` 记 null 不造「A → A」假迁移；种子自洽（负责人 = 线索负责人、阶段链首尾相接且末条 = 当前阶段、停留 NEW 的线索无记录以覆盖空态）。⚠️ 后端缺口：跟进记录表与 `GET/POST /api/ops/leads/{leadNo}/follow-ups` 后端都还没有（`LocExtController` 只有 `/leads` 与 `/leads/{leadNo}`）|

### 渠道与场地 · 代理商管理（6 项）

| 子功能 | 级别 | 缺什么 / 待实施 |
|---|:--:|---|
| 代理商档案 | **F3** | — |
| 代理账号管理 | **F3** | — |
| 设备/点位划拨 | **F3** | ✅ S1 已补：划拨/回收双抽屉（选代理 + 多选机柜/站点，选项标注当前归属防误划）。唯一写入点是资产上的 `cabinets[].agentNo` / `sites[].agentNo`，代理侧的设备数/站点数全部由 `refreshAgentAssetCounts()` 反算，所以划拨后汇总当场变；每次操作落 `agentAssignmentRecords` 审计。守卫：代理不存在/已归档/已停用、空选择、重复划拨、回收直营资产一律整批拒绝，不做半成功 |
| 分润配置 | **F3** | — |
| 代理收益结算 | **F3** | 深链复用财务结算单 |
| 代理绩效 | **F3** | ✅ 已补周期切换 + 导出。代理 GMV 改为**名下站点营收之和**（`sites[].agentNo` 是归属真值），与站点坪效同源、同一套周期口径；原为 `40000+i*7919%200000` 的凭空数，与任何页面都无关且切周期不动。`rank` 由 GMV 降序现算（写死必与 GMV 漂移）。未划拨资产的代理 GMV=0 但仍列出 —— 运营需要看到「有档案没资产」|

### 交易与资金 · 订单管理（7 项）

| 子功能 | 级别 | 缺什么 / 待实施 |
|---|:--:|---|
| 订单列表 | **F3** | ✅ S1 已修：干预状态机 `ORDER_INTERVENTIONS`（types 层单一定义，页面按钮/mock 校验共用）+ 四动作真改订单（eject→DISPENSING 记弹出次数；force_return→SETTLED 写结束时间/时长/费用；waive 应收置 0 记减免额；compensate 累加补偿额）+ 每次干预落 `OrderIntervention` 审计记录（原因必填），详情抽屉展示干预时间线 |
| 预约订单 | **F3** | — |
| 异常订单 | **F3** | ✅ S2 已补：**处置动作**（`order:exception:handle`）。状态机 `EXCEPTION_HANDLINGS`：`PENDING → HANDLING → HANDLED`；转工单会**真的落一条工单**（设备类→FAULT / 计费类→COMPLAINT，`sourceNo` 挂异常单号），发起退款走 S1 同一套退款申请通路并回填 `refundNo`，直接关闭为终态。同一种下游动作不可重复（已有工单/退款单再点被拒），结论必填，记 `handledBy`/`handledAt`/`handleResult` |
| 投诉订单 | **F3** | — |
| 退款记录 | **F3** | — |
| 押金与欠费 | **F3** | ✅ S1 已补：押金状态机 `DEPOSIT_TRANSITIONS`（HELD→RELEASED 解冻 / HELD→BOUGHT_OUT 买断 / ARREARS 催缴只留痕），mock 层强制非法迁移抛错；解冻·买断走 `useConfirm` 二次确认，买断填金额（≤押金额）、催缴选渠道，列表加状态筛选与处置留痕列 |
| 免费订单 | **F3** | — |

### 交易与资金 · 计费定价（3 项）

| 子功能 | 级别 | 缺什么 / 待实施 |
|---|:--:|---|
| 计费模板 | **F3** | — |
| 差异化定价 | **F3** | ✅ S6 已改：**站点下拉**（选项来自场所域真站点）。规则新增 `siteNo` 作为定位键（对齐后端实体 `PriceRule.siteNo`），`scene`/`locationName` 降级为冗余展示列、由服务端按 `siteNo` 反查覆盖——调用方传的名字一律不采信，`savePricingDiff` 对站点不存在直接抛错（引用完整性测试覆盖 `pricingDiffs.siteNo → sites.siteNo` + 号名成对一致）。✅ 2026-08-04 三维已开（[TDD-运营端前端静态收尾](TDD-运营端前端静态收尾.md)）：对齐后端实体 `dimension(SITE/LOCATION/SCENE) + matchRef`，表单按维度切换目标下拉（站点/点位/场景三套真实选项源），mock 层按维度做引用校验（点位不存在拒 / 场景无任何站点拒），冗余列一律服务端反查覆盖；种子覆盖三维。⚠️ 后端缺口：`PriceDtos.PricingDiff` 尚未透出 `dimension/matchRef/siteNo`（实体都有），接真后端前需补 DTO |
| 活动/时段价 | **F3** | ✅ S6 已改：**结构化时段编辑器**（「按星期/时刻」= 星期多选 + `HH:mm` 区间，跨零点合法；「自定义表达式」兜住公共假日/斋月全月这类日历事件）。存储**不变**——仍折叠回 `period` 一列（后端 `price_schedule.period` 就一列表达式），`parsePeriod`/`formatPeriod` 在 `lib/types/pricing.ts` 单一定义，页面预览、编辑回填、mock 归一化共用；round-trip 恒等由测试钉住，老数据编辑一次不会被改写。时刻只填一半提交前拦下（静默丢弃最难查） |

### 交易与资金 · 财务管理（11 项）

| 子功能 | 级别 | 缺什么 / 待实施 |
|---|:--:|---|
| 分润规则 | **F3** | ✅ S7 已补：**双向视图**（「按场地方看 / 按代理商看」视角切换）。切的是同一份规则的 `dimension` 参数，不是两套规则——**筛选落在服务端**（`ShareRuleQ.dimension`），`total` 是筛后总数，翻页/搜索都在筛过之后进行（前端筛只会筛当前页）。视角选定后「维度」列并入表头（「分成方」→「场地方 / 代理商」），新增按钮默认落当前视角（否则在代理视角下新建出一条场地方规则会当场从列表消失），导出文件名带视角。⚠️ 后端缺口（参数级）：`GET /api/trade/share-rules` 只收 page/size/keyword，不认 `dimension` |
| 分润明细 | **F3** | — |
| 分润统计 | **F3** | — |
| 结算单 | **F3** | ✅ S1 已补：生成（选对象类型+对象+周期）→ `DRAFT` → 确认 → `CONFIRMED`，状态机 `STL_TRANSITIONS` 在 mock 层强制；**金额不凭空造**，由 `aggregateShareRecords()` 从分润明细按 `dimension+payeeNo+period` 汇总，详情抽屉可下钻到构成它的明细；同对象同周期重复生成被幂等拒绝。三页同源：确认后分润统计的待结算额同步下降 |
| 账务分录 | **F2** | ✅ 已补**凭证下钻 + 借贷平衡判定 + 期间筛选**。凭证号做成入口：分录表是平铺的，同一张凭证的借贷两方可能隔着几页，逐条看根本判断不了平不平；不平的凭证是记账错误，抽屉里直接给结论（判定在服务端算，前端不心算）。⚠️ **手工记账刻意没做**，故仍是 F2 —— 那是会计操作，开口子前必须先定「谁能记、能不能改已过账凭证、如何强制借贷平衡与留痕」；做一个不校验平衡的「手工记账」比不做更危险。需单独拍板 |
| 对账 | **F3** | ✅ S2 已补：**差错处理**（`finance:recon:handle`）。跑批结果 `status` 与处置进度 `handleStatus` **分两列**——「已核对无误」不回写 MATCHED，不篡改跑批事实。状态机 `RECON_TRANSITIONS` 在 mock 层强制：`OPEN → HANDLING → RESOLVED/IGNORED`，四个动作 已核对无误(→IGNORED) / 标记平台侧(→HANDLING) / 标记渠道侧·挂起待回执(→HANDLING) / 发起补差(→RESOLVED)，各自落 `handleResult` 定责；**结论必填**，记 `handledBy`/`handledAt`/`handleNote`；终态不出按钮。差错分类按 `diff` 正负解读（渠道多/账务少记 ⇄ 账务多记），不造与数据对不上的枚举。汇总条（4 张 StatCard）由 `getReconStats()` 从同一份 `reconciles` 实时重算，**处理一笔未结笔数/金额当场降** |
| 发票 | **F3** | ✅ S2 已补：**开具/作废**。状态机 `INV_TRANSITIONS`：`DRAFT → ISSUED → VOID`，**作废只能从 ISSUED 走**（草稿改错直接编辑，不占作废号）。开具生成 `invoiceCode`/`invoiceNumber`（号码取现有最大值 +1）+ `issuedAt`/`issuedBy`；**开具后抬头/金额锁死**——`saveInvoice` 服务端直接拒，页面编辑按钮禁用并写明原因。作废**原因必填** + `useConfirm` 二次确认，记 `voidedAt`/`voidedBy`/`voidReason`。发票**金额不自造**：新增 `sourceType`/`sourceNo` 挂到一张**已确认**的结算单，抬头/金额/币种由该单带出，开具时再校验一次金额对平（引用完整性测试已覆盖 `invoices.sourceNo → settlements.settleNo`）。作废权限码 `finance:invoice:void` 为本批新增，已写入 [功能权限清单](../requirements/功能权限清单.md) §6 |
| 代理分润配置 | **F3** | 深链 |
| 提现审核 | **F3** | ✅ S7 已补：**手续费接「业务规则」**（`GET /api/platform/biz-rules` 的 `withdraw.feeRate/feeCap/minAmount`，后端 `SysBizRule` 注释同款要求：`fin_withdrawal.fee` 必须取自这里）。口径函数 `computeWithdrawFee`/`withdrawFeeOf`/`withdrawNetOf` 收在 `lib/types/finance.ts`，**表格、审批抽屉、CSV 导出与 mock 种子四处同源**，不再各算一遍；原先写死的 0.6% 与「下限 2 AED」一并删除（下限业务规则里根本没这个字段，是财务侧自己多存的阈值）。语义分两档：**未审批（APPLY/AUDIT）按现行费率实时算**（审核页给的必须是点「通过」那刻真会扣的数），**已审批按落库值**（事后调费率不能改写历史放款额）；规则取不到（未加载 / 无 `system:biz_rule:read`）退回落库值并在提示条里写明，不假装已重算。审批抽屉另加「低于最低提现额」红标 |
| 用户钱包 | **F3** | 深链 |
| 充值订单 | **F3** | — |

### 用户与服务 · 用户管理（7 项）

| 子功能 | 级别 | 缺什么 / 待实施 |
|---|:--:|---|
| 用户列表 | **F3** | ✅ S4 已补：**用户详情抽屉**（档案/风控/钱包/会员次卡/订单一屏）。数据走**一个** `getUserProfile`（⚠️ 后端缺口 `GET /api/user/users/{no}/profile`），不在页面里按用户号调五个列表拼——列表 keyword 是模糊匹配，`U300` 会串出 `U3001~U3009`。抽屉里每一块都是对应 tab 的**同一批记录**（订单徽标直接复用订单页的 `OrderStatusBadge`），一致性由 `lib/mock/db/user-profile.test.ts` 钉住。顺带修掉两处早就存在的跨页矛盾：① `cUsers[].orders` 原是自造计数（列表写 21 单、订单表实存 3 单），现在与订单表实存条数对齐并由单测防漂移；② members/wallets/freeWhitelist 原各用 `NICKS`/`phone()` 另编昵称手机（U3006 在「用户」页叫 Ahmed、在「钱包」页叫 Noura），一律改为反查 `cUsers`。抽屉不重复实现写操作：「查看全部流水」「调整信用分」跳既有抽屉 |
| 风控用户 | **F3** | ✅ S2 已补：**信用分调整**（`user:risk:update`）。0–1000 越界**抛错不静默截断**；`cUsers[].creditScore` 是唯一真相，`userRisks[].creditScore` 只是投影。落 `CreditScoreChange` 留痕（前后分值/增减/原因/操作人），抽屉里展示调分历史时间线。联动已实现：按阈值重算等级（<560 高 / <640 中 / ≥640 低），掉到 640 以下且不在名单则**自动补一条风控记录进观察名单**；分数回升**不自动移出名单**——名单是审计痕迹，静默消失会让复核无从查起，移出由人工操作 |
| 黑名单 | **F3** | — |
| 免费用户白名单 | **F3** | — |
| 会员/次卡 | **F3** | ✅ S4 已补：**会员权益增改 + 次卡发放**（`user:member:update`；四个端点均 ⚠️ 后端缺口）。权益是**固定三档、只改不增**（凭空多一档没有会员能落进去），且写入时强制**随等级单调变好**——高档折扣不得高于低档、低档已免押则高档不可反悔、升级门槛严格递增，校验不过**整表回滚**不留半写。次卡发放：事由必填 / 黑名单拒发 / 失效必须晚于生效 / 次数卡次数为正整数；发完**同步会员行的「次卡·到期」两列**（取有效期最晚的生效卡），派生只有 `syncCardColumns` 一处，且会员表单已剥掉这两个字段——否则名单会说「有月卡」而次卡记录里一张也没有。发放**不动钱包**：它是赠予不是充值，混进流水会破坏「流水合计 === 余额」 |
| 钱包 | **F3** | ✅ S4 已补：**流水明细抽屉**（`GET /api/user/wallets/{userNo}/txns`，后端早已实现、运营端此前没有入口）。不设「变动后余额」列——后端流水行没这个字段，分页只能看到当页，跨页自算必错。夹具自洽由 `wallet-txn.test.ts` 钉住：本金流水合计 === `balance`、赠额流水合计 === `bonus`，`saveWallet` 手工调余额会补一条调整流水 |
| 充值套餐 | **F3** | — |

### 用户与服务 · 营销管理（8 项）

| 子功能 | 级别 | 缺什么 / 待实施 |
|---|:--:|---|
| 公告管理 | **F3** | — |
| 优惠券 | **F3** | ✅ S2 已补：**发放**（`marketing:coupon:issue`）。`stock` 为发行总量不变，`issued` 只增，剩余=stock−issued，超发在 mock 层拒绝且库存与流水都不动。不可发放的四种情形（已归档/已下线/已过期/剩余为 0）**页面按钮与 mock 校验共用同一个 `couponIssuable()`**，杜绝「按钮亮着点了报错」。人群四个维度全部落在既有主数据（全体/会员等级/消费者分层/指定用户号），规模实算不写死；落 `CouponIssueRecord` 并新增「发放记录」tab。Coupon 原本无到期概念，本批补 `expireAt` |
| 活动 | **F3** | ✅ 已补：**启停**（`marketing:campaign:update`）。状态机 `CAMPAIGN_TRANSITIONS`：`DRAFT/PAUSED →启动→ RUNNING →暂停→ PAUSED`，`RUNNING/PAUSED →结束→ ENDED`（终态）。**合法迁移只声明一次**，页面按钮（`campaignActions()`）与 mock 校验读同一张表，杜绝「按钮亮着点了报错」。四道闸门：① 已结束不可复活（ENDED 不在任何 `from` 里）；② **结束时间已过不可启动**（窗口只卡启动，暂停/结束在窗口外仍可用，否则过期的进行中活动收不了尾）；③ DRAFT 不可暂停；④ 加固 `saveCampaign`——剥离 `status` 并去掉表单状态下拉，否则塞 `status:"RUNNING"` 就能绕过前三道。`Campaign` 原本无 PAUSED 态、窗口挂固定时间轴（会随日历全部过期），本批补 PAUSED 并改挂真实当前时间。另补状态筛选。⚠️ 后端缺口：无启停端点（`MarketingController` 只有 GET + upsert），待补 `POST /api/user/campaigns/{no}/{start\|pause\|end}` |
| 推送触达 | **F3** | ✅ S2 已补：**创建并发送**（`marketing:push:send`）。状态机 `PUSH_TRANSITIONS`：`DRAFT →(定时) SCHEDULED → SENDING → SENT`，SENT 为终态，**「已发送不能重发」由状态机兜住而非 if**。发送落 `sentAt`/`targetCount`/`successCount`。**幂等键必填**（已拍板规则的首个落地）：mock 维护全局已用键集合，同键第二次直接拒——**跨推送号也拒**，防「换个推送号把同一批再发一遍」；且状态校验在占键之前，失败不烧掉键。另加固 `savePushMessage`：剥离 `status`/`sentAt` 等字段，否则表单塞 `status:"SENT"` 就能伪造已发送 |
| 邀请裂变 | **F3** | ✅ 已补**邀请奖励规则配置**（奖多少/奖给谁/什么条件触发/每人上限/生效期/启停），页内分「邀请记录（只读流水）」与「奖励规则（可配置）」两个子视图。校验放 mock 层而非只放表单（表单能绕过，这几条错了都是真金白银）：金额必须 >0（0 元规则等于永不发奖）· 结束必须晚于开始（窗口为空却显示生效）· 每人上限不可负（0=不限）· **同一时间窗只允许一条 ACTIVE**（重叠时「一次邀请该发几笔」无法回答，故在入口堵掉而非等结算才发现重复发奖）。`rewardTo` 用三值枚举而非两个布尔 —— BOTH 是「一次事件发两笔」，与「二选一」在结算上不是一回事 |
| 广告位管理 | **F3** | — |
| 广告活动 | **F3** | ✅ 本轮补**上线/暂停/下线**（`AD_CAMPAIGN_TRANSITIONS` SSOT，按钮由状态机+时间窗派生、mock 层强制）。与营销活动的状态机刻意分开：广告要对广告主结算、暂停即停止计费，合表会让任一侧加状态时互相牵连。窗口已过只拦「上线」不拦「下线」，否则过期的 RUNNING 广告永远收不了尾 | — |
| 投放与曝光 | **F3** | ✅ 已补周期筛选（复用 `REPORT_PERIODS`，`date` 按窗口筛，不另造口径）。**「投放动作」刻意没加在本页**：`AdDelivery` 是按天回传的曝光事实行（曝光/播放/日期），没有生命周期可流转 —— 给事实表加按钮等于给日志加按钮。动作加在**广告活动**上（见该行），这是真正能上下线的实体 |

### 用户与服务 · 客服管理（4 项）

| 子功能 | 级别 | 缺什么 / 待实施 |
|---|:--:|---|
| 报障受理 | **F3** | ✅ 已补：转退款 + 转工单，**两者都幂等**（后端以 ticketNo 为幂等键）。转工单此前是 `alert("…（mock）")` 伪实现，而后端 `POST /cs/tickets/{no}/work-order` 一直存在却从未被调用。已转过的显示既有单号并禁用；无关联订单不可转退款（退款必须挂在真实交易上）。`lib/mock/db/cs.test.ts` 钉住两个出口的幂等性 —— 重复执行的代价是重复退款/重复派人 |
| 客服会话 | **F3** | ✅ 已补：会话详情抽屉（消息时间线 + 回复框）。**回复人取登录态、前端不传 sender** —— 让员工能冒名他人回复是审计上的洞（与后端同一约束）；CLOSED 会话只读 |
| 退款/补偿 | **F3** | 深链 |
| 黑名单处理 | **F3** | 深链 |

### 数据报表（6 项）

| 子功能 | 级别 | 缺什么 / 待实施 |
|---|:--:|---|
| 设备运营分析 | **F3** | ✅ S3 已补：周期选择器（`REPORT_PERIODS`：近7日/近30日/近13周/近12月，types 层单一定义，页面选择器与 mock 桶切分共用）+ 汇总条 + 订单柱 / 在线率·故障率双线图 + 导出（文件名带周期）。**注：原表说"缺导出"不准确——`exportCsv` 早已接在工具条上**，S3 补的是周期与图表，并把订单数列进表与导出 |
| 点位坪效 | **F3** | ✅ S3 已补：同上一套周期 + 营收/成本面积图。整域改成「按日事实 → 周期聚合」：表格按站点聚合、图表按桶聚合、汇总条按全周期聚合，**三者必然相等**（`report.test.ts` 用求和断言兜住）；口径闭合 `净收入 = 营收 − 成本`、`成本 = 分润 + 运维`，与财务报表同源。回本天数算不出（毛利为负）显示「—」不显示 0 |
| 财务报表 | **F3** | ✅ S3 已补：**一行就是图上一个点**（表 = 桶序列），GMV 柱 + 分润/净收入线。跨 tab 对得上：财务 GMV = 坪效总营收 = 自定义报表 Σ GMV |
| 实时大屏 | **F3** | ✅ S3 已补（拍板点 #2）：真全屏看板——8 张 KPI + 今日分时面积图 + 柜机在线构成环图 + 站点排名，固定深色，30s 自动刷新。全屏优先 Fullscreen API，被拒时降级 `fixed inset-0` 覆盖层（静态导出下必须有兜底），ESC 两条路径都能退。KPI **从它下面那条曲线反算**（Σ分时 = 今日 GMV/订单 = Σ排名），设备/工单类 KPI 数真实 mock 主数据（在线柜机与设备台账点得上）；存量口径无历史快照，环比渲染「—」不编 0.0% |
| 自定义报表 | **F3** | ✅ S3 已补：**真的自选指标**——维度（站点/场景/月份）× 8 个指标勾选（`REPORT_METRICS` 单一定义，目录走 `listReportMetrics` 端点，页面只留常量兜底）+ 柱图（第一个指标）+ 宽表 + 宽表导出。列序按目录而非勾选顺序（否则两次导出列不一样）；未勾选指标出提示而不是空表 |
| 消费者分析 | **F3** | ✅ S3 已补：转化漏斗（扫码→授权→支付→借出→复借）+ 三张画像环图（年龄/终端/时段）。漏斗**锚在人群分层表**：「成功借出」= Σ人群用户数、「复借」= Σ(用户数×复借率)，上游环节按转化率反推——同一 tab 的图与表是同一批人；画像同维度 Σ = 总人数（末片取余数） |

### 系统与权限 · 员工与权限（5 项）

| 子功能 | 级别 | 缺什么 / 待实施 |
|---|:--:|---|
| 员工 | **F3** | — |
| 组织架构 | **F3** | 树形已落地（`parent` 改存上级 `deptNo`，上级改下拉；关键词命中保留祖先链）。孤儿/成环由 mock 守卫 + `org-tree.test.ts` 兜住 |
| 角色权限 | **F3** | 功能权限勾选树已落地（`<模块>/<资源>/<动作>` 三层 + 父节点三态 + 搜索），`permCount` 改为「实际已分配码数」派生量。⚠️ 读侧后端缺口：`GET /iam/roles/{roleNo}/permissions` 不存在（只有目录与覆盖写 PUT），接真后端前回显读不到 |
| 操作审计 | **F3** | 详情抽屉已落地（改动前后对比 + requestId/UA；纯动作显式给「无字段改动」而非编造 diff）。⚠️ 后端缺口：无 `GET /audit-logs/{id}`，`AuditEntry` DTO 也无字段级改动 |
| 绩效报表 | **F3** | ✅ 已补周期切换 + 导出（复用报表域 `REPORT_PERIODS`），并修掉真实缺陷：种子 `score` 是 3.5~5.0 而页面 `gradeOf` 阈值是 90/75 —— **评价列整列被判「待改进」**，现改 0~100 的 SLA 达成率口径。<br>✅ **数据已真从工单派生**（2026-08-04 核实，代码见 `lib/mock/db/org.ts#buildStaffPerformances`）：工单种子已补 `handlerName`/`acceptedAt`/`completedAt` 流转留痕、`assigneeName` 改用员工真名（`WO_STAFF` 与 employees 同一批），处理量/平均解决时长/SLA 达成率三指标按周期从工单现算；零单员工渲染「—」而非「待改进」 |

### 系统与权限 · 系统设置（16 项）

| 子功能 | 级别 | 缺什么 / 待实施 |
|---|:--:|---|
| 供应商接入 | **F3** | ✅ S7 已补：**连通性测试**（假探测）。结论/往返延迟/原始信息落只读抽屉，权限用 `device:vendor:read`（排障是运维日常，不该要改配置的权）。结论**由接入方式与状态推演而非随机**——同一供应商每次同答案，否则运维分不清是配置有问题还是探测在抖；停用 → 明说「未发起探测」，HTTP 云对接缺 `apiBase` → 判不通。⚠️ 后端缺口：`VendorController` 无 `POST /internal/gw/vendors/{code}/test` |
| 支付渠道 | **F3** | — |
| 通知模板 | **F3** | ✅ S7 已补：**预览 / 试发抽屉**。顺带接上后端实体早就有、前端一直没接的 `scene`/`content`/`params`——没有正文，「编辑模板」其实只改元数据，预览也无从谈起。变量逐个给输入框（留空用示例值兜底），**缺变量禁试发**且服务端同样拦（带着 `{{}}` 发出去是事故）。试发是真发真计费，故与重发同规格**必须带幂等键**并落一条发送记录。⚠️ 后端缺口：无 `/notify-templates/{no}/preview`、`/test-send`（只有内网 `POST /internal/platform/notify/send`）|
| 发送记录 | **F3** | ✅ S7 已补：**重发**（拍板 #6）。`idempotencyKey` 必填、同键第二次 mock 层直接拒且不落新单；**只许重发失败记录**（成功记录再发＝重复扣费+重复骚扰，要补发走模板试发）；目标命中未到期的触达拉黑一律拒；重发是**新增一条**、原记录一字不改（列表加「重发自」列，审计看得见发了两次）。键在全部校验通过后才登记，校验失败不烧键。新权限码 **`system:notify_log:resend`**。⚠️ 后端缺口：发送记录目前 append-only 只读，无 `/notify-logs/{no}/resend` |
| 触达拉黑 | **F3** | — |
| 业务规则 | **F3** | — |
| 登录设置 | **F3** | — |
| 应用版本 | **F3** | — |
| 参数字典 | **F3** | — |
| 地区库 | **F3** | ✅ S6 已改：**三级树**（国家 → 城市/酋长国 → 商圈），复用 `components/ui/tree.tsx` 树原语。`Region` 新增 `parentId`——`parent` 存的是名称，「Sharjah City」这类名字跨国重复，按名字连边必然连错；`level`/`parent` 由所选上级推出，表单不再手填层级，从根上消灭「level=3 却挂在国家下」这类自相矛盾的行。树**不分页**（一分页第 2 页的商圈就找不到父节点），关键词在前端剪枝且保留祖先；导出仍是扁平 CSV，但导的是整棵树。⚠️ 后端缺口：`SysConfigController` 无 `GET /api/platform/regions/tree` |
| 银行管理 | **F3** | — |
| 问题管理 | **F3** | — |
| 系统参数 | **F3** | — |
| 税率与发票 | **F3** | — |
| 多国家市场 | **F3** | — |
| OpenAPI 应用 | **F3** | ✅ S7 已补：**密钥重置**。`OpenApiApp` 新增 `appSecretMasked`/`secretResetAt`——AppKey 是公开标识，AppSecret 才是密钥。重置走强确认（手输应用号）并写明「旧密钥立即失效、在途调用当场 401」；**新密钥只回掩码**，明文由后端带外交付一次，前端永不承载（口径同 `PaymentChannel.apiKeyMasked`）。⚠️ 后端缺口：无 `/openapi-apps/{no}/reset-secret`，且后端实体尚无 `app_secret` 字段 |

### 代理端门户（6 项）

全部为深链复用运营端页面，级别随被复用页面。✅ 2026-08-04 拍板 #5 已实施：**只改标题不改列**——`lib/nav.ts#portalTitleOverride`（纯函数，nav.test.ts 覆盖）+ `lib/use-portal-title.ts` 薄 hook，`TabHeader` 的 h1 与工作台 `PageTitle` 在命中门户叶时换门户文案（我的看板/我的设备/我的订单/我的收益/我的结算/设备报修）；门户叶没细到 tab 的不接管子 tab 标题（AGENT 看实时监控仍叫「实时监控」）。

---

## 三、汇总

| 级别 | 数量 | 占比 |
|:--:|---:|---:|
| **F3 完整** | **96** | 98% |
| **F2 缺次要项** | **2** | 2% |
| **F1 只读缺写操作** | **0** | 0% |
| **F0 伪实现** | **0** | 0% |

> 2026-08-04 重算（绩效报表工单派生落地 → F3）。历次：F3 43(44%) → 85(87%) → 90(92%) → 95(97%) → **96(98%)**。
> **F1 已清零** —— 「只读缺写操作」这一类不再存在。计数由脚本从 §二 各表级别列统计，不手填。

**剩余 2 项，全部是「刻意不做」而非「还没做」**：

| 项 | 为什么停在 F2 |
|---|---|
| 工单看板 | 拍板点 #1 明确**不做拖拽**（静态导出下成本高、移动端不友好），动作按钮已够。**这是决策终态** |
| 账务分录 | 只读侧已补齐（凭证下钻+借贷平衡+期间）。**手工记账刻意未开** —— 会计操作，需先定「谁能记 / 能否改已过账凭证 / 如何强制平衡与留痕」；做一个不校验平衡的手工记账比不做更危险 |

⚠️ **F3 只代表「前端 + mock 层功能完整」，不代表可上线。** 后端仍有大量缺口，且排查中发现
多条落库路径从未成功运行过（时间格式、NOT NULL 无默认值、13 张表缺 `deleted` 列）。
就绪度由 `NavLeaf.ready` 单独表达，与 F 级别是两个维度 ——
详见 [运营端-未实现功能任务清单](运营端-未实现功能任务清单.md) 与 [TDD-分期屏蔽功能 §1.1](TDD-分期屏蔽功能.md)。

---

## 四、建议实施批次（**待你确认**）

| 批次 | 内容 | 项数 | 理由 |
|:--:|---|---:|---|
| **S1** | **修 F0 + 高危 F1**：订单干预四动作落地 · 设备/点位划拨 · 结算单生成确认 · 押金解冻买断 | 4 | 伪实现和"名字有功能没有"最容易误导使用者 |
| **S2** | **权限码已定义未用的补齐**：异常订单处置 · 优惠券发放 · 推送发送 · 对账差错 · 发票开具 · 信用分调整 | 6 | 权限码即需求，且 RBAC 已就绪 |
| **S3** | **报表域成型**：周期选择器 + 图表（recharts 已在用）+ 导出；大屏改全屏布局；自定义报表加指标选择器 | 6 | 整域 F1，且是对外演示重点 |
| **S4** | **详情/流水类补全**：用户详情 · 钱包流水 · 审计详情 · 客服会话详情 · CRM 跟进时间线 | 5 | 同一模式，可批量 |
| **S5** | **规范对齐 A1**：Cabinet 补 siteNo/agentNo + mock 引用真实站点/代理 | 1 | 影响"机柜归属"展示 |
| **S6** | **树形与结构化**：组织架构树 · 地区库树 · 角色功能权限勾选树 · 差异化定价改站点下拉 | 4 | 形态改造，成本略高 |
| **S7** | 收尾：提现手续费接业务规则 · 巡检手动触发 · 供应商连通性测试 · 模板预览 · OpenAPI 密钥重置 | 5 | 零散小项 |

### ✅ 六个拍板点（用户 2026-07-30 全部按建议确认）

| # | 问题 | **结论** |
|---|---|---|
| 1 | 工单看板**拖拽** | ❌ **不做**。动作按钮已够；静态导出下拖拽成本高、移动端不友好 |
| 2 | 「实时大屏」真全屏看板 | ✅ **做**（S3）。否则名不符实；用现有 recharts + 全屏布局 |
| 3 | 合同**附件上传** | ✅ **做假上传**（存文件名+大小，不真传），保留真实交互 |
| 4 | 组织架构 / 地区库**树形** | ✅ **做**（S6）。三级区域用列表看不出层级 |
| 5 | 代理端**按角色改文案** | ✅ **做**，但**只改标题**（「设备台账」→「我的设备」），不改列 |
| 6 | 「重发」类动作 | ✅ **做**，但**必须带幂等键**且 mock 层拒绝重复提交 —— 否则接后端会**重复扣费**（S7）|

---

## 五、实施进度

| 批次 | 状态 |
|:--:|---|
| S1 修 F0 + 高危 F1（4 项）| ✅ 完成（2026-07-30，371 用例全过） |
| S2 权限码已定义未用（6 项）| ✅ 完成（2026-07-30，442 用例全过） |
| S3 报表域成型（6 项）| ✅ 完成（2026-07-30，6 项全部 F3；报表域 mock 新增 26 条自洽性用例，`report.test.ts` 全过，`tsc --noEmit` 干净）。做法：整域改成「按日事实 `dayFact(站点, 日)` → 周期聚合」，表格/图表/汇总条/大屏 KPI 全部从同一份事实推导，断言的是**跨视图等式**而不是具体数字。周期报表统计到**昨日**（T+1），大屏才是今日到点，避免「今日」在两处是两个数。⚠️ 报表域**后端端点实现数 0**（`scripts/check-backend-parity.py` 核对），新增 4 个端点形状（trend / screen-board / metrics / consumer-insight）连同原有 5 个已在 `lib/api/https/report.ts` 逐条标注 `⚠️ 后端缺口`，`USE_MOCK=0` 时整域 404。另：本域 6 个 nav 叶子仍是 P2/P3 且**没有 `ready: true`**（ready 的定义是前后端已贯通，后端为 0 时标上就是说谎），所以默认 `CURRENT_PHASE=1` 下整个 `/reports` 路由仍被 PhaseGuard 拦住——**验收需 `NEXT_PUBLIC_CURRENT_PHASE=3`** |
| S4 详情/流水类（5 项）| ✅ 完成（2026-07-30，5/5 已交叉核实）：**审计详情 ✅**（2026-07-30，随员工与权限页一并落地）· **用户详情 ✅** · **钱包流水 ✅**（均 2026-07-30，随用户管理页落地；同批把 F1 的**会员权益增改 + 次卡发放**一并做了，该项原不在 S4 清单内）· **客服会话详情 ✅** · **CRM 跟进记录时间线 ✅**（均复用 `components/ui/timeline.tsx`）|
| S5 规范对齐 A1（1 项）| ✅ 完成（2026-07-30）：`agentNo` 随 S1、`siteNo` 本批落地（引用完整性 + 一致性不变量已覆盖）。遗留的是**后端 DDL 缺 `site_no` 列**，不属前端范围 |
| S6 树形与结构化（4 项）| ✅ 完成（2026-07-30，4/4 已交叉核实）：**组织架构树 ✅ · 角色功能权限勾选树 ✅**（2026-07-30，新增 `components/ui/tree.tsx` 树原语，含父节点三态勾选）· **地区库树 ✅**（2026-07-30，复用同一树原语）· **差异化定价改站点下拉 ✅**（存 `siteNo` 真键，mock 层拒绝不存在的站点并由站点反推 scene/名称；代价是场景级定价暂不可表达，见任务 #14）|
| S7 收尾（5 项）| ✅ 完成（2026-07-30，5/5 已交叉核实）：**巡检手动触发 ✅**（见 §二 巡检计划）· **供应商连通性测试 ✅ · 通知模板预览/试发 ✅ · OpenAPI 密钥重置 ✅**（2026-07-30），另附**发送记录重发 ✅**（拍板 #6，幂等键 + mock 层拒重复）· **提现手续费接业务规则 ✅**（费率公式单点定义于 `lib/types/finance.ts`，表格/审核抽屉/CSV/mock 种子四处共用；未审核的按当前费率实时算、已审核的显示存档值 —— 改费率不能改写历史付款；同时删掉了 mock 里凭空发明的「2 AED 下限」，后端 `SysBizRule` 明注财务侧不得另存阈值）。⚠️ 本批 4 个动作的后端端点**全部缺失**，已在 `lib/api/https/system.ts` 逐条标注，`USE_MOCK=0` 时会 404 |

每批完成后回填本表与 §二 的级别列。

> **S1–S7 全部完成（2026-07-30）**。验证：`npx tsc --noEmit` 干净 · `npx vitest run` 678/678（35 文件）·
> `npx next build` 静态导出通过 · `scripts/check-backend-parity.py` 后端覆盖率见该脚本实时输出。
> ⚠️ 全批次均为**前端 + mock 层**完成度；本轮新增的动作里有相当一部分后端端点并不存在，
> 已在各 `lib/api/https/*.ts` 逐条标注 `⚠️ 后端缺口`。**浏览器实机验证未做**（端口被另一会话的
> dev server 占用），即 UI 渲染这一层没有人工过目。
