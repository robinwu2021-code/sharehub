# 设计规范 · 阅读入口

**开发页面前先读这里。** 设计相关文档在多轮迭代中积累了 6 份，其中 4 份已被取代 ——
本文说明哪份是真源、哪些只是历史。

---

## 唯一真源（照这两份实施）

| 文档 | 内容 |
|---|---|
| **[设计规范-ShareHub运营台.md](设计规范-ShareHub运营台.md)** | 三层 token 架构 · 12 档中性色阶 · 四套皮肤 · 语义色 · 排版 · 间距 · 形状 · 层次 · 动效 · 层级 · 密度 · 图标 · 可访问性 · 信息设计 · 文案 |
| **[设计规范-布局与外壳.md](设计规范-布局与外壳.md)** | 区域划分 · 表面与分隔 · 尺寸 · 导航状态 · 滚动归属 · 响应式 · RTL |

## 已被取代（只作决策历史，**不要照着实施**）

这四份每份开头都有 ⛔ 横幅列出与现状冲突的点：

- `设计规范-运营端配色与形状.md` —— "对齐 C 端"时期，含药丸按钮 / Nunito
- `设计方案-配色体系v2-待评审.md` —— 提议改用简电青主色 + 三个 C 端表现手法，均未采用
- `设计方案-运营台独立设计v3.md` —— 定位转向的过渡稿
- `设计方案-白底体系.md` —— `data-ground` 双轨方案，已移除

保留它们的价值在于**记录"为什么不那样做"**：例如为什么按钮不用药丸、
为什么运营台默认不用品牌青绿、为什么白底不能只靠描边。
再次提出类似方案前先看一眼，能省一轮返工。

---

## 落地现状（截至 2026-07-30）

| 层 | 状态 |
|---|---|
| Tier 1/2 token（色阶 / 语义层）| ✅ 已落地 `app/globals.css` |
| 字体（IBM Plex 三族）| ✅ 拉丁/阿语自托管，中文走 CDN |
| 外壳布局（两级色调）| ✅ 一级菜单白 / 二级面板灰 / 内容白 |
| 圆角五档 | ✅ `components/` 已清零；⚠️ `app/` 尚有 37 处待清 |
| 类型阶七档 | 🔄 组件层接入中 |
| 密度两档 | ⚠️ token 已定义，组件消费未全覆盖 |

## 自动化守卫

`lib/design-tokens.test.ts` 会在 `npx vitest run` 时检查：

1. `components/` 不使用废弃圆角类（`rounded-sm/md/lg/xl/2xl/full`）
2. 组件层不写死颜色字面量（hex / rgb / oklch）
3. **豁免清单不许变长** —— 加豁免必须改数字，从而在 review 里显形

新增页面/组件时这些会自动拦住最常见的偏离。

---

## 给后续会话的三条提醒

1. **颜色一律改 token，不在组件里写值。** 改一个 hex 就能让守卫测试变红。
2. **改 `globals.css` 的 token 后必须清 `.next` 重启验证。** Turbopack 会缓存
   类名候选与部分 CSS，出现过三次"源码改了但 served CSS 是旧的"，
   且**肉眼看不出来** —— 用 `curl` 拉 served CSS 逐字确认最可靠。
3. **写类名示例文案要避开完整类名形状。** Tailwind 4 扫描所有源文件的字符串与注释，
   写 `z-[var(--z-*)]` 这种带通配符又像类名的文本会生成非法 CSS、
   **让整站白屏**。用省略号或举真实档位名。
