# 运营核心流程 · 后端功能对齐清单

> **范围**：只看 [运营核心流程.md](./运营核心流程.md) 提出的后端能力（P1–P9 流程、E1–E18 联动、状态机、§十 裁决规则）。
> 其它需求在各自的对话里推进，不在本清单。前端（ops-web）接入不在本清单，见 [实现对齐](./运营核心流程-实现对齐.md)。
>
> **核对日期**：2026-09-25。核对方式：逐条对照代码（类 / 端点 / 迁移）与场景测试，**不以文档声称为准**。
>
> **图例**：✅ 已实现并有测试 · 🟡 部分实现（写明缺什么）· ⬜ 未实现 · 🔗 依赖外部（南向网关 / 支付接入）
>
> **分期**（取自原文 §七）：**S1 第一个站点** · **S2 第一个城市** · **S3 引入代理商** · **S4 多城市规模化**

---

## 〇、结论

| 范围 | 条目 | ✅ | 🟡 | ⬜ |
|---|---:|---:|---:|---:|
| P1 场地拓展（S2） | 7 | 7 | 0 | 0 |
| P2 合同签约（S1，提醒 S2） | 14 | 14 | 0 | 0 |
| P3 站点与点位（S1） | 10 | 9 | 1 | 0 |
| P4 设备布放上线（S1，资产线 S2） | 11 | 11 | 0 | 0 |
| P5 代理商划拨（S3） | 7 | 7 | 0 | 0 |
| P6.1 借还（S1） | 5 | 5 | 0 | 0 |
| P6.2 调度（S2） | 3 | 3 | 0 | 0 |
| P6.3 站点效益（S4） | 3 | 3 | 0 | 0 |
| P7 监控 · 告警 · 工单（S1，升级 / 接管 S2–S3） | 16 | 16 | 0 | 0 |
| P7.3 巡检（S2） | 3 | 3 | 0 | 0 |
| P8 结算输入约束 | 3 | 3 | 0 | 0 |
| P9 撤场（S4） | 8 | 8 | 0 | 0 |
| **流程小计** | **90** | **89** | **1** | **0** |
| 联动 E1–E18 | 18 | 18 | 0 | 0 |

（「✅ / 🔗」按 ✅ 计：本线的部分已做，剩下的只等外部接入。）

（2026-09-25 批次 A–G 全部完成后按 §二 / §三 逐行重算。）

- **S1 基本闭环**：合同 → 建站 → 上线门禁 → 停借保还 → 业务告警 → 工单 → 自动复核验收 已跑通。
  §一 列了 6 个未完成项：2 项等外部接入（退款 / 释放预授权要支付，真实心跳 / 弹仓要南向网关）；
  2 项本线可以直接做（**逾期转买断**、**无责任人时派区域运维**）；1 项要先定归属（**调度器**，7 个定时任务只登记未运行）；
  1 项是设计取舍待确认（「站点必须挂合同」放在上线门禁而不是建站）。
- **S2 缺口集中在「提醒」与「资产线」**：合同到期提醒、商机查重与跟进提醒、商机签约转化（E1）、
  调拨签收回写设备（E15）、缺宝阈值调度、SLA 超时升级。
- **S3/S4** 按原分期往后放。

---

## 一、S1 未完成项（先做这些）

| # | 项 | 状态 | 缺口 | 可否本线做 |
|---|---|---|---|---|
| 1 | 定时任务接入调度器 | 🟡 | 合同生效到期、站点上线对账、告警判定、站点画像、试借还超时、临时文件清理共 7 个 JobHandler **只登记、不运行**；调度器（v4/07）未建 | 调度器属横切，**需确认由哪条线做** |
| 2 | P6.1 弹出失败自动撤单 · 退款 / 释放预授权 | 🔗 | 撤单只把订单金额置 0，不动钱 | 等支付接入 |
| 3 | E9 心跳 / 弹仓指令 | 🔗 | 信号入口 `/internal/events/device` 已建；网关未建，现网没有真实心跳（演示环境开告警判定会全部判离线） | 等南向网关；演示环境需先让模拟器发心跳 |
| 4 | E17 逾期达封顶转买断 | ✅ | 已做：逾期扫描任务 + 结算应用总封顶 + 宝 → SOLD | 已做 |
| 5 | P7.2 无责任人 → 派平台区域运维 | ✅ | 数据范围覆盖站点区域的 OPS 员工中取在手工单最少者（策略 LOAD）；都没有才留待派并通知主管 | 已做 |
| 6 | P3 站点「必须挂合同」 | ✅ | 按裁决 #5 维持在上线门禁与恢复营业校验，原文 §P3 已改写 | 已做 |

---

## 二、逐流程

### P1 场地拓展（S2）

| 要点 | 状态 | 后端落点 / 缺口 |
|---|---|---|
| 录入线索、跟进记录 | ✅ | `LeadServiceImpl` · `LeadFollowServiceImpl#addFollowUp` |
| 阶段按顺序迁移；LOST 必填原因、可重新激活 | ✅ | `LeadStateMachine`（允许一步回退 NEGOTIATING→CONTACTED）；编辑与跟进都校验；LOST 原因必填、重新激活留 `reactivated_at` · `ContractLeadFlowTest` |
| 同一地址 / 场地方 90 天内只一个 BD 跟进（查重） | ✅ | 新建时按场地名 / 地址（忽略大小写与首尾空格）查他人在跟的商机 → 409 并提示负责人；窗口 `lead.dedup.days` |
| 超 N 天无跟进提醒、超 M 天回收线索池 | ✅ | `lead-followup` 任务（每天 09:00）：一段沉默只提醒一次；回收记 `prev_owner`；`POST /leads/{no}/claim` 认领；N / M 走 `lead.follow.remind_days` / `lead.pool.recycle_days` |
| 谈判结果直接成为合同草稿（E1） | ✅ | `POST /leads/{no}/convert`：场地方 + 筹备中站点 + 带谈判条款的合同草稿（`source_lead_no`）一个事务；商机上可存谈判条款 |
| 🔒 签约后写 BD 拓展归因（DEVELOP） | ✅ | `LeadServiceImpl#writeDevelopResponsibility`（条件：代理 BD 且已关联站点） |
| 竞品排他：到期前 60 天重新激活 | ✅ | 商机记 `competitor_name` / `competitor_exclusive_until`；`lead-followup` 到期前 `lead.reactivate.before_days` 天把 LOST 激活为 NEW 并推送负责人（一次丢单只激活一次） |

### P2 合同签约（S1，提醒类 S2）

| 要点 | 状态 | 后端落点 / 缺口 |
|---|---|---|
| 状态机 DRAFT→PENDING→SIGNED→ACTIVE→EXPIRED / TERMINATED；驳回、撤回 | ✅ | `ContractStateMachine` · `ContractServiceImpl` · `ContractApprovalTest` |
| 🔒 只有草稿可改条款 | ✅ | `updateDraft` 非 DRAFT → 409 |
| 审批人 ≠ 提交人；驳回必填意见 | ✅ | `audit` |
| 条件加签财务（比例 / 保底 / 进场费超阈值） | ✅ | 运营通过后超阈值 → 仍 PENDING、`audit_stage=FINANCE`；`POST /contracts/{no}/cosign`（新权限码 `location:contract:cosign`，给 FINANCE）；会签人 ≠ 提交人 ≠ 运营审批人；阈值走 `contract.cosign.*` |
| 签署：上传扫描件 + 签署日期；生效日已到即生效 | ✅ | `sign` · 文件服务 `POST /api/platform/files` |
| 🔒 同站点同时刻只一份 ACTIVE；期限不重叠 | ✅ | 提交校验重叠；新合同生效时旧 ACTIVE 自动到期 |
| 续签 = 新合同 | ✅ | `renew`（`prev_contract_no` 链） |
| 🔒 改分成只影响生效后订单 | ✅ | 既有：订单落价格 / 归属快照（ADR-027） |
| 补充协议版本链 | ✅ | `POST /contracts/{no}/supplement` 生成关联草稿（`contract_kind=SUPPLEMENT` + `parent_contract_no`），走同样审批签署；生效日原合同 EXPIRED（「被补充协议取代」），不发签约事件（避免重复牵线费） |
| 提前终止走审批 | ✅ | `terminate` 改为发起申请（合同照常生效）→ `POST /contracts/{no}/termination/audit` 他人审批（驳回必填原因、可再申请）→ 终止日已到即终止，否则 `contract-lifecycle` 到日终止 |
| 定时：SIGNED→ACTIVE、ACTIVE→EXPIRED | ✅ | `ContractLifecycleJob`（`contract-lifecycle`）—— 需调度器接通（§一 #1） |
| 到期前 60 / 30 / 7 天提醒（E3） | ✅ | `ContractExpiryEvaluator` 启用 `CONTRACT_EXPIRING`：60 天开（LOW，BD 待办 + BD / 运营推送），30 / 7 天升 MEDIUM / HIGH 各推一次；续签已提交 / 终止在途 / 站点撤场时恢复 |
| 生效 → 牵线人介绍费（E2） | ✅ | 生效时发 `ContractSignedEvent`，仅一次 · `ReferFeeOnContractSignedTest` |
| 到期未续 → 撤场评估（E4） | ✅ | 告警 `SITE_WITHOUT_CONTRACT` → BD 待办 · `BusinessAlarmTest` |

### P3 站点与点位（S1）

| 要点 | 状态 | 后端落点 / 缺口 |
|---|---|---|
| 站点五态，状态只经动作改 | ✅ | `SiteStateMachine` · `SiteServiceImpl` · `SiteController` · `SiteStatusMachineTest` |
| 🔒 营业时间必填 | ✅ | 建档 / 编辑校验 |
| 🔒 上线必须有生效合同（原文已改写，裁决 #5） | ✅ | 上线门禁 CONTRACT 项 + 恢复营业校验 |
| 现场勘测（信号 / 电源 / 照片），勘测不过不装 | ✅ | `POST /sites/{no}/surveys`（追加，以最近一次为准；无信号 / 不能接电不能判通过）；开业清单 SURVEY 项；首台上线门禁 SURVEY 项（站点已营业免勘测） · `SiteAssetFlowTest` |
| 建点位；点位继承站点归属 | ✅ | 既有 `LocService#savePoint`；点位状态按 `LocationStatus` 校验（C2） |
| 🔒 必须有运维责任人 | 🟡 | 开业清单检查 + 站点摘要「缺责任人」数；**建站不强制**（代理责任人经伙伴页维护，建站时可能还没有） |
| 暂停必填原因与预计恢复日；恢复需有生效合同 | ✅ | `pause` / `resume` |
| 首台设备上线 → 营业（E6） | ✅ | `SiteGoLiveListener` + `site-golive-reconcile` 兜底 |
| 门店生命周期改只读漏斗 | ✅ | `GET /api/ops/site-lifecycles`、`/funnel`；旧「改阶段」端点留 409 桩 |
| 只有 CLOSED 可归档 | ✅ | `archive` |

### P4 设备布放上线（S1，资产线 S2）

| 要点 | 状态 | 后端落点 / 缺口 |
|---|---|---|
| 🔒 SN 唯一 | ✅ | 既有唯一键 `uk_cabinet_sn` / `uk_powerbank_sn` |
| 入库质检，质检不过不入可用库存 | ✅ | `QcService`：机柜看开机 / 仓位，宝看电量 / 循环（阈值走参数）；新建柜 PENDING，未过不能进调拨明细、不能上线（门禁 QC 项）；存量设备免检 |
| 🔒 调拨：发货 → 在途 → 签收逐件核对，差异挂异常（E15） | ✅ | `TransferOpsService`（`/inventory-transfers/{no}/items · /ship · /receive`）：发货柜子 → 运输中；签收逐件扫码，缺件 / 多件落 `inv_asset_diff`，单子照常完成；收到的柜子回在库并回写所在仓。⚠️ 旧的 `save(status=…)` 路径不联动机柜（另一会话在途改动中，未碰），见 §七批次 C 备注 |
| 机柜「运输中」状态 | ✅ | `CabinetStatus.IN_TRANSIT` + SHIP / RECEIVE 两条边（调拨驱动）；运营端词表已同步 |
| 🔒 划拨留痕 | ✅ | 既有 `AgentAssignmentServiceImpl` |
| 装机由工单驱动 | ✅ | 门禁 INSTALL_WO 项；装机单完工（可带扫码点位，不同则改绑）→ 门禁全过自动上线（`InstallCompletedListener`） |
| 🔒 未收到心跳不能上线 | ✅ | 上线门禁 ONLINE 项（心跳 ≤3 分钟）🔗 真实心跳依赖网关 |
| 装宝 60–70%，留空仓 | ✅ | 门禁 LOAD 项：在柜宝占仓位 50%–80%（`device.load.*`，有宝台账按台账、否则按上报可借数） |
| 🔒 试借还 | ✅ | `TrialRentServiceImpl`（指令异步 + 信号回推） |
| 🔒 上线门禁：合同 + 在线 + 试借还 + 计费命中（另含点位、站点开放） | ✅ | `CabinetLifecycleServiceImpl#goLiveGate` · `DeviceLifecycleTest`；批次 C 加质检 / 勘测 / 装机工单 / 装宝比例共 10 项 |
| 机柜状态机（布放 / 故障 / 维修 / 撤机 / 退役），编辑不改状态 | ✅ | `CabinetStateMachine` · `DeviceOpsController` |

### P5 代理商入驻与划拨（S3）

入驻段见 [必要功能清单](./必要功能清单-从入驻到到账.md) §二，不在本清单。

| 要点 | 状态 | 后端落点 / 缺口 |
|---|---|---|
| 🔒 划拨级联：站点 → 点位 → 设备（E7） | ✅ | 既有 `AgentOwnershipSync#apply` |
| 暂停：不能新划拨 | ✅ | 既有 `AgentAssignmentServiceImpl#assign` |
| 暂停：不再给它派新工单 | ✅ | 本轮：自动派单跳过暂停代理，落到员工责任人 |
| 暂停：分润照常计提、**冻结提现**（E8） | ✅ | 停用期间提现申请 / 审批通过 / 确认打款一律 409，已提交的停在原状态（挂起），驳回照常；清退「结清中」例外放开 · `AgentOpsTest` |
| 暂停：名下待处理工单改派平台（E8） | ✅ | 代理状态变更事件 → 名下未完结工单改派站点员工责任人 / 区域运维（REASSIGN 留痕 + 通知），找不到人退回待派池 |
| 清退：收回资产 → 结清分润 → 停账号（顺序） | ✅ | 清退单 `AgentExitStateMachine`：发起即停用 → 收回资产（站点 / 责任 / 机柜 / 工单）→ 结清（分润出账 / 结算付清 / 提现办结）→ 关闭（停账号、归档）；每步门禁。系统无代理保证金，这一步未设门禁 |
| 考核：在线率 / SLA / 单柜收入 / 客诉率 | ✅ | `agent-ops-assessment` 月度考核：SLA 达成率（被接管算未达成）、在线率、客诉、被接管数 → 次月运维分成系数（分档走参数），OPERATE 分润按系数打折；不达标开「代理运维不达标」BD 待办 |

### P6.1 借还（S1）

| 要点 | 状态 | 后端落点 / 缺口 |
|---|---|---|
| 🔒 站点暂停 / 设备离线 / 故障不可借，仍可还（E5） | ✅ | `RentabilityGuard`（借出校验，归还不校验） |
| 🔒 弹出失败自动撤单 | ✅ / 🔗 | 告警 `RENT_NOT_DELIVERED` → 自愈撤单 → 失败 3 次转客服单；退款 🔗 支付 |
| 🔒 还宝未识别有补还通道 | ✅ | 自动：`RETURN_NOT_RECOGNIZED` 按宝入柜时刻结单；人工：既有干预 `force_return` |
| 计费方案恰好命中一个 | ✅ | 既有 `PriceResolver`；上线门禁也校验 |
| 逾期达封顶转买断，宝 → 已售（E17） | ✅ | `order-overdue-buyout` 任务 · `RentOrderServiceImpl#buyoutIfCapped`；结算时总封顶（买断价）此前传 null 从不生效，一并修 · `OrderAssetLinkTest` |

### P6.2 调度（S2）

| 要点 | 状态 | 后端落点 / 缺口 |
|---|---|---|
| 空柜 / 满柜 → 补宝 / 取宝工单 | ✅ | `DispatchEvaluator` 启用「可借不足 / 可还位不足」：逐柜可借 1..N 或可借率 < 20%、空仓 1..N（`dispatch.*` 参数）；为 0 的仍归「借不到 / 还不了」 · `DispatchInspectionTest` |
| 低电宝堆积、宝老化标记待报废 | ✅ | 「低电宝堆积」（在柜低电占比 ≥ 50% → 查仓位充电维修单）；`powerbank-aging` 每日把循环超限的宝标 AGED、不再借出，「宝老化」告警开换宝回收（并入调度单） |
| 调度按区域 / 路线合并派单 | ✅ | REFILL 类工单一律按「区域 × 当天」合并成一张调度单，挂靠的多站点告警即明细；「借不到（无宝）」与「可借不足」因此落同一张单，不重复派人 |

### P6.3 站点效益（S4）

| 要点 | 状态 | 后端落点 / 缺口 |
|---|---|---|
| 每日：在线率、订单数、异常 | ✅ | 既有 `GET /api/ops/sites/{no}/stats` |
| 每月：单柜日均收入、周转率 | ✅ | 既有 `site-analysis` |
| 低效预警、迁机 / 撤场评估（E18） | ✅ | `SiteYieldEvaluator` 启用「低效站点」：上线满 N 月（默认 2）的营业站点，连续 N 个整月单柜日均 < 阈值（默认 5 AED）→ BD 待办 · `YieldSettleMetricsTest` |

### P7 监控 · 告警 · 工单（S1，升级 / 接管 S2–S3）

| 要点 | 状态 | 后端落点 / 缺口 |
|---|---|---|
| 业务告警：借不到 / 还不了（整站、单柜）、交易异常、电池、过热、无合同营业 | ✅ | `AlarmEngineImpl` + 4 个判定器 · `BusinessAlarmTest` |
| 防误报：营业时间内累计、持续时长、去重、整站收敛 | ✅ | 同上；同对象同码最多一条未关闭（库唯一约束） |
| 仓位故障自动禁用；电池异常立即锁仓 | ✅ | 设备信号 → 保护动作（`ProtectionServiceImpl`，引用计数） |
| 离线 → 设备不可借（E9） | ✅ | 离线告警以告警身份整柜停借，恢复即释放 |
| 频繁掉线、弹出失败率、资产类告警 | ✅ | 信号落 `dev_signal_log`、心跳间隔 > 3 分钟后恢复合成 LINK_RESTORED；`SignalCountEvaluator`：弹出失败率高（60 分钟 ≥3，柜子被告警挂停借 + 维修单）、频繁掉线（24h >5）；`PowerbankMissingEvaluator`：宝失联（借出中、无订单、无在途调拨 >24h → 运维待办）。「宝在非本区域出现」系统无宝的归属区域概念，未做 · `AlarmWoEnhanceTest` |
| 通知：按码、接收人、静默窗口、升级 | ✅ | `AlarmNotifier` + V103 默认规则；通道仅 PUSH（短信 / 邮件待接入） |
| 告警 → 自动开单（E11），按运维责任人派单 | ✅ | 按「码 × 根因」路由；代理优先，暂停落员工 |
| 恢复 → 自动关闭；未接单的工单自动撤销（E10） | ✅ | 防抖后 SELF_HEALED；CREATED / DISPATCHED 工单撤单并通知 |
| 🔒 完工必填处理结果、故障原因、现场照片 | ✅ | `WoOpsServiceImpl#complete` |
| 完工自动复核，通过即验收、不过保持待验收（E12） | ✅ | `WorkOrderReviewListener` · `recordReview` |
| SLA 按类型 × 优先级 | ✅ | `activeRule(type, priority)`，V99 默认值 |
| 超 SLA 升级通知（E13） | ✅ | `wo-sla-breach-scan` 顺带升级：响应超时 → 规则 escalate_to（默认运维，按站点区域找人），解决超时 → resolve_escalate_to（默认管理员）；每档只通知一次；超时一天以上的积压静默占位 |
| 代理的单超时由平台接管 | ✅ | `POST /work-orders/{no}/takeover`：SLA 已超时才可接管，改派平台员工，记 taken_over_from 与 TAKEOVER 留痕；按裁决 #1 不逐单扣，计入月度考核 |
| 抢单模式 | ✅ | `GET /work-orders/pool`（未派单、按数据范围 = 本区域）· `POST /work-orders/{no}/grab`（派给自己并接单，GRAB，并发只一人成功） |
| 工单成本记到站点 / 代理 | ✅ | 完工填配件 / 人工金额，累计到工单；代理运维的单记代理，其余记站点；`GET /work-orders/costs` 按承担方汇总 |
| 工单详情：时间线 / 照片 / 关联告警；列表筛选 | ✅ | `GET /api/ops/work-orders/{woNo}` · 列表 6 项筛选 |

### P7.3 巡检（S2）

| 要点 | 状态 | 后端落点 / 缺口 |
|---|---|---|
| 巡检计划到期自动生成工单（E14） | ✅ | 既有 `InspectionPlanRunJob`（每日 06:00）—— 需调度器接通 |
| 巡检发现问题直接派生维修单（保留来源链） | ✅ | `POST /work-orders/{no}/derive`：来源 INSPECTION，来源号 = 巡检单号:类型:机柜（同类幂等）；运营端来源词表已同步 |
| 清点宝数与系统读数比对（兼资产盘点） | ✅ | 巡检完工可填清点数，与在柜数不符落资产差异（来源 INSPECTION），柜子不动 |

### P8 结算分润（输入约束）

结算本身见 [必要功能清单](./必要功能清单-从入驻到到账.md) §三，本清单只列原文对结算的三条输入约束。

| 要点 | 状态 | 后端落点 / 缺口 |
|---|---|---|
| 订单完成 → 分润（E16）；🔒 按成单快照，不追溯 | ✅ | 既有 `OrderSettledEvent` → 分润生成 |
| 🔒 保底 + 分成：结算单层面补齐保底差额 | ✅ | 出账前按账期算「保底（按生效天数折算）− 该合同带来的场地方分成」，差额为正生成已确认的补差调整项并入结算单（幂等）；非按月结算的合同跳过并告警 |
| 撤场时进场费 / 押金退还走结算调整项 | ✅ | 站点关闭事件 → `stl_adjustment`（押金全额、进场费按剩余天数折算，记负数为建议值）→ 财务确认（改金额须说明）→ 下次出账并入结算单（明细 `ADJUST`） |

### P9 撤场与资产回收（S4）

| 要点 | 状态 | 后端落点 / 缺口 |
|---|---|---|
| 撤场决策审批 | ✅ | 按裁决 #4 撤场直接发起（原因 + 计划日 ≥ 7 天后），靠关闭门禁兜底；合同终止才走审批（B2） |
| 🔒 撤场先停借不停还 | ✅ | 撤场中站点借出校验拒绝，归还放行 |
| 撤机工单（每台一张） | ✅ | 本轮：`WorkOrderCommandPort#openRemoval` |
| 🔒 关闭前：无设备、无进行中工单、无在借订单 | ✅ | `closeGate`（关闭动作复核） |
| 条件满足自动关闭 | ✅ | `site-golive-reconcile` 顺带把关闭门禁全过的撤场站点自动关闭（C7） |
| 🔒 回收的宝逐颗清点，与系统在柜数比对 | ✅ | 撤机单完工必填清点数；与在柜数不符落 `COUNT_MISMATCH` 资产差异（`RemovalCompletedListener`） |
| 回仓调拨签收 | ✅ | 撤机完工：柜子撤机回在库 + 生成「站点 → 仓库」回仓调拨草稿（按工单号幂等），签收走逐件核对 |
| 在借订单宽限期 | ✅ | 按裁决 #3：计划撤场日至少提前 7 天（期间停借保还，C8）；拆机时仍未还的订单照常计费至封顶转买断（A1） |

---

## 三、联动 E1–E18

| # | 联动 | 状态 | 落点 / 缺口 | 分期 |
|---|---|---|---|---|
| E1 | 商机签约 → 场地方 + 合同草稿 + 归因 | ✅ | `LeadOpsService#convert` | S2 |
| E2 | 合同生效 → 可上线 + 介绍费 | ✅ | 上线门禁 CONTRACT 项；生效时发一次介绍费事件 | S1 |
| E3 | 合同到期前 60/30/7 天提醒 | ✅ | `CONTRACT_EXPIRING` 告警 + 升级推送 | S2 |
| E4 | 到期 / 终止 → 撤场评估 | ✅ | `SITE_WITHOUT_CONTRACT` + BD 待办 | S1 |
| E5 | 站点暂停 → 停借保还 | ✅ | `RentabilityGuard` | S1 |
| E6 | 首台上线 → 营业 | ✅ | 事件 + 对账兜底 | S1 |
| E7 | 划拨级联 | ✅ | 既有 | S3 |
| E8 | 代理暂停 → 冻结提现 / 工单改派 | ✅ | 提现服务层拦截 + 状态变更事件改派 | S3 |
| E9 | 心跳中断 → 告警 + 不可借 | ✅ 🔗 | 引擎已做；真实心跳依赖网关 | S1 |
| E10 | 心跳恢复 → 关告警 + 撤未开始工单 | ✅ | | S1 |
| E11 | 告警 → 自动开单、按责任人派 | ✅ | | S1 |
| E12 | 完工 → 复核 → 验收 / 返工 | ✅ | | S1 |
| E13 | 超 SLA → 升级 / 接管 | ✅ | 升级通知（E2）+ 平台接管（F4） | S2 / S3 |
| E14 | 巡检到期 → 生成工单 | ✅ | 既有任务，需调度器接通 | S2 |
| E15 | 调拨签收 → 设备回写、差异挂异常 | ✅ | `TransferOpsService#receive` | S2 |
| E16 | 订单完成 → 分润 | ✅ | 既有 | S1 |
| E17 | 逾期封顶 → 买断、宝 → 已售 | ✅ | `order-overdue-buyout` + 宝状态联动 | S1 |
| E18 | 连续低效 → 预警 | ✅ | `SITE_LOW_YIELD` | S4 |

---

## 四、状态机（原文 §五）

| 对象 | 状态 | 说明 |
|---|---|---|
| 商机 | ✅ | `LeadStateMachine`：顺序推进 + 一步回退 + LOST（原因必填）+ 重新激活；运营端下拉尚无迁移表（登记在 known-missing-ui-transitions） |
| 场地方 | ✅ | 合作中 / 归档 |
| 合同 | ✅ | 六态 + 驳回 / 撤回 + 财务会签环节 + 终止申请（申请不是状态，审批期间仍 ACTIVE） |
| 站点 | ✅ | 五态 |
| 点位 | ✅ | 可用 / 停用 / 归档（取值已校验） |
| 机柜 | ✅ | 含「运输中」（调拨驱动）；在线 / 离线为独立维度 |
| 充电宝 | ✅ | 借出挑宝 → RENTED、归还 / 仓位识别 → IN_CABINET、买断 → SOLD（`PowerbankTracker`）；柜子无宝台账时按旧行为放行 |
| 调拨单 | ✅ | 既有（签收核对无入口，见 P4） |
| 代理商 | ✅ | ENABLED ⇄ SUSPENDED；清退走 `AgentExitStateMachine`（F3） |
| 告警 | ✅ | CLOSED 必记恢复方式（五种） |
| 工单 | ✅ | 另加撤单 / 并单两条系统边 |

## 五、运营指标（原文 §六，S2–S4）

| 指标 | 状态 |
|---|---|
| 设备在线率、单柜日均收入、周转率 | ✅ 既有 |
| 告警误报率、自愈率、撤单率 | ✅ `GET /api/ops/alarms/codes/stats` |
| 工单 SLA 达成率 | ✅ `GET /api/ops/ops-flow-metrics`（`OpsFlowMetricsService`，口径见类注释） |
| MTTR（平均修复时长） | ✅ 从故障单开单到最后一次处理记录 |
| 合同续约率、到期未续占比 | ✅ |
| 装机时效（签约 → 上线天数）、首次试借还通过率 | ✅ |
| 线索转化率、平均签约周期 | ✅ |
| 撤场回收率 | ✅ 撤机清点数 ÷ 系统应在柜数 |

---

## 六、裁决（2026-09-25 按建议定）

| # | 问题 | 定论 | 落到执行清单 |
|---|---|---|---|
| 1 | 代理工单超时被平台接管，扣不扣分润 | 不逐单扣；按月度 SLA 达成率调**运维分成系数**，写进代理协议 | F4 · F5 |
| 2 | 场地方要不要对账门户 | 首版不做；结算单导出对账单（PDF）线下发送 | G4 |
| 3 | 撤场在借订单宽限期 | 计划撤场日至少提前 7 天发起（这 7 天停借保还）；拆机当天仍未还的订单照常计费至封顶转买断 | C8 · A1 |
| 4 | 撤场 / 合同提前终止要不要审批 | 合同终止**走审批**（影响分成）；撤场保持直接发起，靠关闭门禁兜底 | B2 |
| 5 | 「站点必须挂合同」放在哪一步 | 上线门禁 + 恢复营业校验（维持现状）；原文 §P3 改写为「上线必须有生效合同」 | A4 |
| 6 | 调度器由哪条线接 | 属横切，另起一条线；本线的任务都按 JobHandler 写好，接上即运行 | 不在本清单 |

## 七、执行清单

**不在本清单**（另线推进，本线只留接口）：支付退款 / 释放预授权（支付接入）· 真实心跳与弹仓（南向网关）· 调度器（横切）· 运营端页面（TDD/08）。

**每项的完成标准**：库表（如需）→ 实体 / Mapper → Service → Controller / JobHandler → 场景测试 → 架构棘轮与 `check:drift` 全绿 → 回填本清单状态。
批次内按编号顺序做；批次之间 A → G，后一批依赖前一批的地方已注明。

### 批次 A · 第一个站点收尾（S1）✅ 2026-09-25 完成

| # | 事项 | 对应 | 要点 |
|---|---|---|---|
| A0 ✅ | 报错文案国际化收口（前置） | 规范 | 本线新增的 94 处中文报错收成 57 个 i18n key（中 / 英 / 阿），`ChineseErrorLiteralRatchetTest` 台账 294 → 283 |
| A1 ✅ | 逾期达封顶转买断 | E17 · P6.1 | 新任务 `order-overdue-buyout`：进行中订单累计费用达封顶 → 买断结单；充电宝 → SOLD（驱动宝状态机）；通知用户 |
| A2 ✅ | 借还驱动充电宝状态 | 状态机 · P6.1 | 弹出回执带宝号 → RENTED 并回填订单真实宝号；仓位识别到宝 → IN_CABINET（柜 / 仓）；买断 → SOLD |
| A3 ✅ | 无责任人派区域运维 | P7.2 | 站点区域内、数据范围覆盖该区域的 OPS 员工中取在手工单最少者；都没有才留待派并通知主管 |
| A4 ✅ | 原文 §P3 改写 | 裁决 #5 | 「建站点必须挂合同」→「上线必须有生效合同」 |

### 批次 B · 合同与商机（S2）✅ 2026-09-25 完成

| # | 事项 | 对应 | 要点 |
|---|---|---|---|
| B1 ✅ | 合同到期提醒 60 / 30 / 7 天 | E3 | 每日任务；每档对 BD 与运营各提醒一次（待办 + 推送，幂等）；启用告警码 `CONTRACT_EXPIRING` |
| B2 ✅ | 合同提前终止走审批 | 裁决 #4 | 终止申请（原因、终止日）→ 他人审批 → 到终止日置 TERMINATED；驳回留痕；审批中不影响营业 |
| B3 ✅ | 条件加签财务 | P2 | 分成比例 / 保底 / 进场费超阈值（系统参数）→ 运营审批后还需财务审批 |
| B4 ✅ | 补充协议 | P2 | 生效中合同改条款 = 新建补充协议（关联原合同），生效日起取代原条款；原版保留、历史订单不追溯 |
| B5 ✅ | 商机阶段顺序 · LOST 必填原因 · 重新激活 | P1 · 状态机 | 迁移表：NEW→CONTACTED→NEGOTIATING→SIGNED，任意→LOST（原因必填），LOST→NEW（重激活） |
| B6 ✅ | 商机查重 | P1 | 同一地址 / 场地方 90 天内已有他人跟进 → 拒绝并提示当前负责 BD |
| B7 ✅ | 跟进提醒与回收线索池 | P1 | 每日任务：超 N 天无跟进提醒负责人；超 M 天回收到公共池（N / M 系统参数） |
| B8 ✅ | 商机签约转化 | E1 | 签约 → 生成 / 关联场地方档案 + 带谈判条款的合同草稿 + BD 归因；不重复录入 |
| B9 ✅ | 竞品排他记录与重新激活 | P1 | 记录竞品独家到期日；到期前 60 天把 LOST 商机重新激活并提醒 |

### 批次 C · 站点与设备资产线（S2，C8–C9 为 S4）✅ 2026-09-25 完成

| # | 事项 | 对应 | 要点 |
|---|---|---|---|
| C1 ✅ | 现场勘测 | P3 | 勘测记录（信号、电源、可摆位置、照片、结论）；上线门禁加「勘测通过」项 |
| C2 ✅ | 点位状态取值校验 | P3 | ACTIVE / PAUSED 之外拒绝 |
| C3 ✅ | 入库质检 | P4 | 质检记录（开机、仓位、宝电量与循环）；未通过不进可用库存、不能调拨 / 上线 |
| C4 ✅ | 调拨逐件签收与差异，回写设备，机柜「运输中」 | E15 · P4 | 发货 → IN_TRANSIT；签收逐件核对，缺件 / 多件挂差异；签收完成机柜回 IN_STOCK 并更新所在仓 |
| C5 ✅ | 装机工单驱动布放 | P4 | 装机工单完工（扫码绑定点位 + 照片）→ 自动带出上线门禁；上线门禁加「装机工单完工」项 |
| C6 ✅ | 装宝比例 | P4 | 上线门禁加「在柜宝占仓位 50%–80%」项（满装没地方还、太少借不到） |
| C7 ✅ | 撤场满足条件自动关闭 | P9 | 撤场中站点门禁全通过 → 定时任务自动关闭 |
| C8 ✅ | 撤场：提前期、逐颗清点、回仓 | P9 · 裁决 #3 | 计划撤场日 ≥ 发起日 + 7 天；撤机工单完工须填清点宝数，与系统在柜数比对，不一致挂资产差异；撤下设备生成回仓调拨 |
| C9 ✅ | 押金 / 进场费退还 | P9 · P8 | 撤场关闭 → 生成结算调整项（退押金、结清进场费） |

> **批次 C 备注**：调拨的发货 / 签收新开在 `AssetOpsController`（`/inventory-transfers/{no}/items · ship · receive`）。旧的 `POST /inventory-transfers/{no}`（body 带目标 status）仍能发货 / 收货但**不联动机柜状态与所在仓** —— 该文件当时有另一会话的在途改动（请求体 DTO 化），按约定未碰。待其提交后应让旧路径的 SHIP / RECEIVE 改调 `TransferOpsService`，或拒绝经它改状态。

### 批次 D · 调度与巡检（S2）✅ 2026-09-25 完成

| # | 事项 | 对应 | 要点 |
|---|---|---|---|
| D1 ✅ | 缺宝 / 满柜阈值调度 | P6.2 | 可借 ≤ 1 或可借率 < 20% → 补宝单；空仓 ≤ 1 → 取宝单（阈值可配）；与「整站借不到」告警合并不重复开单 |
| D2 ✅ | 低电宝堆积、宝老化 | P6.2 | 低电宝占比超阈值 → 查仓位充电工单；循环次数超阈值 → 标记待报废 + 回收单 |
| D3 ✅ | 调度按区域 / 路线合并 | P6.2 | 同区域同日的补宝 / 取宝合并成一张调度单（多站点明细） |
| D4 ✅ | 巡检派生维修单 | P7.3 | 巡检单上直接开维修单，来源记巡检单号（来源链可追溯） |
| D5 ✅ | 巡检清点比对 | P7.3 | 巡检填清点宝数，与系统读数比对，不一致挂资产差异（兼盘点） |

### 批次 E · 告警与工单增强（S2）✅ 2026-09-25 完成

| # | 事项 | 对应 | 要点 |
|---|---|---|---|
| E1 ✅ | 窗口计数型判定 + 启用 L1 告警码 | P7.1 | 新增 COUNT 判定器；启用：频繁掉线（24h > 5 次）、弹出失败率高（1h ≥ 3 次，柜暂停借出）、宝长期未归还 / 异地出现 |
| E2 ✅ | 工单超 SLA 升级通知 | E13 | 响应超时 → 通知组长；解决超时 → 通知区域经理（按工单优先级档，幂等） |
| E3 ✅ | 抢单模式 | P7.2 | 未派单工单进入区域抢单池，接单即派给自己 |
| E4 ✅ | 短信 / 邮件通知通道 | P7 | 告警规则的 SMS / EMAIL 按员工联系方式发送（明文只在 PII 库，日志脱敏） |

### 批次 F · 代理商（S3）✅ 2026-09-25 完成

| # | 事项 | 对应 | 要点 |
|---|---|---|---|
| F1 ✅ | 代理暂停冻结提现 | E8 | 暂停期间提现申请拒绝、已提交未审批的挂起；分润照常计提 |
| F2 ✅ | 代理暂停改派名下工单 | E8 | 名下未完结工单改派平台（按 A3 规则），留痕并通知 |
| F3 ✅ | 代理清退流程 | P5 | 收回资产 → 结清分润 → 退保证金 → 停账号，按顺序门禁 |
| F4 ✅ | 平台接管超时代理工单 | P7.2 · 裁决 #1 | 代理的单解决超时 → 平台可接管（改派平台运维），记接管留痕 |
| F5 ✅ | 代理运维考核 | P5 · 裁决 #1 | 月度 SLA 达成率、在线率、客诉率；低于阈值开告警 `AGENT_SLA_BELOW`；**运维分成系数**按达成率落到分润规则 |

### 批次 G · 效益、结算与指标（S4）✅ 2026-09-25 完成

| # | 事项 | 对应 | 要点 |
|---|---|---|---|
| G1 ✅ | 低效站点预警 | E18 · P6.3 | 连续 N 月单柜日均收入低于线 → 启用 `SITE_LOW_YIELD`，给 BD / 运营待办（迁机或撤场评估） |
| G2 ✅ | 保底差额补齐 | P8 | 保底 + 分成合同：结算单层面按周期补齐保底差额（调整项） |
| G3 ✅ | 场地方对账单导出 | 裁决 #2 | 按场地方 × 周期导出 PDF：订单汇总、分成计算、保底补差、调整项 |
| G4 ✅ | 工单成本 | P7.2 | 完工记配件与人工金额，归到站点（效益）或代理（代理运维时） |
| G5 ✅ | 运营指标 | 原文 §六 | 工单 SLA 达成率、MTTR（从工单算）、合同续约率 / 到期未续占比、装机时效、首次试借还通过率、线索转化率 / 签约周期、撤场回收率 |

> **G3 备注**：对账单为结构化数据（`GET /api/trade/settlements/{no}/statement`）+ 可打印页（`…/statement.html?lang=zh|en|ar`，浏览器「打印 → 存为 PDF」）。直接出 PDF 需要引入 PDF 库并嵌中文 / 阿文字体 —— 本机仓库无任何 PDF 库、构建离线，依赖与字体授权待定。

**合计 41 项**：A 4 · B 9 · C 9 · D 5 · E 4 · F 5 · G 5。
