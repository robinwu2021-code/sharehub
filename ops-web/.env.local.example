# 后端 API「源」。端点路径已自带 /api、/internal 前缀（lib/api/http.ts），故此处填「源」不填 /api：
#  - 同源 nginx 反代（生产）：留空 → 同源请求 /api/** 、/internal/**
#  - 跨源本地开发：指向后端源，如 http://localhost:8080
NEXT_PUBLIC_API_BASE=
# 用 mock 数据运行（无后端时置 1；接后端置 0）
NEXT_PUBLIC_USE_MOCK=1
# 产品交付分级（0=L0 最小闭环 / 1=L1 首批站点运营 / 2=L2 规模化 / 3=L3 增长与生态）。默认 0。
# 高于此值的功能在导航灰显（L1/L2/L3 徽章）、URL 直达由 PhaseGuard 兜底。改此值需重新 build。
# 依据 docs/requirements/功能清单-分级矩阵.md（2026-09-23 起取代 PRD-分期路线图 的三期划分）。
# 注意：分级只管「该不该开放」；某个叶子补完前后端后标 nav.ts 的 ready: true 即可单独解锁。
NEXT_PUBLIC_CURRENT_PHASE=0
# 子路径部署（可选）
# NEXT_PUBLIC_BASE_PATH=/powerbank/ops-web

# Google Maps JS API Key（设备地图分布用；不配则地图降级为占位提示，不影响其它功能）
# 与 c-app 共用同一个 GCP 项目的 key（c-app 用 VITE_GMAPS_KEY）。
# ⚠️ Key 需在 GCP 控制台限制 HTTP referrers（生产域名 + localhost），并启用 Maps JavaScript API。
# NEXT_PUBLIC_GMAPS_KEY=

# 高德地图 JS API（建站点时「地图选点」用；不配则地址与经纬度仍可手填，只是没有地图）
# ⚠️ 两个都要填：JS API 2.0 只填 key 不填安全密钥时，地图不是报错而是**一片空白**。
# 申请时「服务平台」选 Web端(JS API)，两个值在同一个页面签发。
# 将来切 Google Maps 时替换 lib/amap.ts + components/ui/address-picker.tsx，表单侧不动。
# NEXT_PUBLIC_AMAP_JS_KEY=
# NEXT_PUBLIC_AMAP_JS_SECURITY_CODE=
